// Field-debug capture — see captureBuffer.ts for the full doctrine header.
export {
  installCapture,
  isCaptureInstalled,
  getCaptureText,
  getCaptureCount,
  clearCapture,
  downloadCapture,
  shareCapture,
} from './captureBuffer';
