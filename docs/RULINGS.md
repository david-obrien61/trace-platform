# RULINGS

**What this is:** every ruling David has made, ONE LINE EACH, newest first.
**What this is NOT:** the reasoning. Reasoning lives in the decision docs, and each line points at
its own. This file exists to be READ IN FULL at the start of every session — CLAUDE.md §10 STEP 0
— so it must stay short enough that reading it is free. **A ruling that needs a paragraph here is a
ruling whose decision doc is missing.**

**Why it exists:** four rulings evaporated between the session that made them and the session that
built against them (2026-07-30). Not disputed — forgotten, because a ruling lived only in the prose
of a close-out entry that scrolled out of §3 at N=3. A decision the platform has to re-derive is a
decision nobody made.

**Columns.** `STATE` is one of:
- **IMPLEMENTED** — the ruling is in code or schema, and the `GUARD` column names what keeps it there.
- **PARTIAL** — some of it landed; the line says which part did not.
- **OPEN** — ruled, nothing built yet.
- **OWED** — the ruling itself is owed *by David*; the question is on the table and unanswered.

`GUARD` names the mechanical check, if one exists. **A ruling with no guard is a ruling waiting to
be broken by someone who knows it** — including its author (2026-07-29: four self-catches in 24
hours, three by the author of the rule). `—` is an honest answer and it renders as debt.

| Date | Ruling | State | Guard | Source |
|---|---|---|---|---|
| 2026-08-29 | 🔴 **R-23 — A READ AGAINST A LIVE CUSTOMER INTEGRATION IS READ-ONLY, STORES NOTHING, AND ITS RAW RESPONSE IS CAPTURED TO A FILE OUTSIDE THE REPOSITORY.** David, on approving the QuickBooks item read: *"THIS PASS IS READ-ONLY AGAINST INTUIT. Not one write, not one test invoice, not one 'just to see'. A QBO invoice you delete still consumes its number and leaves a trail Terry's accountant sees."* And on the capture: *"write the raw response body to a file OUTSIDE THE REPO and report the path. It is a customer's live accounting data — nowhere a commit can sweep it up, same class as the JWT that sat in settings.local.json. A parsed view on screen is fine ON TOP of the raw file, never instead of it: re-reading must never require re-querying."* 🔴 **THREE CLAUSES, AND THE THIRD IS THE ONE THAT WILL BE FORGOTTEN FIRST.** **(a) READ-ONLY** — the granted scope permits writes and the token is in hand; the restraint is the ruling, not the capability. **(b) STORE NOTHING** — persisting a customer's chart of items (or invoices, or accounts) is a SEPARATE ruling that has not been made, and a read that quietly persists makes it by default. **(c) CAPTURE THE RAW BODY OUTSIDE VERSION CONTROL, ON FAILURE TOO** — an error body is the artifact that distinguishes a 401 from a 403, and losing it means querying a customer's books a second time to find out. ✏️ **THE FILE IS NOT ABOUT CONVENIENCE, IT IS ABOUT WHERE.** A `service_role` JWT sat in `.claude/settings.local.json` this week; the fix for live customer data is not to redact it, it is to keep it where git cannot reach it. ⚠️ **Applies to every integration read, not only QuickBooks** — the next one is Terry's invoice history, and it is the same rule. | **IMPLEMENTED** (for the item read) | `itemList.test.ts` §D asserts `QBO_ITEM_QUERY` contains no write verb; the endpoint returns `stored: false` in its own payload; the capture fires BEFORE any render, on failure too (owner-test CARD 6 checks `git status --short` is clean). ⚠️ **NO CAP asserts "no new Intuit write site"** — a future POST would pass every check. | ledger #229 · `docs/owner-tests/quickbooks-invoice-full-surface-test.md` CARDS 5–8 |
| 2026-08-28 | 🔴 **R-22 — THE OWNER ROLE CARRIES FULL AUTHORITY, INCLUDING ASSIGNING ALL ROLES. `owner_id` IS THE ACCOUNT HOLDER OF LAST RESORT, NOT AN AUTHORITY MECHANISM.** David, verbatim: *"Currently I'm the owner. Lauren needs all perms and authority to act. So changing a role from manager to owner is a decision the owner makes, then the new 'owner' can administer the system and assign all roles. Once the 'owner' leaves a new 'owner' can be promoted. If the original owner_id departs someone has to take that place. So the perms need allow the role to assume the perms within that role."* 🔴 **THIS IS [[2026-07-30]] FOLLOWED ONE STEP FURTHER.** That ruling made the owner pass the check like everyone else by deleting the owner branch from both permission functions; it did NOT move the ~45 POLICIES that fence on `owner_id`, and it said so — 20260730c's own V6 comment reads *"Retiring the policy-level owner branch is a SEPARATE build, and this is its first input."* This is that build. 🔴 **AND THE MECHANISM IS THE PART WORTH KEEPING: A MANIFEST FLIP ALONE CHANGES NOTHING.** `has_permission()` reads the STORED `business_members.permissions` array while the client COMPUTES `OWNER_LOCKED_SET` from the manifest — two materialisations of one authority, moved by different means — so flipping a status to `enforced` grows the client set, leaves the server at 54, and reproduces the *"Tax: not identified"* inversion in the opposite direction: **the client offering controls the database refuses.** Every future status flip carries a funnel re-materialisation or it is half a change. ✏️ **THREE SUB-RULINGS, all David's on the same day:** **(a)** `service_offerings` gets **NO delete verb** — R2 stands, and the hard delete is REMOVED rather than gated, because it raised 23503 on any offering ever sold and permanently destroyed one that was not; **(b)** the invite goes behind a SECURITY DEFINER RPC rather than an INSERT policy, because the authority trigger is BEFORE UPDATE and would not have covered an INSERT, so the permission array must be resolved server-side; **(c)** `removeMember` and `setMemberActive` are **ACCESS CONTROL, not data edits** — they belong in the funnel beside `assign_member_role`, and until they get there they render locked-with-a-reason rather than refusing. | **PARTIAL — Stage 1 (ACCESS) is BUILDER-COMPLETE and owner-proof is OWED; Stage 2 (AUTHORITY) is OPEN and not started.** The split is deliberate rather than incremental: an OWNER-role actor can now reach services, invitations and the roster, but still **cannot assign a role**, because every act that CREATES an owner belongs in one audited door (`assign_member_role`) instead of arriving early through the invite path. Stage 1 therefore refuses an OWNER-role INVITE from anyone but the account holder — **Lightning's call, flagged for overrule, not David's.** | **STAGE 1:** `20260828_owner_role_carries_authority.sql` — five member policies + `create_invitation` + the floor at 57, each with a V-check (**V7 impersonated is the headline; V8 is the MANAGER control**). **capA assertion 3** fails the build if the manifest and the newest `$OWNER$` literal disagree — which is what makes the two materialisations safe rather than duplicated. **capQ** holds the three flipped strings through a **self-pruning declaration** (`r-b2-wired-since-declarations.json`): a declaration for a string that is still declared-unwired, or that the 20260727 snapshot never held, FAILS THE BUILD. **`rosterAuthority.test.ts`** (47 probes) holds sub-ruling (c), and **`serviceWriteFailure.test.ts` §D** holds (a) as four negatives — the way (a) regresses is somebody re-adding a convenience delete. **STAGE 2: no guard, because no code — and the one guard it must ship is named now: NO ACTOR MAY CHANGE THE ROLE OF THE `owner_id` HOLDER.** | David 2026-08-28 (Pass 2 Stage 0 answers) · ledger **#228** · story *Hand over the keys — the owner role outlives the person who opened the account* |
| 2026-08-28 | 🔴 **R-21 — WHETHER AN ORDER HOLDS STOCK IS DECIDED BY ITS ORIGIN, NOT BY ITS STATUS.** 🔴 **A HISTORY ORDER — captured by OCR from a paid paper invoice — NEVER carries a `business_inventory_id` on any line.** David: *"The goods left the farm before Cultivar existed, the SKUs are a document's, not our lots, and a lot id on such a line silently reduces what the business can sell with no ledger row and nothing to reverse. Verified 08-28: history_lines_with_lot = 0, and that invariant must hold."* 🔴 **A WILL-CALL ORDER — an online order the customer will collect — ALWAYS carries a `business_inventory_id`.** David: *"It is paid and uncollected. The tree is theirs, the gap between paid and gone must be protected, and the hold is what stops the same tree being sold at the register."* 🔴 **BOTH SIT AT `invoiced`. Same status, opposite rule. That collision is what this ruling exists to prevent** — and it is why the deciding field is **`order_kind`**, not `status`: [[R-20]] has just made `invoiced` the one word both of them wear, so a reader who asks the status what to do gets the same answer for the two cases that need opposite ones. ✏️ **Corollary, and it does NOT contradict the online-shop ruling:** David: *"'no reservation, first purchase wins' governs BROWSING AND CARTING. The hold begins AT PURCHASE. Will-call is a completed purchase, so it holds. Same rule, different point on the line."* | **PARTIAL — the history half is IMPLEMENTED and guarded; the will-call half is OPEN and has NO guard, because the online shop that originates a will-call order does not exist** (`online_shop` is `status:'planned'`). Nothing is owed *by David* on either half: the rule is settled in both directions, and only the second half's code is missing. | **HISTORY HALF:** `historyOrder.ts:69-73` types the field as the literal `null` on `HistoryOrderLine`, so **setting a lot id is a COMPILE error**, and its header states the invariant at the site (`:22-28`, `:43`); `historyOrder.test.ts` asserts it in both directions (`:66`, `:90`, `:198`) plus a source probe that the writer still maps the null through (`:247`); `migrate-order-status-vocabulary.mjs:114` **guard G2 refuses to write at all** if any history line carries one. **WILL-CALL HALF: `—`** — no cap can guard a path with no code; the guard is owed WITH the build, and this cell is the debt. | David's ruling 2026-08-28 · ledger #226 · `user_stories.md` → *Will-call — a customer orders online and collects it themselves* · pairs with **R-20** (both origins sit at `invoiced`) · [[D-52]] on-hand / committed / available · the *Online shop* story's **THERE IS NO RESERVATION** clause, which this corollary bounds rather than overturns |
| 2026-08-28 | 🔴 **R-20 — THE ORDER STATUS VOCABULARY MATCHES QUICKBOOKS: `pending` · `invoiced` · `fulfilled` · `cancelled`. `confirmed` IS RETIRED.** David: out of `pending` an order goes to **INVOICED** and is put on the schedule — **scheduling lives on the DELIVERY row, not in a fifth status**. **THIS CLOSES R-STATUS**, open since 2026-07-15, which `orderStatus.ts` had carried as a self-declared *"RATIFICATION-PENDING"* proposal in its own header. 🔴 **AND THE RATIFICATION WAS AN INVENTORY OPERATION, WHICH IS THE PART NOBODY WOULD HAVE PREDICTED FROM THE WORDS.** `invoiced` was ALREADY LIVE on 12 rows — written only by the QuickBooks push — and ABSENT from `ORDER_STATUSES`; `fetchCommittedByLot` builds its query as an **allow-list from that enum**, so those orders held **zero** commitment because of an enum they were not in. Admitting the value admitted the rows. **Four were walk-ins whose stock had already physically left** (born `fulfilled`, decremented at checkout, status then overwritten by the push) and would have been **double-subtracted**; they were settled to `fulfilled` by id, each re-verified against its own ledger delta. **Eight were genuinely open sales**, and their 32 units entering the derivation is the **CORRECTION the ruling buys**. ⚠️ **`holdsCommitment()` did not move and must not** — it still excludes exactly `fulfilled` and `cancelled`. **LAWNS holds zero `invoiced` rows, so the live customer's available-to-sell is untouched.** | **IMPLEMENTED — code + data shipped together (they had to: a deploy where the code says `invoiced` and the rows say `confirmed` shows Lauren an empty screen).** | `orderRosterFilter.test.ts` §A asserts `confirmed` is absent from the vocabulary and `invoiced` is present; `historyOrder.test.ts` §A2 asserts the history status follows the delivery under the new words; `migrate-order-status-vocabulary.mjs` **guards G1–G5** (walk-ins re-verified by id · history-lot invariant · **LAWNS must not move** · every mover attributed to its orders · zero `confirmed` survives), and **refuses rather than warns**; the QBO write-back now preserves a terminal status so this class cannot recur | David's ruling 2026-08-28 · ledger #225 · `docs/owner-tests/orders-roster-full-surface-test.md` · `user_stories.md` *Filter the orders roster by status* · supersedes the `Confirmed` wording in the order-status-lifecycle story |
| 2026-08-27 | 🔴 **R-18 — EVERY INCIDENT THAT DOES NOT COMPLETE THE INTENDED ACTION IS LOGGED, AND THE LOG IS REVIEWABLE AND ARCHIVABLE.** David, verbatim: *"every incident that doesn't complete the intended action this is a log we can review and archive."* **The RULE is settled; the SHAPE is not.** 🔴 **What counts is a FAILED INTENT** — a write refused by RLS/constraint/permission · a read refused 401/403 · a request that failed on network, timeout, 4xx or 5xx · a session lost, wiped or unrefreshed · an action gated off and abandoned · a queued item that did not drain · an import row refused · anything the app catches and shows as an error. **Not renders, not navigation, not successful reads or writes — if it completed, it does not go in.** 🔴 **THE TRAP IS PART OF THE RULING, NOT AN IMPLEMENTATION DETAIL: THE LOG MUST BE WRITABLE BY SOMEONE WHOSE AUTH IS BROKEN.** A null `auth.uid()` is precisely the failure most worth capturing, so a table RLS-gated on it **goes quiet during the crash** — required shape is **write-open, read-closed** (anyone inserts; only the business owner reads; `business_id`-null rows are platform-only), a deliberate **[[AC-2]] exception carrying a WHY.** **It fires for a GUEST** — a shopper who hits an error leaves rather than reports. **Four prohibitions:** failed intents only, never everything · **fire-and-forget — a logging failure must never break the action it describes** · never prettify the error · **never gate it behind a module: this is CORE, an owner does not subscribe to knowing why their software failed.** ✏️ **THE PROVING CASE IS REAL AND RECENT:** 2026-08-26/27 Lauren could not invite Joel; the entire diagnosis (a `refresh_token` 400, repeated *"no local session — settled logged-out (wipe)"*, a 403 on `GET /rest/v1/invitations`, and her SHA) **survived only because David asked her to copy it out of the browser by hand. One closed tab and the morning is unexplainable.** | **OPEN — ruled, nothing built.** 🔴 **BLOCKED ON A SECURITY RECON, not on effort:** the write-open/read-closed policy and its abuse surface (rate limit + size cap) is a recon, not a patch. Two further shapes are open and **must not be answered by picking a default in code**: retention/archival semantics, and whether the write can ride an existing endpoint (**`api/` is 12/12** — §6 r11 makes #13 a STOP-and-surface event). ⚠️ **What exists does NOT satisfy it:** the `captureBuffer.ts:20` 600-entry `localStorage` ring is BUILDER-COMPLETE and **never leaves the device**; `audit_log` ([[D-51]]) and `business_inventory_ledger` ([[D-50]]) are DOMAIN event tables and neither takes an exception or a failed call | — 🔴 *(honest answer: **none, and the absence is the ruling's own subject.** The 2026-06-27 wrap-and-capture decision reached this exact question — *"should errors be persisted?"* — answered it with a ring buffer, and **filed no ruling**, so the request read as never-built for two months. The only thing that raises it today is `user_stories.md:884`, filed 2026-08-27 for precisely that reason. A cap is unwritable until the policy shape exists)* | David's ruling, restated 2026-08-27 · `REQ-incident-log.md` · story `user_stories.md:884` · ledger **#221** · the 2026-08-26/27 invitation incident · `docs/decisions/2026-06-27-wrap-and-capture.md:24-34` · [[D-50]] append-only · **R-18 was a genuine HOLE, not a reservation — see the note on R-19** |
| 2026-08-25 | 🔴 **R-19 — A FIELD LIST THAT CLAIMS TO COVER A RECORD MUST COVER IT, AND A CHECK MUST SAY SO. *"i want it fixed and i want it fixed right the first time"* · *"they will have this with them when i leave… if i leave and 30 min later they go hey i cant find someone to add an order i get egg on my face"*.** **THE STANDARD, IN ONE SENTENCE: wherever a cashier puts a customer on an order, they find the same people by the same terms, and the customer's details arrive complete and correct.** 🔴 **THE EVIDENCE IS A COUNT, NOT A COMPLAINT: FOUR DEFECTS IN ONE WEEK, ONE SHAPE — a hand-maintained field list omitting a field it should hold.** `email` missing from `customerUpsert`'s `offer()` (0840b30) · `organization_name` missing from the roster search (8b26348) · the legacy address columns missing from the picker search (f1c26ef) · **the ADDRESS missing from `CustomerCapture`'s select-copy**. `customerFieldRegistry.ts` was built to end exactly this and all four paths did not read it. **Fixing the fourth alone guarantees a fifth**, which is why the ruling is about the CHECK and not about the fourth defect. ✏️ **SCOPED BY DAVID TO THE `customers` ENTITY FOR THIS PASS** — other entities carrying the same shape are REPORTED, not swept. | **IMPLEMENTED for `customers`, 2026-08-25 (ledger #220).** One list (`CUSTOMER_ORDER_FIELDS`) + one copy (`customerOrderFill`/`customerOrderInput`, billing-first) + one search on both order doors (`<CustomerSearch>` mounted in `ScanOrder`, retiring tech-debt **#116** and **#117**). ⚠️ **NOT SWEPT, BY INSTRUCTION:** `orders`, `business_inventory`, `deliveries`, `assets`, `cost_objects` carry the same shape and are named in the close-out. ⚠️ **The registry is a FLOOR, not the schema** — there is no `CREATE TABLE customers` in the corpus (#39) and this machine has no catalog access, so the check asserts against the REGISTRY and DECLARES the four columns it does not cover | ✅ **`customerFieldCoverage.test.ts` — every registry field has an EXPLICIT DISPOSITION (carried, or excluded WITH A REASON) and there is no third option; a new field with neither FAILS THE BUILD (B1/B6).** Asserts in BOTH directions — a stale exclusion for a field the registry no longer has also fails (B3), so it cannot rot the way `OWNER_ONLY_PENDING` did. **18 planted defects, each FAILING and NAMED, each restored byte-identical.** 🔴 **AND THE CHECK'S OWN FIRST VERSION WAS BLIND TO THE BUILD'S HEADLINE DEFECT:** commenting out `setAddress(...)` left every probe GREEN, because the source probes read raw text. Comment-stripping added — then that stripper ate the imports, because a LINE comment can contain `/*` (`/checkout/*`). **Both holes were found by red-first, not by review** | David's ruling, 2026-08-25 · ledger #220 · **first instance IS this build** · retires #116/#117 · [[R-12]] (make illegal states unrepresentable) · ⚠️ **filed as R-19 at David's instruction, leaving R-18 empty; ✅ THE HOLE WAS FILLED 2026-08-27** — R-18 was checked repo-wide and in `git log -S` and was a genuine GAP, never a reservation (its only occurrence anywhere was this clause saying so), so the incident-log ruling took it rather than appending at R-20. **Nothing cited R-18, so nothing was renumbered** |
| 2026-08-24 | 🔴 **R-17 — THE DASHBOARD'S ZEROS ARE NOT A DEFECT. WHAT THE SCREEN OWES IS A VISIBLE NOT-CONNECTED STATE AND CACHED DATA.** David's words, quoted rather than paraphrased: *"the dashboard displaying 0 was on airplane mode and no wifi so it rendered correctly in my opinion it should also display you are not connected and cash some data"*. **Read as: the zeros rendered CORRECTLY given that nothing could be read — the ruling is that this is not a bug to chase.** What is owed is **(a) a visible not-connected state** and **(b) cached data**. *(Measured 2026-08-24 on `1c60964`, iPhone Safari, normal tab, airplane mode ON with wifi manually OFF.)* ✏️ **THE TWO NOTES BELOW ARE THUNDER'S, ATTACHED TO THE RULING AND NOT PART OF IT — David ruled the two clauses above and nothing more.** **① A CACHED FIGURE MUST CARRY ITS OWN AS-OF TIMESTAMP, OR THE CACHE MOVES THE ILLEGAL STATE UP A LAYER.** Without one, stale and current become **the same pixels** — the screen stops saying *nothing was read* and starts saying *this number*, with nothing distinguishing a live figure from a day-old one. That is **R-11's class one level out**: the failure and the value are no longer distinguishable, except here the collapse happens in the DISPLAY rather than in a return type, so `tsc` cannot reach it. **A dashboard that answers a question it did not ask anyone is a worse outcome than four zeros under a banner.** **② THE PRECEDENT IS ALREADY IN THE TREE AND IT HAS EXACTLY THIS COST.** `usePlant.ts` mirrors every successful plant resolution into `localStorage` under `plant_cache:<tagId>` behind a 24-hour TTL (`CACHE_TTL_MS` `:73`, read `:77`, write `:91`) and **renders SILENTLY STALE on a hit** — up to 24h-old qty, price and status, with nothing on screen saying so. ✏️ **ONE CORRECTION TO THAT CITATION AS IT WAS HANDED TO ME, made because the fact changed four days ago and the wrong word would teach the wrong lesson: it is no longer *undocumented*.** It was undocumented until **2026-08-23**, when it was DECLARED at the site (ledger #206); the declaration block runs **`usePlant.ts:41-65`** — the cited `41-62` lands inside it and stops three lines short — the *"SILENTLY STALE"* sentence the note rests on is at **`:56`**, and the implementation is `:66-93`. **The staleness cost is real, unchanged, and still unclosed; only the word *undocumented* is stale.** | **OPEN — ruled, nothing built.** Both clauses are unbuilt: no dashboard surface reports a not-connected state, and no dashboard figure is cached. ⚠️ **Note ① is a constraint on the (b) half, not a second ruling** — if cached data ships without an as-of stamp, this row is the record that the cost was named first | — 🔴 *(honest answer: **nothing mechanical reads this yet, and nothing should pretend to.** The six-state ruling's own guard **capB** is still OWED, and a not-connected dashboard is another case whatever capB eventually asserts would have to cover. A staleness stamp is not a shape a cap can read either — it is a render decision, the same class as the `usePlant` half that has been named twice now and built neither time)* | David's ruling, 2026-08-24 · measured on `1c60964`, iPhone Safari, airplane mode + wifi off · **Thunder's two notes attached, NOT ruled** · `usePlant.ts:41-65` · [[R-11]] · precedent ledger #206 |
| 2026-08-23 | 🔴 **A TRIAL TERM IS A TERM, NOT A METER — THE CLOCK DOES NOT PAUSE AND DOES NOT RESET.** David's words: turn a module off at **day 12 of a 14-day trial and TWO DAYS REMAIN**. Turn it off mid-billing-month and **THE MONTH RUNS OUT** — the business is not charged for the next month, and there is **NO PARTIAL REFUND**: *"that's bad math."* ✅ **THE MECHANISM IS ALREADY CORRECT AND MUST NOT BE TOUCHED:** `trial_days` is snapshotted at seed (2026-08-01 ruling, row below) and counts **wall-clock**, and `term_rewrite_refused` (`20260801c:281`) proves the term travels with the row rather than being re-read live. **So this ruling changes NO mechanism.** ⚠️ **What it makes owed is COPY:** the card must state the policy plainly rather than implying a meter — a countdown that keeps running while a module is off is honest under this ruling and looks like a bug without the sentence. **Partial refunds are OFF THE TABLE BY CONSTRUCTION** — proration requires a billing engine that does not exist and is not wanted. | **IMPLEMENTED as mechanism — nothing to build, nothing to change.** ⚠️ **The COPY is OWED** (a build, not a ruling): the marketplace/tile card does not state the term-not-meter policy anywhere today | — *(owed with the copy; **no cap reads card prose**, and none should — this is a sentence, not a state)* | David's ruling 1, this session · ledger #205 · **answers M-A** · builds on the 2026-08-01 snapshot ruling |
| 2026-08-23 | 🔴 **A CONFIDENTIAL COLUMN IS ENFORCED IN THE DATABASE, NOT BY A LINTER. C-B's ANSWER IS *NO CAP* — BUILD THE WALL AND THE CAP IS UNNECESSARY.** Industry practice is **column-level access control in the database**, and in this stack that already has a proven shape: **the `labor_resource_wages` split (`20260621_financial_wall_phase2.sql:48-83`)** — the confidential columns moved to their own relation, RLS on that relation keyed to the permission (`permissions ? 'view_wages'`), the parent's copies **cleared to NULL**, so the value is not in the member-readable row at all. **Postgres enforces it and no client change can defeat it.** ✏️ **ONE FACTUAL CORRECTION TO THE SHAPE AS IT WAS DESCRIBED WHEN RULED, made because the wrong precedent would be copied: `labor_resource_wages` is a CHILD TABLE, not a VIEW.** A view omitting the column is a legitimate alternative implementation of the same ruling, but it is **not what this repo did**, and the child-table split has the stronger property — **the value is not merely unselected, it is not present in the parent row.** 🔴 **WHY NOT COLUMN GRANTS DIRECTLY — VERIFIED AGAINST THIS PROJECT, NOT ASSUMED: the claim HOLDS.** Every logged-in user connects as the single Postgres role `authenticated` (every RLS policy in the corpus is written `TO authenticated`; role differentiation happens INSIDE the policy via `auth.uid()` → `business_members.permissions`), so **`GRANT SELECT (cols)` cannot distinguish an owner from a manager from staff** — it would apply identically to all three. Column grants are the textbook answer and they are **the wrong tool for this stack**, for a stated reason (§6 r10). 🔴 **AND THE REASON A CAP WAS NEVER THE ANSWER, RECORDED SO IT IS NOT RE-PROPOSED: a select-string linter is BLIND TO `select('*')` — which is EXACTLY how C-D got in** (`PMI.tsx:225`). The cap would have been green over the defect it was commissioned to find. | **RULED — C-B IS ANSWERED AND CLOSED: A CAP IS NOT OWED.** The BUILD (splitting `business_inventory.unit_cost` and `business_service_log.cost` behind their own gated relations) is **OPEN and unbuilt**; #81 stays open | — 🔴 *(and that is the ruling: the guard is **the database**, not a script. When the split lands, the guard is the RLS policy itself + the existing `inventory-read-model.rls.mjs` card N-7 (`owed` today — it exists as a probe and has never been owner-run), which the split is what finally makes provable, with no new cap)* | David's ruling 2, this session · ledger #205 · **answers C-B (no cap owed)** · [[#81]] · precedent `20260621_financial_wall_phase2.sql` |
| 2026-08-23 | 🔴 **TILE CAPABILITY IS DECLARED, NOT ACCIDENTAL: `phone` \| `desktop` \| `either`.** A field on `TileEntry`, reusing **the vocabulary the owner-test board already uses** (`DEVICE:` — 31 desktop / 1 phone / 1 either), so the platform states which device a surface is for instead of leaving it to be discovered. **Today DataSheet's desktop-ness is ACCIDENTAL** — nothing declares it — **and a table rendered on a phone is indistinguishable from a bug.** David: *"make it phone/desktop/either and move on — if it needs changing we will readdress."* ✏️ **Filed as the RULING ONLY. The field, its DEFAULT, and any cap are a BUILD and were explicitly out of scope when this was ruled** — in particular the default is not decided here, and choosing one in code would be answering a ruling with a constant. | **OPEN — ruled, nothing built.** The build is OWED: add the field to `TileEntry`, decide its default, backfill 33 tiles | — *(owed with the build; the natural guard is the one `verify-tile-fields` already owns — it derives its rules from the `TileEntry` interface, so a new required field arrives enforced with no second edit, the #179 property)* | David's ruling 3, this session · ledger #205 · vocabulary from `docs/owner-tests/*` `DEVICE:` |
| 2026-08-23 | 🔴 **READ HONESTY IS A TYPE, NOT A DISCIPLINE — THE DRAFT RULE IS RULED IN SHAPE, AND THE SHAPE IS A DISCRIMINATED UNION.** The standing draft (*a read whose error path returns a value must keep FAILED distinguishable from EMPTY* — A9 on the read side) is enforced by `type Result<T> = { ok: true; value: T } \| { ok: false; error: Error }`, **not by convention**: the compiler refuses to let a caller reach `.value` without handling the failure branch. 🔴 **THIS IS WHY A HELPER CANNOT DO IT, and the evidence was already counted: `readPricingConfig` ALREADY returns `{data, error}` correctly and ALL SEVEN of its callers throw the error away.** A shape that *can* be ignored *will* be — a union cannot be. **`tsc` is already in `npm run verify`, so this converts a discipline problem into a BUILD FAILURE** at zero new tooling (§6 r10 — the standard earns its value here). ✏️ **The lived case, recorded because it is what the rule costs when absent:** SocialSetup's config read failed, fell back to instagram-only, and **David lost hours believing his SAVE was broken** — three distinct situations (narrow config · missing config · failed query) rendered identically. | **PARTIAL — THE TYPE EXISTS AND HAS ITS FIRST INSTANCE; THE 30-SITE BUILD STAYS OWED.** ✅ **`packages/shared/src/utils/readResult.ts` MINTED 2026-08-23 (ledger #206)** — `ReadResult<T> = {ok:true;value:T} | {ok:false;error:ReadFailure}` — and applied to the **3 reads** in `stockLineResolver`, converting **5 call sites** in 3 files. 🔴 **THE RULING'S CENTRAL CLAIM IS NOW MEASURED, NOT ARGUED: flipping the two signatures produced 36 NET-NEW `tsc` ERRORS, one per site that had been discarding a failure.** The compiler found every caller with no list, no grep and no reviewer. ⚠️ **THE REMAINING ~27 CONFIRMED SITES (floor; ceiling 91) ARE UNTOUCHED AND STILL OWED** — including `readPricingConfig`'s seven, which are the ruling's own headline evidence and were deliberately NOT taken in this build (STEP 5 scope). ⚠️ **The auth reads (7) are still explicitly NOT covered by this ruling.** ✏️ **ONE DELIBERATE DEVIATION FROM THE LITERAL SPELLING, recorded at the site rather than silent:** the failure branch carries a structured `ReadFailure {kind:'offline'|'error'; message}` instead of `Error`, because every consumer needs the one discriminator an `Error` subclass would only re-encode less legibly — **dead zone (retry with signal) vs the server answered NO (retrying is pointless).** The ruled property — a failure branch that cannot be skipped — is unchanged | ✅ **`tsc`, exactly as ruled — and it is ALREADY LOAD-BEARING**: the union is in `npm run verify`'s first metric, so a future caller that ignores the failure branch fails the build. ⚠️ **Nothing yet forces a NEW read to adopt the union** — that cap is owed with the 30-site build | David's ruling 4, 2026-08-23 · ledger #205 · **first instance ledger #206 / `3d0944b`** · **rules the standing DRAFT RULE in shape** · #204's `stockLineResolver` finding was its live instance and is now FIXED |
| 2026-08-23 | 🔴 **A WRITE MUST PROVE IT WROTE — E5's ANSWER IS `RETURNING` / THE RETURNED REPRESENTATION, AND CHECK THE COUNT.** **A PostgREST write matching ZERO rows returns SUCCESS WITH NO ERROR.** So a save RLS refuses reports success, **the typed value stays on screen**, and nobody knows — and on a phone, in a row, with no console, that is the worst version of the class. The fix is not a new mechanism: ask for the affected rows back and **assert the count is what you intended**. 🔴 **THE GENERAL FORM, SHARED WITH THE RULING ABOVE — MAKE ILLEGAL STATES UNREPRESENTABLE.** *"Failed"* and *"empty"* being the same value **IS** the illegal state; R-11 removes it on the read side, this removes it on the write side. | **PARTIAL — THREE INSTANCES NOW, AND THE THIRD IS THE FIRST ON THE RULING'S OWN NAMED SUBJECT.** 🔴 **THIRD INSTANCE 2026-08-25 (ledger #217) — `customers`, THE TABLE THIS ROW NAMED AS "the live risk":** `customerUpsert.ts`'s fill-update went `updRows?.length === 0` → **`!== 1`**, so it now refuses the zero-row PostgREST shape AND a write that affected more rows than the single row it named. 🔴 **AND THE GUARD NAMED IN THIS ROW'S OWN `GUARD` CELL DID NOT RECOGNISE THE RULING'S SHAPE — the cap FAILED the build for adopting it.** `verify-zero-row-writes`'s CHECKED regex knew `length === 0` / `< 1` / `!x?.length` / `> 0` and **not `!== 1`**, so a site upgraded to the ruling's own, strictly stronger form was reclassified `NEEDS_CHECK`. Regex widened to the exact-count form + probes **Z3b/Z3c/Z3d** (the third a negative control: mere USE of `data` must still read as unchecked). ✏️ **The measured consequence of that blind spot, and it is the finding: widening it moved THREE `scripts/lib/memberSession.mjs` sites OUT of the unchecked baseline — all three already written as `(data ?? []).length !== 1`, BEFORE this ruling existed.** They were correct all along and were being counted as debt. **A cap that cannot see the strongest form of what it measures manufactures false debt in one direction and false green in the other.** ⚠️ **STILL OWED: the ruling asked for `RETURNING` / the returned representation ACROSS the PostgREST write surface — this is one path on one table, not the sweep.** The baseline still holds **79 known unchecked app sites** (82 → 79, three unlocked, zero added). — **PARTIAL — TWO CLIENT-SIDE INSTANCES BEFORE IT.** ✅ **SECOND INSTANCE 2026-08-24 (ledger #212), on the module off switch:** `setBusinessModuleState` had been READING `enabled_before`/`enabled_after` off the RPC, LOGGING them, and throwing them away — so every caller had only `applied`, which is **a different claim** (it is also what a no-op and a NULL `p_enabled` return). They are now returned as a **TRISTATE, never coerced** — `Boolean(undefined)` is `false`, and `false` here MEANS off, so coercion would have manufactured the very proof this adds — and `disable()` asserts `applied && enabledAfter === false`. ⚠️ **The general lesson is the ruling's own: the acknowledgement is not the outcome.** — **FIRST INSTANCE 2026-08-24 (ledger #207).** ✅ **The same illegal state was found and removed one layer earlier than the ruling was aimed:** `store.save()` swallowed a quota / disabled-storage exception, so a LOCAL write that stored nothing reported success — and **ONLINE the engine returned `applied`, its strongest claim, with the database called ZERO times** (measured by call count, not reasoned). `StoreWriteResult` now carries the outcome and `submit()` proves the write rather than inferring it from an absent queue entry. ⚠️ **THE RULING'S OWN SUBJECT — a PostgREST write matching ZERO rows returning success — IS UNTOUCHED AND STILL OWED.** `customers` remains the live risk: latent under owner-only RLS, live the moment a non-owner writes | ✅ **`verify-zero-row-writes` — AND AS OF 2026-08-25 IT CAN SEE THE EXACT-COUNT FORM** (Z3b/Z3c/Z3d). It still RATCHETS rather than asserts: it fails a NEW unchecked site, it does not yet demand the check on the 79 baselined ones. *(This row's own prediction — "it names the paths, it does not yet assert the check" — held, and the reason it could not assert was partly that it could not RECOGNISE the check.)* ⚠️ **owner-test card 7 stays `needs-test`: the behaviour is ruled, not fixed** | David's ruling 5, 2026-08-23 · ledger #205 · **answers E5 in shape** · **client-side instance ledger #207 / `4056de8`** · [ui-control-standards.md](standards/ui-control-standards.md) §4 |
| 2026-08-23 | 🔴 **JUAN LOGS HIS OWN PMI. The staff member who PERFORMS the maintenance must be able to READ the schedule and LOG the work.** David: Lauren entering data Juan wrote on paper *"is going backwards"* — **it is the labour inversion TRACE exists to end, aimed at TRACE's own product.** ⚠️ **THE FACT, VERIFIED THIS SESSION FROM SOURCE RATHER THAN ASSUMED: `STAFF_DEFAULT_BUNDLE` is TEN strings** (`permissionManifest.ts:1260-1271`) **and contains NO `pmi:*`**; `pmi:read` and `pmi:update` sit in `MANAGER_DEFAULT_BUNDLE` (`:1242`). **So staff cannot open the PMI surface at all today** — this is not a polish gap, the capability is unreachable for the person who does the work. 🔴 **NO BUNDLE WAS CHANGED AND NO STRING MINTED, deliberately: Juan's role SHAPE is itself deferred (R-16) — clone MANAGER and subtract, or clone STAFF and add — and moving two strings into the staff floor would pre-answer it in the one direction that is hardest to reverse.** | **OPEN — ruled, nothing built.** The build is OWED and is BLOCKED on R-16's role-shape decision (David + Lauren, Wednesday) | — *(owed with the build; capQ/capP already parse the bundle literals, so whichever shape lands is asserted by the caps that exist)* | David's ruling 6, this session · ledger #205 · blocked on **R-16** · `permissionManifest.ts:1242, :1260` |
| 2026-08-23 | 🔴 **THE MANAGER MAY SEE MAINTENANCE COST — C-D IS CLOSED AS RULED, NOT AS A DEFECT.** David: small business, **most things are discussed in the open**, and the person scheduling maintenance **arguably should know what it costs**. So `business_service_log.cost` reaching a `pmi:read` session is **the intended behaviour**, not the #81 class on a second table. ⚠️ **ONE THING IS STILL OWED AND IT IS A DOC FIX, NOT A CODE FIX — NAMED HERE, DELIBERATELY NOT MADE:** the PMI module header (`PMI.tsx:70-80`) asserts the premise *"a manager can legitimately hold pmi:read and NOT costs:read"* and describes a redaction, **while `PMI.tsx:225` does `select('*')`** on the service log. The header's claim is TRUE of the ASSET LIST (which genuinely is redacted with an explanation) and FALSE of the SERVICE LOG in the same file. 🔴 **A header claiming a protection it does not provide is the class that has cost this repo repeatedly** — the correction is one paragraph in that header saying the cost column is deliberately visible to `pmi:read` per this ruling. | **RULED — C-D CLOSED. No code change owed; the surface is correct as built** | — *(review-only. ⚠️ **The doc fix is OWED**: `PMI.tsx:70-80` currently reads as a protection claim, and nothing mechanical reads a comment)* | David's ruling 7, this session · ledger #205 · **closes C-D as ruled** · `PMI.tsx:70-80, :225` |
| 2026-08-23 | ✅ **THE FREE-FORM DISCOUNT ALREADY EXISTS AND IS SETTLED — F-B IS ANSWERED, AND NOTHING IS OWED.** A seller **can change the delivery fee or the per-plant price at checkout**, and **it is documented with who did what and when.** That is **R-7 working exactly as ruled — attribution, not approval** — and it means F-B's three-way fork (same string / its own string / should not exist) was asking about a capability that is already present and already attributed. **Answer: the same act, the same string, no new gate.** ⚠️ **The residual is unchanged and belongs to R-7, not here: the lost-money REPORT is the guard and it does not exist.** Attribution without the report is just permission — and a free-form amount is precisely the case where the sanctioned-tier-list control does nothing and the report does all the work. | **IMPLEMENTED — the capability exists and is attributed. NOTHING TO BUILD** | — 🔴 *(the guard is **R-7's lost-money report**, still owed. This ruling raises its value rather than adding a guard of its own)* | David's ruling 8, this session · ledger #205 · **answers F-B** · confirms **R-7** |
| 2026-08-23 | 🔴 **THREE ITEMS DEFERRED BY THE OWNER — RECORDED SO THEY ARE NOT REHASHED. A DEFERRAL IS A DECISION, and an unrecorded one reads as an oversight.** **① F-A (the fuzz's payment prompt for a $0 `core_optional`)** — David: *"we will have to live with this until after the demo and I figure out how I want this to work."* It stays **OWED, DEFERRED BY THE OWNER**, not unresolved-by-omission. **② The UNDER-PRODUCTION name** — deferred until after Wednesday. 🔴 **THE STATE IS RULED AND ONLY THE VOCABULARY IS OPEN: it means NOT READY FOR SELL.** A build may rely on the semantics; it may not pick the word. **③ Juan's role SHAPE** — clone MANAGER and subtract, or clone STAFF and add; David discusses it with Lauren on Wednesday. ⚠️ **THE CONSTRAINT HE MUST KNOW GOING IN, recorded because it is the expensive half: saving the floor's own set with no tenant row MINTS AN OVERRIDE, and that is a ONE-WAY DOOR.** The choice of starting point is therefore not a matter of convenience — it is the shape the tenant is stuck with. | **DEFERRED BY THE OWNER — all three. Not open questions nobody noticed; open questions with a date on them** | — *(nothing to guard; the point of the row is that the next session inherits the deferral instead of re-deriving the question)* | David's ruling 9, this session · ledger #205 · **F-A stays OWED-deferred** · **blocks R-13** |
| 2026-08-23 | 🔴 **CORE CANNOT BE SWITCHED OFF. ONLY WHAT IS NOT INCLUDED IN CORE CARRIES AN ON/OFF SWITCH — IF IT IS CORE, IT IS ON.** The affordance is DERIVED from `billing`, the field that already decides `enabledByDefault` (2026-08-02), so a disable control never renders for a core tile. **No new field, no second opinion about liveness** — the same one decider, read one more place. | ✅ **IMPLEMENTED 2026-08-24 (ledger #212).** The off path now exists: `mayBeSwitchedOff(billing)` (`tileRegistry.ts:541`) is **the NEGATION of `enabledByDefault`, not a second spelling of `billing === 'core'`** — exactly as the ruling worded it — and `Subscription.tsx` renders `Turn off` only where it returns true, in Included and in Active. **A core row renders NO control at all — absent, not greyed** (a greyed switch claims *not allowed*; the truth is *nothing to do*). `disable()` also re-checks it before writing, so the off path does not depend on a button failing to draw | ✅ **`moduleSeed.test.ts` A21b–A21g (6 probes, red-first proven BOTH directions** — always-false fails 4, always-true fails 3, incl. A21f over the LIVE catalog and A21g the negative control that catches an always-false predicate). ⚠️ **NOT the `verify-tile-fields` cap this row predicted** — that cap reads the tile registry, and the affordance is derived at the CATALOG's `billing`; the probes assert the union TOTALLY (A21e), so a fifth `ModuleBilling` member is covered with no second edit. **The cap is not owed; the derivation is what it would have guarded** | David's ruling 1, this session · ledger #203 · [[#201]] · **built ledger #212** |
| 2026-08-23 | 🔴 **THE DISABLED / LAPSED TILE IS THE FUZZ.** A tile the owner switched off, or whose trial reached expiry unpaid, **stays VISIBLE showing fuzzy data**; selecting it requests payment; once payment clears it is active and un-fuzzed. This answers M-C: **not absent, not a fifth flat state — the fuzz.** 🔴 **AND THE PART THAT IS A SECURITY REQUIREMENT, NOT A STYLE ONE: FUZZ IS NOT A CSS BLUR OVER REAL DATA.** The standing fuzz ruling already says it — the aggregate is computed **SERVER-SIDE** and delivered as **one number**; **the detail never reaches the browser.** A blur over a full payload is **#81 with a filter on it**: the value is in the response and one devtools line reads it. | 🔴 **STILL OPEN — AND NOW REACHABLE, WHICH IS THE CHANGE.** Nothing of the fuzz is built: a repo-wide search returns **twelve hits, every one prose**. But R-1's off switch landed 2026-08-24 (ledger #212), so a module CAN now be disabled — and `useModules.ts:122` still maps a disabled row to `'available'`, identical to never-enabled and to a missing row. ⚠️ **THE DISPLAY HALF WAS DELIBERATELY NOT PATCHED IN THAT BUILD, on this row's own words:** minting a fifth flat `TileState` is the one direction this ruling explicitly closes (*"not absent, **not a fifth flat state** — the fuzz"*), and a CSS blur is refused by name below. **Filed as tech-debt #100 rather than half-answered in code** | — 🔴 *(owed, and it is the SAME cap #81 wants: a confidential payload reaching an ungated client. A fuzz implemented as CSS would pass every cap we own today — capA reads gates, not selects)* | David's ruling 2, this session · ledger #203 · answers **M-C** · [[#100]] |
| 2026-08-23 | 🔴 **THE ROUTE RENDERS THE FUZZ; IT DOES NOT BLOCK.** A disabled module's route stays reachable and shows the fuzzy state — **it does not 404 and it does not redirect.** This is `PermissionRoute`'s six-state ruling (2026-07-30, *a page without access RENDERS AND SAYS SO*) extended one axis from PERMISSION to ENABLEMENT. **The route layer reads enablement in order to show the right thing, never to refuse** — which is why M-D's *"not a one-line gate"* framing was right about the cost and wrong about the shape: the router does not need a guard, it needs the overlay. | 🔴 **STILL OPEN — AND THE LATENT CASE WENT LIVE 2026-08-24.** ⚠️ The structural obstacle from #201 stands and is what sizes it: **`useModules` fetches the enablement overlay and `router.tsx` has no access to it** — a grep of all 48 routes for `business_modules\|module_key\|useModules\|enabled` returns ZERO. With the off switch shipped (ledger #212), a disabled module's route now renders and works **in full** — `/delivery-schedule` is gated on the `deliveries:read` PERMISSION (`router.tsx:149`). **Filed as tech-debt #101, blocked on #100: there is nothing to render until the fuzz exists, and adding a guard instead would be the refusal this ruling forbids** | — *(owed; the six-state ruling's own guard **capB** is still OWED, and this adds a seventh case to whatever it eventually asserts)* | David's ruling 3, this session · ledger #203 · answers **M-D** · extends the 2026-07-30 six-state ruling · [[#101]] |
| 2026-08-23 | 🔴 **MARKETPLACE SCOPE IS A SET, AND PROMOTION TO CORE IS A FIELD CHANGE.** A capability is **core**, or belongs to **one or several verticals**, and **may be ELEVATED to core** if it proves general enough or if several verticals need it. **David wants the capability even though nothing exercises it today** — #201 found 32 of 33 tiles are `vertical:'general'`, so free composition currently works **by the emptiness of the partition rather than by machinery**, and that is not a mechanism, it is a coincidence that will end. ✅ **The promotion mechanism ALREADY EXISTS on David's own #181 reasoning — rows for every module INCLUDING core — so a module moving to core is a FIELD CHANGE, not a migration against live tenants.** The 2026-08-01 ruling anticipated exactly this case and said so in its own words. | **OPEN — ruled, nothing built.** ⚠️ Answers **M-E** in the direction of YES-scope-it-but-as-a-SET: the axis moves off `TILE_REGISTRY` onto the catalog the marketplace actually reads, and *several* is the cardinality — **not one vertical per tile** | — *(owed; `verify-tile-fields` already derives its rules from the `TileEntry` interface, so a `verticals` field arrives enforced with no second edit — the #179 property)* | David's ruling 4, this session · ledger #203 · answers **M-E** · builds on the 2026-08-01 rows-for-everything ruling |
| 2026-08-23 | 🔴 **THE SERVER RESOLVES PRICE. THE CLIENT DISPLAYS IT. COST IS AN INPUT TO RESOLUTION, NEVER AN OUTPUT OF THE API.** A manager or staff member selling at wholesale, friends-and-family, contractor or **at-cost** does **NOT** see unit cost — they see **the price already resolved for that tier**. On an at-cost line the server reads the cost, applies the tier rule, and returns **a price**; the seller sees a number and never learns it is cost. Anyone who genuinely needs actual cost needs **`costs:read`**. 🔴 **THIS DISSOLVES C-A RATHER THAN ANSWERING IT: the Review-shows-retail / submit-charges-cost divergence CANNOT OCCUR once the client is not computing price at all.** The hazard was never really about the wall — it was about two callers of one pure function receiving different inputs, and this removes the second caller. | **OPEN — ruled, nothing built.** ⚠️ **THE #202 CONDITIONAL NARROWING IS CORRECT AS-IS AND MUST NOT BE CHANGED**, because `CartReview.tsx:182` computes pricing client-side TODAY. **When resolution moves server-side, checkout stops receiving cost FOR ANYONE INCLUDING THE OWNER and the narrowing becomes UNCONDITIONAL** — the conditional is a way-station, not the end state. ✅ **Cost still reaches Cost-to-Produce and the inventory-value metric under `costs:read` — those surfaces are ABOUT cost. Checkout is not.** | — *(owed with the move; `tierPricing.ts` is already the ONE pure function and already server-authoritative in `submit.ts:394` — this deletes a caller rather than adding a mechanism)* | David's ruling 5, this session · ledger #203 · **dissolves C-A** |
| 2026-08-23 | 🔴 **A LATE DISCOUNT MUST BE TAKEABLE AT THE REGISTER — AND THE TAX RECONFIGURES WITH IT.** The customer reaches Review and asks *"do you have a military discount?"*. The seller backs up, selects a discount type (military · contractor · friends-and-family · wholesale), **the displayed price recomputes AND THE TAX RECONFIGURES FROM THE NEW SUBTOTAL.** 🔴 **The tax half is the part that breaks silently: Texas is origin-based and tax computes on the DISCOUNTED subtotal, so a cached tax figure fails in the direction of UNDERCHARGING** — and an undercharge is invisible to everyone in the room. ✏️ **Recorded because it is a VALUE gap, not a mechanism gap: "military" is not a configured type today.** The mechanism exists — the Discounts page already offers `+ Wholesale`, `+ Municipality`, `+ Custom`. | **OPEN — ruled, nothing built** | — *(owed; `computeOrderPricing` already taxes the discounted subtotal — the risk is a Review that recomputes price without re-running it, so the guard is that the recompute calls the ONE function rather than patching a number)* | David's ruling 6, this session · ledger #203 |
| 2026-08-23 | 🔴 **PREFER ATTRIBUTION OVER APPROVAL — PLATFORM-WIDE, NOT A DISCOUNT RULE.** **AN APPROVAL GATE IS A PERSON STANDING NEXT TO ANOTHER PERSON, and it fails exactly when the approver is unavailable.** David's case is the whole argument: LAWNS has six employees, three in the office and two on sales; Lauren or the office assistant rings the sale, **Terry sells but does not ring it up** — so a gate fails when Lauren is out and Terry is outside **with a customer holding a card.** Therefore: **NO OWNER OVERRIDE to apply a discount. Everyone who takes a sale holds `order_discount:apply`.** The platform records **WHO DID WHAT AND WHEN**, a lost-money report surfaces a discount of $XX, Lauren investigates, **and what the business does after that is its own affair.** 🔴 **SIBLING OF *"surface, don't decide"*, NOT AN INSTANCE — CHECKED BOTH DIRECTIONS, and neither implies the other:** *surface-don't-decide* governs **WHO JUDGES** (TRACE informs, the owner judges); this governs **WHEN AUTHORITY IS EXERCISED** (the act proceeds and is recorded, rather than waiting on a second person). **(a) A system can surface-not-decide perfectly and still gate:** TRACE recommends a discount, decides nothing, and still makes Lauren wait for Terry — the sale is lost with the principle intact. **(b) A system can prefer-attribution and still decide:** every seller applies discounts freely with no approval, while TRACE refuses to offer a tier it disagrees with or picks one itself. **Two children of one parent — the owner is the authority, TRACE is the instrument — each reachable while the other is violated.** ✅ **AND THE CONTROL IS ALREADY FREE, which is why this costs nothing: THE TIER LIST IS THE CONTROL.** Lauren configures the types and the percentages; **a seller picking from that list can only give away a number she already sanctioned.** | **OPEN as a build; IMPLEMENTED as doctrine.** ⚠️ **The unbounded case is a FREE-FORM amount, where attribution does all the work and the sanctioned-list control does none** — filed OWED below rather than assumed either way | — 🔴 *(owed: the lost-money report is the guard and it does not exist. **Attribution without the report is just permission** — the ruling's whole cost-benefit rests on Lauren being able to SEE the discount afterward, and nothing surfaces it today)* | David's ruling 7, this session · ledger #203 · sibling of the 2026-08-23 outbound-initiation ruling, same parent |
| 2026-08-23 | 🔴 **TRACE NEVER INITIATES AN OUTBOUND ACTION — PLATFORM-WIDE.** Anything that reaches a person or a system **outside the business** — an email, an SMS, a social post, a purchase order, an invoice — **requires a human to name the moment. TRACE may be the mechanism once they have; it is never the trigger.** A drafted, queued or scheduled artifact is **prepared work, not a sent one, and must be visibly distinguishable from a sent one.** ✏️ **THE WORDING WAS CORRECTED BEFORE FILING, and the correction is the load-bearing part:** the consumables story said *"nothing leaves the building on its own"*, which **would have forbidden the Blotato publish path — a path that EXISTS and is CORRECT**, because it is keyed on an explicit `postId` from a button. **The rule is about INITIATION, not about whether TRACE is the mechanism.** 🔴 **SIBLING OF *"surface, don't decide"*, NOT AN INSTANCE OF IT — checked, not assumed, and neither implies the other:** *surface-don't-decide* governs TRACE vs the owner's **JUDGEMENT** (inform, never override) **inside** the product; this governs TRACE vs the owner's **VOICE AND REACH** (act, but only when they named the moment) **toward third parties**. A system can surface-not-decide perfectly and still auto-send — it flagged, then emailed anyway; and a system can never-initiate and still decide — it refuses to draft what it disagrees with, but only sends when told. **Two children of one parent (the owner is the authority, TRACE is the instrument); each is reachable while the other is violated.** | **IMPLEMENTED as doctrine, and TRUE IN CODE TODAY — swept, not assumed. SEVEN outbound-capable paths, ZERO that initiate.** ① order confirmation → CUSTOMER (`useSubmitOrder.ts:164`) fires on the Pay click ② silent-partner analysis → PROSPECT (`ingest.ts:101`, `action:'send'`) — sole caller `DiscoveryInspect.tsx:110` `sendAnalysis()`, a button needing a typed recipient, and **generation and sending are separate acts** ③ QBO invoice → Intuit, on Pay ④ QBO token refresh → Intuit ⑤ Blotato — declared inert, and the only `published` write is keyed on an explicit `postId` ⑥ two `mailto:` (`Settings.tsx:897`, the marketplace Coming tap) — **TRACE is not even the mechanism** ⑦ SMS PIN reset — STUBBED, Twilio unprovisioned. 🔴 **AND THE STRUCTURAL FACT UNDER ALL OF IT: `vercel.json` has NO `crons` key. Nothing in the platform fires on a schedule — there is no mechanism by which anything COULD initiate today, which is exactly why filing this now is cheap.** | — *(review-only; no cap reads prose)* ⚠️ **A cap IS buildable and is named-not-taken: a new call site reaching `sendNotification`/`fetch` to an external host, outside the declared set, could fail the build.** 🔴 **TWO EDGES THE WORDING SHOULD BE READ AGAINST, both found by the sweep: (a) `submit.ts:954` — the owner leakage alert fires with NO human naming that moment, and is OUT OF SCOPE BY THE RULE'S OWN WORDS because its recipient is `businesses.phone`/`.email` — it reaches INSIDE the business.** It is the path most likely to be misread as a violation later. **(b) the QBO token refresh reaches an external system with nobody naming the moment** — it is not an act on anyone's behalf, it maintains an already-authorized connection, **but the sentence as written does not say so** | ledger #197 · David's ruling · consumables story `user_stories.md` § NEEDED |
| 2026-08-21 | 🔴 **`businesses` IS THREE DISJOINT ACTS ON ONE ROW, AND THE ANSWER IS A DECLARATION — NOT A MERGE.** `verify-write-paths` counts a path as a FILE, so the RLS anchor reported **7 undeclared writers**. The column matrix partitions them CLEANLY with **zero overlap in either direction**: **CREATION** (`id`/`owner_id`/`business_type`/`trial_started_at`) · **IDENTITY** (`name`/`address`/`phone`/`email`/`website`) · **ACCOUNTING** (`accounting_*`, written by an OAuth callback and a token refresher — **a machine act with no form behind it**). **Merging a signup INSERT, a settings form and a token refresh because a file-counting cap said "7" would be the wrong build.** Per **#168** the honest floor is **THREE, not one** — reached independently, from a different table. 🔴 **AND THE TWO SLATED REPOINTS WERE STOPPED ON EVIDENCE: `set_business_profile` SETs all five identity columns UNCONDITIONALLY — IT IS NOT A PATCH API.** A subset writer cannot call a full-record writer without inventing values it does not hold; routing `OnboardingWizard:608` (state `{name,address}`, every SELECT `id,name,address`) would have sent `p_email=null` and **WIPED the email signup inserts.** *Narrower and correct beats wider and convenient* (2026-07-31), applied to a merge instead of a permission string. | **IMPLEMENTED** — declared in `ALLOWED_DIVERGENCE` with the reasons written out; `businesses` flips `GOAL:FAIL → GOAL:PASS`, goal count 16 → 15. **Both unroutable sites declared VISIBLY as second IDENTITY writers**, not folded in quietly ✏️ **Permission was NOT the blocker and was checked FIRST** — `has_permission_for` is owner-inclusive by `owner_id` and both sites run after their member row exists, so **both would have PASSED**; ledger #47's *"auth-free"* describes the front-door REVEAL, never the write-back | `verify-write-paths` (the declaration asserts itself — a NEW path beside a declaration still FAILS, probe P7) ⚠️ **SEVEN IS A FLOOR, NOT A TOTAL:** a function created outside the migration path is invisible (§6 r17) and **the schema-snapshot checker that would close it is OWED and NOT BUILT** | ledger #190 · `93d8fde` · [recon](audits/businesses-write-paths-recon-2026-08-21.md) |
| 2026-08-21 | 🔴 **THE PROFILE WRITER'S COLUMN LIST IS THE COLUMN-LEVEL POLICY, AND IT IS NOW ENFORCED INSTEAD OF DESCRIBED.** `20260727_rbac_flip_corrections.sql:53-55` already said it — *"⚠️ THE COLUMN LIST IS THE SECURITY BOUNDARY. `owner_id`, `accounting_*`, `business_type` … are UNREACHABLE from here BY CONSTRUCTION"* — **and that was a COMMENT.** The recon's most durable finding is why it needed teeth: **`business_type` is protected by nothing but the absence of code that writes it** (Postgres RLS has no column-level restriction, the owner's own UPDATE policy would permit changing it, and it selects **which tiles a tenant sees**), and **`owner_id` is safe only IMPLICITLY** — `businesses_owner_update` specifies `USING` with **no `WITH CHECK`**, so Postgres applies USING as the UPDATE check by default. | **IMPLEMENTED** — **NEW `scripts/verify-profile-writer-boundary.mjs`**, chained into `npm run verify`. 🔴 **The SET list is DERIVED from the function source; NO column list is typed in the cap** — so adding `logo_url` PASSES and adding `business_type` FAILS (**STD-025**). ⚠️ **INTENDED CONSEQUENCE: if David ever rules `business_type` MAY be edited here, the cap goes RED with no code change** — the ruling then deletes the entry in the open, with its reason, rather than silencing a check | **RED-FIRST AGAINST THE REAL MIGRATION (STD-024)** — both columns planted into the live SET list, each **FAILING and NAMED**, then restored **byte-identical** (`git diff` empty, `diff -q` identical). **16 probes both directions** incl. **B3 a negative control**, **B6/B7 absent-function/absent-UPDATE as HARD FAILURES** (a cap that finds nothing and reports green is the 08-01 zero-row false-green), **B9/B10 newest-wins both ways**, **B15/B16 a commented-out later definition must not win**. ✏️ **STD-021 self-correction: the comment-stripping claim was MEASURED — disabling the stripper still PASSES on the real corpus — and the header now says so instead of claiming a save it did not make** | ledger #190c · `93d8fde` |
| 2026-08-02 | 🔴 **ENABLING A PRICED ADD-ON *IS* STARTING ITS TRIAL — ONE CALL, NOT TWO.** The marketplace's `Enable` called `set_business_module_state({enabled:true})` and nothing else, while the clock's only writer was a SEPARATE RPC the page never called — **so enabling a priced add-on created a BILLABLE MODULE LIVE WITH NOTHING THAT EVER ENDED IT: free forever, no conversion date.** B6's exact defect, arriving through a BUTTON instead of a seed. 🔴 **David found it as a GLYPH first:** the Contractors card carried a green CHECK — *done, nothing to do* — beside a `[Turn on]` button. **Two sequential RPCs would NOT have fixed it, which is the whole reason for the shape:** a partial failure between them produces precisely the state the ruling prevents (#69's class). **One call makes it UNREACHABLE rather than merely unlikely.** | **IMPLEMENTED in code; the MIGRATION IS GATED** (`20260802c`, unapplied). `set_business_module_state` CALLS `start_module_trial` in its own transaction — **inlining was rejected on evidence:** `start_module_trial` is the only function spelling the trial keys as write targets and **`20260801c` V2 asserts that against `pg_proc`**, so inlining would have made this file the third name and failed its own predecessor's check. The 6-arg signature is DROPPED in the same transaction (two overloads → PostgREST *"could not choose the best candidate"* on the money path); old bundles bind by NAME and `p_trial_days` defaults to NULL | 🔴 **NOTHING MECHANICAL CAN COVER THIS, and B6's blind spot is named in its own comment per David: it asserts over the SEED PROJECTION, and a row created by a BUTTON never passes through `moduleSeedRow`.** A CHECK constraint would need `billing` and the DB has no catalog; a mirror over the RPC's inputs **would have passed on 08-02 because the client sent nothing.** The guard is making the state unreachable + a standing QUERY (`20260802b` V3b) · `COALESCE(p_trial_days,0) > 0` keeps `core`/`core_optional` clock-free · **card 32 AMENDED, Part B as MANAGER** | ledger #187 · `9da6790` · `20260802c` |
| 2026-08-02 | 🔴 **A SECTION HEADER IS A CLAIM, AND A PER-ROW GLYPH MUST NOT RESTATE WHAT IT ALREADY SAID.** A header's assertion must hold for **every row the section can currently contain**; both directions are the same lie (*"nothing to do"* over a row with something to do, and *"turn one on"* said to someone who may not). A glyph repeating the header's fact is a second representation of one fact — **and the redundant copy is the one that drifts**, which is exactly what happened. Headers naming an action stay **permission-aware**. | **IMPLEMENTED as doctrine** — **NEW CLAUDE.md §6 r18** + `ui-control-standards.md` §5/S1. The glyph now carries the only other fact a row has — whether it is ON | — *(review-only: no cap reads prose)* ✏️ **r18 numbered BY APPENDING:** the first draft inserted at 16 and pushed the table-editor rule 17 → 18, **silently breaking `RULINGS.md:68`'s citation of "§6 r17"** — caught by grepping for the reference before committing, not by review | ledger #187 · `d89b124` · `9da6790` |
| 2026-08-02 | 🔴 **`20260802` IS SUPERSEDED BY `20260802b` AND WAS OBSOLETE BEFORE IT WAS EVER APPLIED — THE APPLICATION REACHED THE ROWS BEFORE THE MIGRATION COULD.** It was written to repair rows seeded under the superseded spec (add-ons DARK with a clock over them). **David's tenant was never in that state**: his pre-apply query found the five add-ons `enabled:true` WITH live clocks, which is what the CORRECTED code produces — the tenant was seeded by the app *after* the #182 code fix deployed. So the file found nothing to do and `20260802b` was sufficient alone. **This is the general shape and it is worth keeping: a DATA migration races the code fix that made it necessary, and on a tenant seeded after the deploy the migration is dead on arrival.** | **MARKED IN THE FILE, not deleted** (§6 r1 is append-only, and a replay against an OLD snapshot still needs it). It is **INERT, safe to run and safe not to run**: after `20260802b` every catalog module fails at least one clause of its WHERE — five had their clock STRIPPED, three are `enabled:true`, three are core and never clocked | ⚠️ **One condition under which it is NOT fully superseded, named because it is CHECKABLE rather than assumed:** a `business_modules` row whose `module_key` is not in `MODULE_CATALOG`, clocked and disabled. `20260802b` touches only the nine keys it names; this file's WHERE is key-agnostic. **The query is in the file's header and expects ZERO ROWS** | ledger #187 · David's question |
| 2026-08-02 | 🔴 **THE MIGRATION DIRECTORY HAS NEVER REPRODUCED `business_modules`, AND THAT IS CORRECT RATHER THAN A DEFECT — but it must be said out loud, because "a corrective ran while its predecessor did not" reads like a reproducibility failure and is not one.** **NO migration creates a `business_modules` row.** They are created by the APPLICATION at business creation (`seedBusinessModules` → the `seed_business_modules` RPC). The migrations supply the MECHANISM and repair EXISTING rows; the app supplies the call. So a fresh database replaying `20260801c` → `20260802` → `20260802b` ends with **the two functions and ZERO module rows** — both data migrations match nothing, and `20260802b`'s `RAISE` cannot fire because its loop body never executes on an empty table. **The first signup then seeds from the CURRENT `catalogSeedRows()`, which encodes every ruling — so a fresh tenant is correct BY CONSTRUCTION and never enters the state either migration repairs.** | **VERIFIED BY READING, NOT BY RUNNING** — there is no Postgres on this machine, which is the same KNOWN LIMIT recorded above | — *(nothing asserts that replay converges; the reasoning is the WHERE clauses and the loop structure, both quoted in `20260802`'s new header)* | ledger #187 |
| 2026-08-02 | 🔴 **THE MARKETPLACE — SIX RULINGS IN ONE.** **①** Included · Active · Available · Coming, **ONE SCROLL, no tabs** — *a tab hides the very comparison the page exists to make.* **② ADMIN, not Settings — and the general rule is the correction: the section is chosen by WHAT THE THING IS, and the gate follows.** Thunder argued from which permission happens to gate a section today, which is backwards. 🔴 **The menu follows the SIX STATES: a manager sees the item and lands on a page that says *ask the owner*. It does not vanish and it does not redirect.** **③** the Coming tap is a **`mailto:`** — *"I just need the hook, not a full dev solution"* — and **not an inert card, because an input surface that swallows input is worse than none.** **④** days remaining, and **at 0 it must NOT say expired while the thing still works** — *do not render a claim the platform cannot keep.* **⑤** list ALL of them incl. `qb_invoicing` and the four settings entries: **the marketplace reads the catalog, not the registry.** **⑥** ENABLE is **WIRED** to `set_business_module_state` — *a marketplace that cannot enable is the fifth surface this week declaring a capability it does not have* — 🔴 **and it does NO payment, NO Stripe, NO invoice; enabling sets `enabled:true` and nothing bills.** | **IMPLEMENTED** — `/admin/subscription`, gated `subscription:read`. **The six-state ruling's FIRST NEW SURFACE**, which is why card 33 Part B is a regression test for that ruling and not just for this page | **CARDS 32–33** (33 Part B is MANAGER-ONLY and cannot be skipped; Part A's real check is confirming the *nothing has been billed* claim is TRUE). ⚠️ **No cap asserts the marketplace's sections match the catalog** — that is the same human read as the 7-of-14 row, owed again whenever the brief changes | ledger #186 · `ca4539a` |
| 2026-08-02 | 🔴 **THE SWEEP CAME BACK CLEAN, AND THAT GOES ABOVE THE FIX.** Every tile that actually renders is correct — `qr_checkout` and `inventory_intake` read `active` with no badge (core, nothing expiring), `social_media` and `delivery_routing` read `active` with `30D TRIAL`, and every state matches its row. **The first clean sweep this week, said plainly rather than buried under the one finding.** ✏️ **The counts, corrected against source because they matter for the cap:** the catalog holds **ELEVEN** entries, not six, and **all eleven have tiles** — assertion 2 has guaranteed that since it was written. The real division is **RENDERABLE**: 6 of 11 reach a renderer, 5 do not. Of those 5, **`cost_to_produce` was the gap** and four (`qb_invoicing`, `online_shop`, `seasonal_module`, `contractor_tiers`) plus `business_insights` are DECLARED. | **IMPLEMENTED.** `operating_costs` now carries `module_key:'cost_to_produce'`. 🔴 **NOT a display failure and NOT the 2026-08-02 (1) class — David's correction, and it is the right one: the tile had no key, so it did not know the module existed. The pairing was never made; nothing was hidden.** Same name, same capability — the D-14.6 SEVER split one product into a WRITER (`/operating-costs`) and a TUNER (`/costs`). **THE TRIAL STAYS:** pairing makes it visible rather than orphaned, so the condition that made it wrong disappears — and *"a trial starts when the thing being trialled can be used"* was **satisfied all along**: Cost-to-Produce was usable the whole time; the trial was invisible, not inert. That is the distinction from the four coming modules, whose tiles do not exist at all | **NEW `verify-tile-fields` ASSERTION 3** — every catalog entry needs a tile a renderer actually DRAWS, or a declaration with a reason. **`RENDERABLE` is derived, not assumed:** `dashboardTiles()` is the only renderer that exists — `dashboardReadouts()` and `tilesForPlacement()` both have **ZERO CALLERS**, verified. **8 new probes both directions (C21–C28)** incl. **C24, a STALE declaration failing** (what stops the list becoming `OWNER_ONLY_PENDING`) and **C26, proving `planned` is still renderable**. **RED-FIRST against the live registry — it named `cost_to_produce` before the pairing, and again when the pairing was reverted** | David's ruling, this session · ledger #185 |
| 2026-08-02 | 🔴 **A TRIAL STARTS WHEN THE THING BEING TRIALLED CAN BE USED.** The four `planned` add-ons (`followup_engine` $19 · `business_insights` $19 · `online_shop` $19 · `seasonal_module` $29) come off their clocks alongside `contractor_tiers`, in ONE corrective migration. **David's reasoning: a clock started before a tile exists counts down against nothing, and the catalog's own rule — a trial is a countdown to a PRICE DECISION — has nothing to decide about a module nobody can use.** The `unpriced` precedent is the same shape and was already ruled. **The alternative (ship the countdown, start the clock on ship) was REJECTED for the reason it was raised: it would put a countdown against a tile that does not exist, on David's demo tenant, days before LAWNS.** | **IMPLEMENTED in code; the DATA REPAIR is GATED** (`20260802b`, extended to all five). **DERIVED, NOT DECLARED** — `moduleSeedRow` reads `tile.status`, so the catalog keeps its OFFER (`trial_days: 30`) while the seed withholds the GRANT; hand-editing four rows to 0 would have destroyed the offer and needed undoing per tile on ship day. **BOTH halves are gated**: suppressing `start_trial` while the TERM still travelled would invent B8's defect (a term with no trial) inside the fix meant to remove one | **A23–A27** (A25 catches the half-fix; **A27 stops the rule leaking into the baseline** — a core module is live even with a dark surface) · 🔴 **B10 states the requirement INDEPENDENTLY, reading `TILE_REGISTRY.status` rather than the mapping's own helper** — a test reusing the code's predicate can only prove the code agrees with itself. **Red-first: removing the gate fails 7, naming all four planned add-ons by tile status** | David's ruling, this session · ledger #184 |
| 2026-08-02 | 🔴 **THE `available` FINDING — FIVE MODULES WHOSE TILES ARE `planned` WERE SEEDED AS LIVE, BILLABLE ROWS, AND THE ONLY SURFACE THAT WOULD HAVE SHOWN IT NEVER EXISTED.** David's headline, above the fix: **the seeder's output has never been visible anywhere.** No screen renders a module's seed state, so a row saying *"live, on a paid trial, converting in 30 days"* sat behind a tile saying *SOON* and nothing could put the two side by side. **A STATE NOTHING RENDERS CANNOT BE CONTRADICTED** — which is the same sentence the `campaigns.status` finding produced, nine weeks after that value went wrong (#179). | **RECORDED as the reason, not as a build.** The fix is the ruling above; this is why nobody caught it for a day and why the marketplace matters beyond selling anything — **it is the first surface that renders what the seeder decided** | — 🔴 **and the gap is the point: no cap can see this class, because there is nothing to compare the seed against.** `moduleSeed.test.ts` B10 now asserts the ONE cross-check that exists (clock ↔ tile status); everything else about a seeded row is still unrendered and therefore uncontradictable | David, this session · ledger #184 · #179's sentence |
| 2026-08-02 | **THE CONTRACTOR TIER BELONGS TO THE CUSTOMER, NOT TO THE NURSERY'S MODULE LIST** *(second half of the contractor ruling; it governs a build that does not exist yet, which is why it is written down now)*. If a contractor portal is ever built, **the same person sees their own tier** — the tier follows the CUSTOMER across both surfaces rather than being re-decided per surface. Consistent with what already exists: `customers.price_tier` is a column on the customer. **This settles half of the contractor-portal contradiction filed earlier: the PRICING half is a customer attribute and is answered. Only the LOGIN half — how a contractor authenticates into a portal — is unresolved.** | OPEN — nothing built; recorded so the portal build does not re-derive tier ownership from the module list | — *(no cap; the guard that will matter is this row existing when the portal is specced — the D-53/D-54 lesson)* | David, this session · `customers.price_tier` · [[D-38]] · [[D-55]] |
| 2026-08-02 | 🔴 **`contractor_tiers` IS CORE-WITH-A-SWITCH: PRICE 0, NO TRIAL, SEEDS DISABLED.** It ships with the platform and a nursery that gives contractor discounts turns it on; **nothing expires because there is nothing to expire.** It is on a LIVE 30-DAY CLOCK RIGHT NOW and must come off it — *a working capability that expires in a month is exactly what the fuzz would take away wrongly.* | **IMPLEMENTED in code; the DATA REPAIR is GATED.** 🔴 **DAVID'S REASONING REPLACED MINE AND IT IS THE BETTER DIAGNOSIS: *"three fields doing one job with contradictory rules is the real defect, and contractor_tiers at $0 is what surfaced it."*** So the fix is not just a fourth value — it is `enabledByDefault(billing)`, **one field deciding the BASELINE**, with a running clock as an OVERRIDE on top rather than a second opinion. `'core_optional'` is its own value, not core-with-an-exception: same money as core, different default. `20260802b` is REQUIRED and unapplied — the catalog edit cannot reach the row already on disk | `verify-tile-fields` `BILLING_RULE` gains `core_optional` (price must be 0) + **probes C17–C20**, incl. **C19 — `core_optional` carrying NULL FAILS, because the price is DECIDED, and using `unpriced` for a decided zero is D-9 pointed backwards** · `moduleSeed.test.ts` **A16–A22** (**A20 proves the derivation is TOTAL over the union; A21 fails if the mapping RE-SPELLS the baseline instead of reading it** — red-first proven, both fire) | David's ruling 3, this session · `MODULE_CATALOG` |
| 2026-08-02 | **`cost_to_produce` IS PRICED AT $29/mo, `add_on`, TRIALLED LIKE THE REST.** 🔴 **David's reasoning, recorded because it governs the number's future: $29 is a PLACEHOLDER and does not matter yet — he will price the verticals from what Cost-to-Produce tells him the platform actually costs to run.** It is one field to change when that data exists, and the snapshot ruling is what makes changing it safe (a catalog edit governs NEW trials only). | **IMPLEMENTED in code; the DATA REPAIR is GATED** (`20260802b`). ⚠️ The repair STARTS A CLOCK on a live tenant, so it goes through `start_module_trial` — **never a hand-written `jsonb_set`**, which would be a second writer of the pair the 2026-08-01 ruling made unique | `verify-tile-fields` accepts `add_on`/29/30 unchanged; `moduleSeed.test.ts` B4/B5 already forbid a clock over a null or $0 price, which is exactly what this row stops being. **20260802b V3 asserts the clock arrived WITH the RPC's audit row** — zero rows there means a second writer exists | David's ruling 2, this session · `MODULE_CATALOG` |
| 2026-08-02 | **`inventory_intake` IS CORE. Ships on, always — it is HOW STOCK GETS CAPTURED, and there is nothing to buy.** | **IMPLEMENTED in code; the DATA REPAIR is GATED** (`20260802b`). Closes half of the `unpriced` OWED question — and with ruling 2 closing the other half, **`unpriced` now has ZERO members and is KEPT deliberately**, as the honest answer for the next module that arrives before its decision does | `moduleSeed.test.ts` A10/A15 + `verify-tile-fields` C13 already prove `core`/0/0 seeds live and passes the cap — this row needed the value, not a new probe. 🔴 **NOTHING MECHANICAL PINS WHICH MODULES ARE CORE, deliberately** (`moduleSeed.test.ts` header): a test pinning the core set turns David's next promotion into a red build | David's ruling 1, this session · `MODULE_CATALOG` |
| 2026-08-02 | 🔴 **A RUNNING TRIAL MEANS THE MODULE IS LIVE — `enabled:true` AT SEED. THE CLOCK ENDS ACCESS; IT NEVER WITHHOLDS IT.** *(David, correcting the 2026-08-01 seed spec below.)* The superseded rule mapped `enabled = billing === 'core'`, so all seven paid add-ons seeded **DARK with a thirty-day clock running over them** — nothing was being trialled. **`enabled:false` is a FUNCTIONAL GATE, not a label** (`api/social/generate-posts.ts:52` refuses on it), so social_media counted down toward a $19/mo conversion **while the server refused to generate a single post**: the owner reaches day thirty with no basis on which to convert. **David's model: the site goes live, EVERYTHING WORKS, and at the end of the term the unpaid modules go behind the fuzz.** A trial is the grace period during which the module is fully live. **`configured` flips with it and that is not a rider** — `useModules` renders `active` only on `enabled && configured`, so correcting `enabled` alone fixes the data and leaves the dashboard showing `[ENABLE]`: the symptom survives its own fix. **Liveness follows the CLOCK, not the billing class** — an add-on with `trial_days: 0` still seeds dark. **NO new `TileState`:** a trialling module is `active` with the countdown as an ANNOTATION. | **IMPLEMENTED in code; the DATA REPAIR is GATED.** `moduleSeedRow` corrected (every future tenant) + NEW `trialClock.ts`, the one reader of the stored pair. 🔴 **`20260802_trialling_modules_are_live.sql` is REQUIRED and unapplied** — `20260801c`'s `ON CONFLICT DO NOTHING` means the code fix cannot reach the 11 rows already seeded | 🔴 **`moduleSeed.test.ts` B3 was INVERTED — it ASSERTED THE DEFECT.** It forbade `enabled && start_trial` ("a module cannot be both included and expiring"), which is exactly what a trial is; **138 assertions went green over the bug because the guard and the code were the same mistake written twice.** Now: B3 forbids a clock over a DARK module · **B6 rewritten not deleted** (the danger is live-with-nothing-that-ends-it, not live-and-not-core) · **B6b — liveness has exactly two sources and no third** · A7b, the probe that fails anyone who summarises this as *"add-ons are now enabled"* · red-first 16 fails. NEW `trialClock.test.ts` 16 probes both directions, **T4 proves the term is read from the ROW not the catalog**, **T16 is a negative control that fails a null-returning stub** | ledger #182 · `20260802` |
| 2026-08-01 | 🔴 **THE ISMISSINGFUNCTION CATCH — A COMPATIBILITY BRIDGE SILENTLY DEFEATING THE MIGRATION IT WAS WRITTEN BESIDE.** `moduleState.ts`'s pre-migration fallback matched a **generic `does not exist`** alongside the two real not-found codes, so a transient PostgREST schema-cache miss would have routed a `business_modules` write **AROUND both authority gates** into a direct upsert — which RLS then discards with **no error and zero rows**. Not dead code: live code behind a condition that could still fire, in the one file whose header calls itself *THE ONE WRITER*. **Same class as a guard the write does not depend on (STD-023), arriving through ERROR HANDLING rather than through a check** — the fallback path is the one nobody reads, tests, or reviews, and it was the only path that could reach the un-gated write. | IMPLEMENTED — the bridge is DELETED, in the commit that had the standing to delete it (both migrations applied + proven), exactly as its own header promised | — 🔴 **NOTHING MECHANICAL CATCHES THIS CLASS.** `verify-write-paths` saw the file and the table and correctly reported ONE path; it cannot see that one branch of that path bypasses the RPC. **A cap that reads which ERROR BRANCHES reach a write is buildable and is not built** — named here so the gap is a decision rather than an oversight | ledger #181 · `moduleState.ts` |
| 2026-08-01 | 🔴 **A TENANT'S TERMS ARE WHAT THEY WERE GIVEN — `trial_days` IS SNAPSHOTTED AT SEED, NOT READ LIVE.** The row carries the PAIR `(trial_started_at, trial_days)` and expiry computes from the pair; the catalog governs **NEW trials only**. **Ruled because this is the shape that has cost the most this week: a value nobody ratified, inherited from Ignition, silently governing live tenants.** Reading the length at render meant changing 30 → 14 would retroactively expire every tenant seeded more than fourteen days ago, with no event marking it. **It also demotes the number itself to a scheduling question** — MASTER_BRIEF:243 and BD-2 say 14, `Help.tsx:524` tells a customer 30, the donor code assumed 30. *Resolve the number separately; do not resolve it by changing a live field.* **NOT STD-011:** the catalog is the OFFER, the row is the TERM GRANTED — two facts that share a number for one instant (D-35's stored-`sell_price` relationship, on a different table). | IMPLEMENTED — `start_module_trial` writes both keys as one object and **refuses to rewrite the term on a running clock**, so the repair path cannot re-term a tenant either | `moduleSeed.test.ts` A13–A15 · B7 · B8 (red-first: a hardcoded term fails 6) · `20260801c` **V5b passes 14 to a live 30-day trial and every term must still read 30**, with `term_rewrite_refused` in the audit row | ledger #181 |
| 2026-08-01 | 🔴 **A SEEDED CORE MODULE READS `active` ON DAY ONE.** `useModules.ts:104` renders `active` only on `enabled && configured`, so the `configured:false` draft put an **`[ENABLE]` button on a working, already-included feature** — the dead-affordance class, the same defect that kept `campaigns` off `module_key` entirely. **There is nothing to configure about being included.** Second reason and it is not cosmetic: the draft left this build with **no visible change at all**, and *a build with no observable change reads as a failed deploy* — which is how a real deploy failure gets waved through (OP-15's subject). ⚠️ `qb_invoicing` is the arguable one and is flagged in place: a green tile says *included*, not *connected*; the QB connection indicator is a separate surface. | IMPLEMENTED — `configured` is projected from the catalog (`billing === 'core'`) and travels in the seed payload | `moduleSeed.test.ts` A10–A12 · **B9** (red-first: reverting to `configured:false` fails 3) · CARD 23 ② is now a POSITIVE observation — green dots on QR Checkout and QuickBooks — where the draft's card said *expect nothing to change* | ledger #181 |
| 2026-08-01 | 🔴 **CORE IS RENDERED AS "INCLUDED" FROM `MODULE_CATALOG`; NO NO-COST KEYS ARE MINTED.** A `module_key` on the owner dashboard would make **the shell every tile renders into a purchasable row** — a thing with an enablement flag someone could set false — and **`verify-tile-fields` assertion 2 is RIGHT to reject a catalog entry no tile references.** The rejected option was rejected by a cap that was correct. `billing:'core'` already carries the fact, and the marketplace reads it **whether or not a `business_modules` row exists**: **two rows seeded, five shown**, and the difference is a RENDERING concern rather than a data one. | **IMPLEMENTED as a decision; the render is ITEM 3's.** ⚠️ What it leaves: the three unmapped core items have no catalog entry at all, so *render from `billing:'core'`* yields two — the marketplace needs a display-only line for each (copy, not a `ModuleEntry`), sourced from MASTER_BRIEF:295-300 | — *(nothing asserts the marketplace's core section matches the brief; that is the same human read as the 7-of-14 row, and it is owed again whenever the brief changes)* | ledger #181 · `MODULE_CATALOG` |
| 2026-08-01 | 🔴 **A ROW FOR EVERY MODULE, INCLUDING CORE — because a module may MOVE to core as the platform builds out, and with rows for everything that is a FIELD CHANGE rather than a migration against live tenants.** Core is seeded ENABLED (included in the base subscription, no trial); paid is seeded disabled with the clock started; the marketplace SHOWS core as included rather than hiding it — *Terry seeing what he already gets is worth more at a demo than only seeing what costs extra.* ✏️ **The ruling asked for a new catalog field marking core vs paid; `billing` already is that field** (#179), so a second was NOT minted — STD-011 — and **the mapping needs no new field at all**: `enabled = billing==='core'`, `start_trial = trial_days > 0`. | 🔴 **SUPERSEDED IN PART 2026-08-02 — the `enabled` half was WRONG and is reversed by the row at the top of this table.** *"Paid is seeded disabled with the clock started"* put a running clock over a module nobody could use. **The rest of this ruling STANDS and is untouched:** a row for every module including core, core seeded enabled, the marketplace showing core as included, and `billing` already being the core-vs-paid field. Kept rather than rewritten — a ruling edited to look like it was always right teaches nobody why the reversal happened | **`moduleSeed.test.ts` — 88 assertions**, red-first proven twice; it pins the MAPPING and the cross-field invariants, deliberately **NOT which modules are core** (that is David's to change, and a test pinning it would turn his own ruling into a red build) | ledger #181 · `20260801c` |
| 2026-08-01 | 🔴 **THE TRIAL CLOCK IS GATED ON `subscription:update`, NOT `settings:update` — a trial is a SPEND WITH A DELAY.** At day thirty it converts or goes dark, so starting the clock does not decide the money, it SCHEDULES the decision on a date the starter picked for an owner who may not have been asked. The clock is one-shot and never restarts. **Decisive: it lives in the same row as `enabled`, behind the same wall** — gating it more loosely would make the timer writable by a strictly LARGER set than the enablement it governs, and *a wall whose weakest gate is the one on the timer is not a wall.* **Cost stated, not hidden: a MANAGER cannot start a trial.** And the key `{trial_started_at}` is a SQL literal in ONE function — never `p_config_patch`, whose arbitrary keys turn a typo into a `success` audit row over a trial that never ends. | IMPLEMENTED — `start_module_trial`, the only writer of the key anywhere | `20260801c` **V2 asserts the key is spelled once PLATFORM-WIDE, against `pg_proc` not against the file** — a third function name there is a second clock writer. **Owed: nothing in `npm run verify` can see it** (the caps read repo `.sql`, not the catalog) | ledger #181 |
| 2026-08-01 | 🔴 **A NON-BLOCKING SEED WHOSE ROW HOLDS THE CLOCK IS A CUSTOMER WHO IS NEVER BILLED — so the failure must be LOUD AND CHEAP TO REPAIR, not rare.** `useModules` renders a MISSING row and a disabled row identically, so an unseeded tenant is invisible on every screen (D-9's shape, in the data layer). Therefore: the RPC returns **COUNTS not a boolean**; it is **idempotent**, so the repair path IS the create path; and the onboarding call sits **OUTSIDE the legacy-create branch** — unlike `seedPricingConfig` — so a signup-path failure self-heals. | PARTIAL — **the residual is named, not claimed**: an owner who abandons onboarding with both calls failing has no rows and nothing notices. The third home is the marketplace load (ITEM 3), NOT BUILT | **CARD 24** (finish onboarding twice; every timestamp identical, `restart_refused: true`) · `20260801c` **V6 is the standing detection query** until ITEM 3 lands | ledger #181 · tech-debt #39's class |
| 2026-08-01 | 🔴 **THE MARKETPLACE'S "WHAT YOU ALREADY GET" LIST HAS TWO ROWS, NOT FIVE.** `MASTER_BRIEF` names five core items; only QR Checkout and QuickBooks carry a `module_key`. **Owner dashboard cannot have one** — it is the shell every tile renders into, and a row for it could be set `enabled:false`. Basic inventory/asset tracking and Customer records simply have none. So three of the five things the brief promises would be missing from the screen built to show Terry what he is getting. | **OWED — the ruling is the deliverable.** Two ways out, written into `MODULE_CATALOG`, **neither chosen**: (a) mint no-cost core `module_key`s — consistent with *rows for everything*, ⚠️ but it **FAILS `verify-tile-fields` assertion 2**, which rejects a catalog entry no tile references, so it needs a cap change with its own probes; (b) render *included* from a separate static list | `verify-tile-fields` assertion 2 guards the join both directions but **structurally cannot see a brief item with no tile** — the same human read as the 7-of-14 row below | ledger #181 · `MODULE_CATALOG` |
| 2026-08-01 | 🔴 **AN `owner-only` STRING CANNOT BE DELEGATED, BY CONSTRUCTION — AND NOBODY HAD STATED THAT.** `CATALOG_PERMISSIONS` filters every `sensitivity:'owner-only'` resource out of the Roles-page chip catalog (spec §4), so such a string can never be granted to anyone: it is held by holding the OWNER role, and by nothing else. The ruling that surfaced it said `owner-only` AND "delegable through /roles" in one breath; **those are incompatible in this model**, and the sensitivity is the half that shipped. **DECIDED: `subscription:*` STAYS owner-only** — deciding what the business pays TRACE is the owner's, on the `team:*` reasoning. **A SECOND OWNER gets it by holding the computed set, not by delegation, which is the entire point of the two-owner model.** Granting it to a manager would be a deliberate act with a recorded reason — a sensitivity change — never a default. | IMPLEMENTED — `subscription` seeded `sensitivity:'owner-only'`; the incompatibility asserted, not assumed | `permissionManifest.test.ts` asserts `!CATALOG_PERMISSIONS.includes('subscription:update')` **with the tension recorded at the assertion**, so the day someone wants delegation the model says why it is not already true instead of reading as a bug | `d48a053` · this session |
| 2026-08-01 | **A CHECK PINNED TO A HISTORICAL SNAPSHOT EVENTUALLY DEMANDS AN IMPOSSIBLE EDIT.** Assert against the CURRENT materialisation, never a dated file — and prefer a CONTENT marker the artifact necessarily carries over a FILENAME convention somebody has to remember. Three instances now: capQ's R-B2 direction (07-31), capA's assertion 3 pinned to `20260730a` (**which froze the permission model at 52 strings server-side — one line in one check, and nobody would have found it until the next mint**), and capQ's floor selector picking by `/_align_floor/` — **which the very next floor migration promptly forgot**. | IMPLEMENTED — capA follows the newest `$OWNER$` carrier; capQ resolves the floor PER ROLE by content across both live shapes | capA probes M1–M11 · capQ `floor/newest-wins`, `floor/per-role`, `floor/dollar-shape`, `floor/no-carrier`. **Both caps ALSO failed on their own first real run and were fixed by it** — capA read the marker inside its own PROSE; the cure is comment-stripping, probes M10/M11 | `5e7f009` · `d48a053` |
| 2026-08-01 | 🔴 **SEVEN OF FOURTEEN PRICED MODULES HAVE NOTHING BEHIND THEM — THAT IS THE MARKETPLACE'S REAL SCOPE, AND IT IS VISIBLE BEFORE THE SCREEN IS BUILT.** `MASTER_BRIEF:304-321` prices 14 add-ons; only 7 map to a `module_key`. **PMI is billable at $29/mo and its tile carries NO `module_key`, so it cannot be enabled, disabled or billed** — revenue the platform structurally cannot collect. Equipment Tracking ($19) is likely the `assets` tile, same shape. GPS Tracking, Water System, Greenhouse, EPA 608 and DOT Compliance have no surface at all. **DO NOT FIX BY ADDING A `module_key`** — that flips a live tile to "set up", the dead-affordance class. | OPEN — filed, deliberately not fixed | `verify-tile-fields` assertion 2 guards the join in BOTH directions, so the gap cannot silently widen; it does NOT and CANNOT see a price in the brief with no tile — **that reconciliation is a human read, done once (this session) and owed again whenever the brief changes** | this session · `MODULE_CATALOG` · `56b171d` |
| 2026-08-01 | **THE PER-MODULE TRIAL CLOCK LIVES IN `business_modules.config`, WRITTEN ONLY BY AN RPC — NEVER `created_at`, NEVER A CLIENT WRITE.** `created_at` was the cheaper shape and it is disqualified by DATA: the 2026-06-04 `INSERT SELECT` PRESERVED timestamps from `nursery_modules`, so live rows carry a birth date from months ago and **a trial read off them expired before it began** — #70's genesis-row shape exactly. It also expresses ONE state where the model has three (trial start · paid-until · reactivation). **The jsonb typo risk is answered by the RPC, not by the column:** key named as a SQL literal inside `jsonb_set`, one writer, value validated, audit row, `IF NOT FOUND → INSERT`. The `set_business_tax_rate` shape, **third instance**. | OPEN — ruled, nothing built. Blocked behind the RLS narrowing by build order | — *(owed: the RPC is the guard; until it exists `business_modules.config` has NO check constraint, NO TS type and NO runtime validation, and a key typo fails **OPEN** — free forever)* | this session · `20260727_rbac_resource_action_flip.sql:293` |
| 2026-08-01 | **THE MODULE SEEDER IS A SIBLING OF `seedPricingConfig`, CALLED FROM BOTH CREATION PATHS IN THE SAME COMMIT.** `OwnerSignup` (modern) and `OnboardingWizard.finalize()` (legacy) both mint a business; the pricing-config seeder already learned this and says so in its own comment — *"one of them silently producing an unconfigured tenant is exactly how the missing row survived this long."* Non-blocking per §6 r6. **🔴 AND THE CLOCK MUST SAY WHAT IT ASSUMES WHEN THE SEED FAILS: a signup that succeeds with no module rows is a tenant with NO TRIAL**, and a missing row must not read as "trial never started" (free forever) — the absent-is-not-empty rule (A9/D-9) applied to money. | OPEN — ruled, nothing built | — *(owed with the seeder)* | this session · `seedPricingConfig.ts` · `OwnerSignup.tsx:304` · `OnboardingWizard.tsx:548` |
| 2026-08-01 | **THE MARKETPLACE GATE IS A NEW OWNER-ONLY RESOURCE: `subscription:read` · `subscription:update`. NO DELETE VERB** — a module is disabled, not deleted, and minting a verb nothing enforces is the fake-pill class. `category:'admin'`, `sensitivity:'owner-only'`, on the `team` precedent. 🔴 **`settings:update` was DISQUALIFIED ON EVIDENCE, not on taste: it is in `MANAGER_DEFAULT_BUNDLE`**, so a manager could change what the business PAYS TRACE from the same string that lets her fix the business hours. **THE FINDING IS NOT THE NAMING — it is that all 25 manifest resources are about what the business DOES or CHARGES, and NOTHING in the model has ever been about what the business PAYS.** That capability has never existed. | **IMPLEMENTED 2026-08-01** — minted; owner floor 52 → 54; the enablement gate split in `20260801b` (GATED, not applied) | **9 assertions in `permissionManifest.test.ts`** (owner holds both · MANAGER and STAFF hold neither · no delete verb · category admin) + capA assertion 3 pins the floor to the manifest. 🔴 **capP reports `EXTRA:subscription`** — `subscription:read` is `routeEnforced` and capP does not read that field; third instance after settings (P18) / team (P19). The prediction is NOT edited to match output — **David's call, OWED** | this session · `permissionManifest.ts` `team:` precedent |
| 2026-08-01 | **EVERY TILE DECLARES EVERY FIELD WITH A LEGAL VALUE, AND A CAP SAYS SO.** The sweep's finding is the ruling's reason: **every registry field WITH a reader was correct or nearly so; every field WITHOUT one had drifted** — `campaigns.status` wrong for nine weeks, `qr_checkout.kind` stale since the Nav C2 rename, `nav_eligible` wrong on SEVEN of 33 in both directions. A declaration nobody reads is a declaration nobody corrects. Also: `nav_eligible` DELETED rather than fixed (NAV_IA already holds the fact — STD-011); `action`/`context` collapsed to `destination`. | IMPLEMENTED — 33 rows uniform; `campaigns` live; `seasonal_module` the first `cultivar` row | **NEW `scripts/verify-tile-fields.mjs`** in `npm run verify` — **rules DERIVED from `interface TileEntry` + the `Tile*` unions, never hardcoded** (#73's lesson): a new field or union member is enforced with no second edit. 15 probes both directions incl. **T15, which proves the derivation**; red-first on the real registry | ledger #179 · `b00f086` |
| 2026-08-01 | **A RATCHET THAT CRIES WOLF TEACHES REFLEXIVE RE-BASELINING, AND A REFLEXIVE RE-BASELINE IS HOW A REAL VIOLATION GETS ABSORBED.** A baseline is keyed on WHAT A SITE IS, never on where it happens to sit — line numbers are printed for the human and never keyed on. **The cost of a noisy ratchet is not the minutes; it is the habit it trains.** | IMPLEMENTED — `scripts/lib/siteKey.mjs`, both ratchet caps re-keyed on `path::binding#table.verb` | **8 new probes both directions** (a comment above a site is not new; **a genuinely new site still fails**) + a `--dump` count-identity proof, run before re-locking: 85→85, 35→35, 0 unmatched | ledger #177 · tech-debt #78 |
| 2026-07-31 | **A PLANNED TILE RENDERS BECAUSE IT IS PLANNED, not because someone happens to hold its string** (BUILD 2). `useModules` filtered `can()` BEFORE reading `status`, so `planned` was reachable exactly for the people who did not need to be told the feature was coming — the inverse of the state's purpose. Plus: the tile's coming-soon treatment reads the **TILE's** status, never the manifest's — one source per surface. | IMPLEMENTED — the filter admits planned tiles · NEW `TileState` `'planned'` (amber SOON, **not** the red lock) · `<BeingBuilt>` MOUNTED for the first time, on the `business_insights` readout | **capA assertion 6** — a tile on a `planned` permission must itself be `status:'planned'`; **the converse is NOT asserted** (7 of 8 planned tiles gate on live strings). 7 probes, red-first proven by flipping the real registry row | ledger #176 · #89 |
| 2026-07-31 | **A FOURTH PERMISSION STATUS: `planned`.** THE PILL IS REAL, THE FEATURE IS NOT BUILT — and that is not a fake pill. A fake pill claims something WORKS when it does not; a planned pill says a thing is COMING, which is true, and it is the conversation starter. `planned` renders in the catalog, visually distinct, and **is NOT GRANTABLE**. **THE FLOOR: `planned` requires a build behind it** — a named gap, a card, a slot, or a decision; anything else stays `declared-unwired` until it earns the status. | **BUILDS 1 + 2 IMPLEMENTED.** 🔴 **BUILD 1's FINDING, recorded above the mint:** `OWNER_LOCKED_SET` filtered `!== 'declared-unwired'` rather than UN-GRANTABLE, so three planned strings would have entered the owner's computed set — the owner holding `reports:read` for a readout that does not exist, and capA then demanding an APPLIED migration grow **52 → 55 to record a grant of nothing**. *The same conflation the fourth status exists to end, one derivation later, in the commit that ends it.* **Caught by tests, not by review.** Then: status + `reports:read` MINTED + `maintenance:override` and `deliveries.route:update` reclassified + non-interactive amber chip. **BUILD 2 (the tile path) NOT STARTED** | **capQ** over UNGRANTABLE = declared-unwired ∪ planned (19 probes) · `permissionManifest.test.ts` (**9 new assertions** incl. no-planned-in-any-bundle and no-planned-in-OWNER_LOCKED_SET) | ledger #175 · supersedes the #88 declared-unwired read |
| 2026-07-31 | **A STRING IS DERIVED FROM THE ACT, NEVER FROM WHO NEEDS TO PASS.** *"Which string does this person hold"* always produces a plausible answer and only sometimes a correct one — it produced `deliveries:create`, then `orders:update`, on ONE LINE in twelve hours. *"What capability is being exercised"* produced `orders:create` on the first try. **The first question is how a gate ends up naming a resource the path does not touch.** Lightning's error as much as Thunder's. | IMPLEMENTED *(as method)* | — *(a question cannot be linted; **capA assertion 5** catches the downstream symptom — a string outside the model — and **assertion 4** catches the other symptom, a grant set nobody chose)* | ledger #174 · the three-string sequence on `CustomerCapture` |
| 2026-07-31 | **The A7 client-gate sweep is mechanical.** Every literal permission string in `packages/*/src` that is not a current model entry FAILS the build — capP's assertion 1 pointed at the layer it had never read. Four instances, four different discovery routes, none of them a check. | IMPLEMENTED | **capA assertion 5** · 12 probes both directions · red-first on the current tree (**4 strings / 5 sites**, of which 2 were live defects) | ledger #174 |
| 2026-07-31 | **A PERMISSION GATES A CAPABILITY, NOT A FIELD.** If a member may perform the act, they may set the act's own data — `orders:create` grants creating an order, and a delivery date is a FIELD ON THAT ORDER. The alternative is a string per column and a model nobody can use. A separate string is warranted only when the field is a DIFFERENT capability: `order_discount:apply` and `tax_exempt:apply` are the counter-examples, **separate because they are MONEY and TAX acts, not because they are fields.** | IMPLEMENTED — `CustomerCapture.tsx:147` gates on `orders:create`; **`submit.ts` needs nothing and its silence is a RULING, not an oversight** (tech-debt #84 closed ruled-not-built) | — *(judgement, not a shape a cap can read; capA assertion 4 makes the CONSEQUENCE — who passes — visible)* | ledger #173 · tech-debt #84 |
| 2026-07-31 | **The grant set of every authority site is BASELINED — who PASSES, not what the check looks like.** A narrowing is reported by the person who lost something; **a widening is reported by nobody, ever**, because the person who gained access does not file a ticket. Three of the first four findings were widenings. | IMPLEMENTED | **capA assertion 4** + `authority-grants-baseline.json` (41 sites, keyed `file::binding` not `file:line`) · 19 probes both directions · red-first proven against `123ad3a` | ledger #173 · R-GRANTDIFF |
| 2026-07-31 | **A permission string is chosen from what the code WRITES, not from what the surface is called.** The checkout delivery-date field writes `orders.delivery_date`; it never touches the `deliveries` table. **(SUPERSEDED the same day: the string is `orders:create`, not `orders:update` — see the capability-not-a-field ruling above. The reasoning here was right; the verb was not.)** **Narrower and correct beats wider and convenient** — granting a manager `deliveries:create` to fix a field on a different table would also hand her the ReceiptKeeper delivery INSERT she was never owed. | IMPLEMENTED | ✅ **capA assertion 4** — when this was written the guard was `—`, because capA asserted SHAPE and the shape was perfect throughout. Ruled and built the same day; the class is now covered | ledger #172 · #173 |
| 2026-07-30 | **Six surface states.** A page without access RENDERS AND SAYS SO — it never redirects. Withheld data ANNOUNCES its redaction; never an empty list, never a zero. NOT PERMITTED names what is needed. | IMPLEMENTED — `SurfaceState.tsx` + `PermissionRoute` (26 routes) + the menu. **BEING BUILT is built but NOT MOUNTED** (card 12, `needs-test`) | — **owed: capB** (nothing yet fails the build on a silent refusal; the 27→0 conversion was done by hand and a 28th could be added tomorrow) | Phase 3 · ledger #171 |
| 2026-07-30 | **Permissions are ALWAYS checked; there is no exception path.** OWNER holds every enforced permission, LOCKED, COMPUTED from the manifest — never stored, never curated. `owner_id` is a fact about who owns the business, NOT an authority mechanism. Two owners = two members holding the same locked set. | IMPLEMENTED (client) · **migrations GATED — David applies a→b→c** | **capA** (3 assertions, 16 probes) + `permissionManifest.test.ts` (5) | Phases 1–2 · ledger #171 |
| 2026-08-02 | 🔴 **`npm run verify` CANNOT TELL WHETHER A MIGRATION COMPILES, AND THAT IS A RECORDED LIMIT — NOT AN OWED BUILD.** `20260802b` was reported BUILDER-COMPLETE with the gate green and then failed on David's first paste: `42804`, an untyped `NULL` into a `uuid` column. **Nothing in the gate asks Postgres anything** — every cap reads `.sql` as TEXT. Same class as `api/` before `verify:api-parses`, **except the cheap version would not have helped**: an offline parser (`libpg-query` — the real PG grammar, milliseconds, no connection) catches SYNTAX, and `INSERT … SELECT DISTINCT x, NULL` **is syntactically valid**. `42804` is a *type-resolution* error that needs the catalog. **So catching this class requires a real Postgres with the schema loaded — `BEGIN; <migration>; ROLLBACK;` — and this machine has no `psql`, no `postgres`, no `docker`** (checked, not assumed). It therefore belongs in **`verify:rls`, never `verify`**, whose own header already makes the argument: *"that gate must stay offline and deterministic… a gate that fails for reasons unrelated to the change gets worked around, which is worse than no gate."* **The honest control is the REFERENCE ENVIRONMENT — §9's fourth completion bar, dormant until the first paying customer.** | **IMPLEMENTED *(as a recorded decision)*** — named and accepted, on the E2E precedent below. **Nothing is being built for it**, and the reason is stated rather than left as a silent absence | — 🔴 *(that is the point of the row. The one control that exists is a HUMAN pasting the file, which is how both instances were found)* ✏️ **What WAS built is the narrower guard: the rule is `DISTINCT`/`GROUP BY` forcing `unknown → text`, not "a bare NULL"** — all 25 `INSERT…SELECT` statements swept, one other bare NULL, no forcing clause, correctly safe | ledger #185 · `42804` · [e2e precedent](decisions/2026-07-30-e2e-browser-tests-not-built.md) |
| 2026-07-30 | **E2E browser tests are not built.** The gap is named and accepted, not silently absent. | IMPLEMENTED *(as a recorded decision)* | — | [2026-07-30-e2e-browser-tests-not-built.md](decisions/2026-07-30-e2e-browser-tests-not-built.md) |
| 2026-07-29 | **TRACE is not a record system.** Capture a document only to EXTRACT DATA from it; pass the document through to the system that IS the record. A tax certificate has nothing to extract → keep the reference and the expiry, never the file. | PARTIAL — the certificate upload shell is removed; the **receipt → QuickBooks buy-side push** (`Purchase`/`Bill`) is NOT built, so a receipt's data is as stranded as its image | — | MASTER_BRIEF PART 1 · ledger #167 |
| 2026-07-29 | **A write path is a FILE, not a call site**, and more than one per table fails the build unless declared. 17 failing tables = **17 decisions owed, not 17 builds** — most resolve to a declaration. | IMPLEMENTED | `verify-write-paths` (ratchet) | ledger #167 · [platform-architecture-standard.md](standards/platform-architecture-standard.md) A2 |
| 2026-07-29 | **An unenforced rule is a placeholder for a cap**, not a finished control. A1/A3/A5/A6/A9 are review-only and the standard says so on each. | IMPLEMENTED *(as doctrine)* | — *(that is the point of the ruling)* | [platform-architecture-standard.md](standards/platform-architecture-standard.md) §5 |
| 2026-07-28 | **A Save that changed nothing is an operator act worth keeping**, but `success` on `role.permissions_changed` asserts an event that did not occur → short-circuit the write, KEEP the audit row, `outcome='no_change'`. Compare as SETS, not arrays — a pure reordering is not a change. | IMPLEMENTED | STD-023 · `20260728c` V3 | tech-debt #74 · ledger #163 |
| 2026-07-28 | **Create tables through migrations, not the dashboard table editor** — the table editor's `supabase_admin` default ACL grants TRUNCATE + REFERENCES to `anon`, and RLS cannot filter TRUNCATE. The SQL editor is NOT the gap (proven by the V4 probe, not assumed). | IMPLEMENTED *(as a rule)* | — *(owed: the schema-snapshot checker; `verify-universals` cannot host it — it reads repo `.sql`, not the catalog)* | CLAUDE.md §6 r17 · ledger #160b |
| 2026-07-27 | **A staff member READS what they need to do the work, and WRITES nothing without an explicit grant.** | IMPLEMENTED | capP · `STAFF_DEFAULT_BUNDLE` | [rbac-transition-execution-plan.md](decisions/2026-07-27-rbac-transition-execution-plan.md):359 |
| 2026-07-27 | **Eleven delivery permissions** — read + update on both delivery resources. | IMPLEMENTED | manifest | [rbac-transition-execution-plan.md](decisions/2026-07-27-rbac-transition-execution-plan.md):821 |
| 2026-07-27 | **Owner-masking is definitively ruled OUT** — `is_owner=false` for a manager is not simulated by hiding; the manager IS a distinct session. | IMPLEMENTED | — | [rbac-transition-execution-plan.md](decisions/2026-07-27-rbac-transition-execution-plan.md):308 |
| 2026-07-27 | **No default bundle, role definition, or member array may hold a `declared-unwired` string** — the Roles page filters it out, so a held one is UN-REMOVABLE through the UI. | IMPLEMENTED | capQ (a)(b) · V5 | [permissionManifest.ts](../packages/shared/src/auth/permissionManifest.ts):918 |
| 2026-07-26 | **TWO OWNERS.** An LLC has two owners — David and Regina; Lauren at LAWNS holds owner-level access. `businesses.owner_id` is single-valued and CANNOT express that. | IMPLEMENTED | capA | this build · the ruling Lightning acknowledged and then designed on `owner_id` anyway |
| 2026-07-26 | **R1–R9** — the resource:action model: create is free of read, `margin:read` is derived, the unmintable five have no delete verb, `view_dashboard` retires into the `member` sentinel. | IMPLEMENTED | capP · capQ | [resource-action-permission-spec.md](resource-action-permission-spec.md) v3 |
| 2026-07-23 | **The funnel is the ONLY way a role→permission fact changes** (OPTION 1) — template write + member re-materialisation + audit row, one transaction. Direct writes are refused by the side-door trigger. | IMPLEMENTED | `20260723` §1 trigger | [roleFunnel.ts](../packages/shared/src/auth/roleFunnel.ts) |
| 2026-07-23 | **Mints read the resolved floor** — `DEFAULT_PERMISSIONS` retired; an invite and an owner-signup seed from the SAME resolution the Roles tab renders. | PARTIAL — `OnboardingWizard:561` and `Settings:205` comply; **`SignUp.tsx` still writes a hardcoded literal** | — | [roleDefinitions.ts](../packages/shared/src/auth/roleDefinitions.ts):122 |
| 2026-07-22 | **Over-commit OVERRIDE: default REFUSE**, owner/manager permission to proceed, audit row per D-51. **`reserved` is removed from the lot-status vocabulary.** | OPEN | — | D-51 · D-52 |
| 2026-07-21 | **The middle inventory state is RESERVED in DISPLAY** — the column name is irrelevant; what the owner reads is what is ruled. | OPEN | — | [D-52](decisions/2026-07-21-inventory-states-onhand-committed-available-D52.md) |
| 2026-07-21 | **Two logs split by RETENTION** — event log is the source of truth (replay), audit log is the accountability record (nothing computed from it). A discretionary act writes BOTH. | OPEN — **docs only, nothing built**; zero code, zero schema | — | [D-51](decisions/2026-07-21-event-log-audit-log-retention-split-D51.md) |
| 2026-07-20 | **An append-only ledger row is immutable including against `postgres`** — genesis rows are not re-dated; fix at the consumer. | IMPLEMENTED | `20260720` trigger · V3(b) | [D-50](decisions/2026-07-19-inventory-movement-ledger-D50.md) · tech-debt #70 |
| 2026-07-17 | **Owner-prove STEP ZERO** — confirm the deploy for THIS SHA is live before any observation counts as evidence. Homed where DAVID stands (GATE 0 on the board), not only in a protocol doc. | IMPLEMENTED | SHA stamp → DebugPanel footer | OP-15 · tech-debt #60 |
| 2026-07-16 | **§3 HANDOFF retains N=3**; overflow moves VERBATIM to the archive. CLAUDE.md line 3 is a POINTER, never a summary. | IMPLEMENTED | §9 close step 0 | OP-13 |
| 2026-07-09 | **TIER DISCOUNT IS PERCENT-OFF-BASELINE**, owner-set per tier, default 0% (retail 0 · contractor 10 owner-editable · wholesale owner-set), taken off the **stored** `sell_price` — never re-derived from cost ([[D-35]]). Paired with **D-38**: tiers are **FLAT and OWNER-MANAGED with manual promote/demote — no auto-progression, no decay, no cumulative-spend engine; the system never assigns or removes a discount on its own.** *(**D-38 is WHO decides a tier; D-55 is HOW it computes.** They were conflated for three weeks because neither had a register row.)* | **DECIDED — build OWED.** The mechanism is settled and `tierPricing.test.ts` carries **138 assertions**; the **AC-4 hold in `submit.ts` is still in place**, and the CONFIG surface (set the %, assign a tier) is a GAP — `customers.price_tier` is display-only. 🔴 **Numbered [[D-55]] on 2026-08-01, decided 2026-07-09:** it lived in **three docs with no D-number and no row here**, its own home titling itself a *"D-35 addendum"* — cited in the close-out ledger, `built-inventory`, the bootstrap and two board stories, and invisible to anyone reading this file in full | — *(no cap asserts the tier math; `tierPricing.test.ts` proves the arithmetic, not that the hold ever comes off)* | [D-55](decisions/2026-07-09-tier-pricing-mechanism.md) · [D-38](decisions/2026-07-10-contractor-pricing-model.md) |
| 2026-06-25 | **THE PERSON SPINE.** One row per human behind every role — lead, customer, member, invited-worker, labor-resource — in a GLOBAL `people` table with **no `business_id`**. **THE ONE RULE: `person_id` is an OVERLAY, NEVER the auth principal.** RLS stays on `user_id = auth.uid()`; the spine ADDS a table and nullable columns and ALTERS no existing policy, function, or trigger. `auth_user_id` is nullable (an invited-not-enrolled worker and a captured customer have no auth account) and UNIQUE (one auth account = exactly one person). *Ruled 2026-06-25 · **captured to RULINGS 2026-08-01, six weeks late** — the migration shipped and the ruling never did.* | IMPLEMENTED — [`20260625_person_spine.sql`](../supabase/migrations/20260625_person_spine.sql) applied; `people` + nullable `person_id` overlay columns | — *(no cap asserts the overlay rule; nothing fails the build if a future policy keys RLS on `person_id`, **which is precisely the thing the one rule forbids**)* | `data/grower-scan/person-spine-recon.md` (R1 shape · R4 RLS-to-protect · R5 FK surface) |
| 2026-06-18 | **QR RESOLVE — three jobs a URL plays; conflating them was the original bug.** (1) **The QR points at TRACE, never the tenant's own site** — a tag targeting `lawnstrees.com` is a MISMATCH TO FIX: their site is a dead end, no cart, no capture, no session. (2) **The tenant's URL is BRANDING** on the page TRACE serves — one app, skinned per tenant. (3) **STRIP-AND-RESOLVE** handles an existing tag that points at their site: strip the URL, keep the identifying part, resolve against tenant-scoped inventory. **Capture and count are URL-AGNOSTIC** — operator-facing, tenant-scoped; the customer-facing URL never enters that path. *Ruled 2026-06-18 · **captured 2026-08-01**, having resurfaced twice in between as though open.* | **IMPLEMENTED — and the design and the build AGREE.** `scanTag.ts` strips `/plant/<tag>`; the ladder is column-agnostic by design (L1 `cultivar_plants.tag_id` → L2 `business_inventory.sku` → L4 name token-set → L5 size-picker). The METHOD was settled in June; only *"was it built"* was ever open. **Recorded so it is not re-opened a seventh time.** 🔴 Separate and real: **nothing PRINTS a tag** — `generatePlantQR` has zero callers | — *(nothing asserts a QR target is TRACE, nor that the ladder keeps its lane order. The guard that would have helped most was **this row existing**)* | [D-53](decisions/2026-06-18-qr-resolve-D53.md) · D-34 |
| 2026-06-03 | **GATED PRICE — a price is unlocked by a KEY, and the key differs by context. The two are SIBLINGS, NOT RIVALS.** **ON THE LOT the key is LOCATION** — physical presence substitutes for contact details. **ON THE WEB the key is REGISTRATION** — name, address, ZIP before any price renders. One mechanism, per-context value (AC-4); a third context, contractor KEY = TIER, plugs in rather than forking. *Ruled in the 2026-06-03 flow spec · **captured as a decision 2026-08-01** — it lived in a flow spec and two stories, so it had no status, no guard, and no drift row.* | 🔴 **OPEN — and worse than unbuilt: the platform honours NEITHER key.** Price renders on **screen 1 of 5** (`PlantHero.tsx:88`, `PlantProfile.tsx:145`) while `CustomerCapture` is **screen 3** — the REGISTRATION key's *inverse*. **No geofence exists** (the only `navigator.geolocation` is `RhythmLogger`, unrelated). **US-001 demands unconditional price on the scan page** — the older story stayed authoritative because the newer decision never became one. **Recorded as a KNOWN GAP against a decided model, not as an open question** | — *(a "price render site outside a gated context fails the build" cap is buildable and cheap; named, not taken)* | [D-54](decisions/2026-06-03-gated-price-key-D54.md) · flow spec §5 + §6 |

> 🔴 **The three rows above are the OLDEST rulings in this file and were ADDED LAST — on 2026-08-01,
> six weeks to two months after they were made.** They sort to the bottom because the Date column is
> when DAVID RULED, not when someone finally wrote it down, and that gap is the point. This file was
> created on 2026-07-30 in response to four rulings evaporating; these three had already evaporated
> before it existed, which is why creating the file did not retroactively catch them. **A ruling made
> before the register existed is not automatically in the register.** See the sweep at the foot of
> this file.

## OWED — the ruling itself is the deliverable

These are on David's table. Each blocks something concrete; none is rhetorical.

| Question | What it blocks | Source |
|---|---|---|
| ⏸️ **DEFERRED BY THE OWNER 2026-08-23 (R-16) — STILL OWED, NOT UNRESOLVED-BY-OMISSION.** David: *"we will have to live with this until after the demo and I figure out how I want this to work."* **Do not answer it by picking a default in code.** The question below stands unchanged. 🔴 **F-A — A LAPSED `add_on` AND A SWITCHED-OFF `core_optional` LOOK IDENTICAL UNDER THE FUZZ RULING AND MUST NOT ACT IDENTICALLY.** R-2 says the disabled/lapsed tile is the fuzz and selecting it **requests payment** — ✅ correct for an `add_on` whose trial expired unpaid: there is something to pay, so fuzz + price. 🔴 **But a `core_optional` is $0 and *nothing expires because there is nothing to expire* (David's own 2026-08-02 ruling), so "requests payment" is a prompt to pay nothing.** Its honest action is **fuzz + "turn it back on"**. **Same visual state, different action — and the action is decided by `billing`, the field that already decides `enabledByDefault` and (per R-1) whether the switch renders at all.** ⚠️ **Flagged rather than decided: this is a refinement of David's own ruling made minutes ago, and picking the branch in code is exactly what this table exists to prevent.** | the fuzz build — the tile's action, not its appearance. ⚠️ **NOT ANSWERED BY THE 2026-08-24 OFF SWITCH (ledger #212), and saying so is the point:** that build gave `core_optional` and `add_on` **different CONFIRMATION COPY** on the marketplace (*"your settings are kept"* vs *"the current period runs out as normal"*) because the two facts genuinely differ and neither claims a refund — **but F-A is about what a DISABLED TILE DOES WHEN SELECTED**, on a surface that does not exist yet. Different moment, different surface, still open. **No default was picked in code.** | David's ruling 2 (R-2), this session · ledger #203 · [[#201]] |
| ✅ **ANSWERED 2026-08-23 BY RULING R-15 — AND THE ANSWER IS (a): THE SAME ACT, THE SAME STRING, NOTHING NEW.** The capability David was being asked to rule on **already exists and is already attributed** — a seller can change the delivery fee or the per-plant price at checkout, and the record names who did what and when. So the three-way fork was asking about a build that is done. ⚠️ **The residual moves to R-7 rather than closing with this row: the lost-money REPORT does not exist**, and free-form is exactly the case where the sanctioned-tier-list control does nothing and the report does all the work. **The question below is retained verbatim as the reasoning R-15 answers.** 🔴 **F-B — IS A FREE-FORM DISCOUNT AMOUNT A DIFFERENT ACT NEEDING A DIFFERENT STRING, OR SHOULD IT NOT EXIST AT ALL?** R-7's cost-benefit rests on a control David named himself: **the tier list IS the control** — a seller picking from Lauren's configured types can only give away a percentage she already sanctioned, so attribution is enough. 🔴 **A free-form amount removes that control entirely and leaves attribution doing ALL the work**, which is a materially different bargain from the one the ruling weighed. **Three ways out: (a)** free-form is the same act — one string, attribution covers it; **(b)** free-form is a distinct act — its own string, so *may take a sale* and *may invent a number* separate; **(c)** free-form should not exist — every discount comes from the configured list, and the answer to a new case is to configure a new type. ✏️ **Do not resolve it by shipping a free-form box and relying on the report** — that is (a) chosen by default rather than by decision. | whether `order_discount:apply` is one string or two — and therefore the discount build's permission shape | David's ruling 7 (R-7), this session · ledger #203 |
| 🔴 **N-A — WHAT IS THE *UNDER-PRODUCTION* STATE CALLED? THE STATE IS RULED; ONLY THE WORD IS OPEN.** ✅ **Ruled 2026-08-23 (R-16): it means NOT READY FOR SELL.** A build may rely on that semantics — it may **not** pick the word, because a name that ships is the name every screen, every export and every conversation with Lauren inherits. **Deferred by David until after Wednesday**, deliberately and with a date. ⚠️ *Recorded so the next session does not mint a label in a component and call it settled — that is answering a ruling with a constant.* | any surface that must render or filter the not-ready-for-sell state | David's ruling 9, this session · ledger #205 · **R-16 ②** |
| 🔴 **N-B — DOES JUAN'S ROLE CLONE *MANAGER* AND SUBTRACT, OR CLONE *STAFF* AND ADD? DAVID DISCUSSES IT WITH LAUREN ON WEDNESDAY.** 🔴 **THE CONSTRAINT HE MUST CARRY INTO THAT CONVERSATION, and it is the expensive half: saving the floor's own set with no tenant row MINTS AN OVERRIDE — a ONE-WAY DOOR.** So the starting point is not a convenience: it is the shape the tenant is stuck with, and *clone-and-subtract* vs *clone-and-add* produce a materially different set the first time the shared floor moves underneath it. ⚠️ **This BLOCKS R-13** (*Juan logs his own PMI*) — staff hold none of `pmi:*` today (`STAFF_DEFAULT_BUNDLE` is ten strings, `permissionManifest.ts:1260`), and moving two strings into the staff floor would pre-answer this question in the direction hardest to reverse. **No bundle was changed and no string minted.** | **R-13's build** — the staff member who does the maintenance cannot open the PMI surface at all today | David's ruling 6 + 9, this session · ledger #205 · **R-16 ③** · [[R-13]] |
| 🔴 **M-B — `user_stories.md:1052` SAYS THE CONNECTOR-MANAGEMENT CONSOLE IS `scoped-out`. DOES THE TILE ROUND TRIP REOPEN IT?** The row's reason reads *"`business_modules` is the partial impl … a full connector-management console is **post-demo**, not a gap"* — and David's requirement (*select tiles, add, deselect, re-enable, data persists*) **is that console.** Per §9's story gate this is **CONFLICT → STOP and surface**, not NO-MATCH. ⚠️ **And a surface was already built against the scoped-out row: `Subscription.tsx` (527 lines, 2026-08-02) IS a partial console with no story.** Only David may flip a `scoped-out` row. | **every scope of the tile round trip** | ledger #201 · [tile-round-trip-recon-2026-08-23.md](audits/tile-round-trip-recon-2026-08-23.md) |
| ✅ **ANSWERED 2026-08-23 BY RULING R-8 — A TRIAL TERM IS A TERM, NOT A METER.** The clock does NOT pause and does NOT reset: day 12 of 14 with the module off still leaves two days; a month turned off mid-month RUNS OUT, with no charge for the next month and **no partial refund**. 🔴 **THE THIRD OUTCOME THIS ROW FOUND — *the clock never paused, so a module off for a month burns a month* — IS NOW THE RULED BEHAVIOUR, NOT A DEFECT.** The mechanism is correct and must not be touched; **the COPY is what is owed**. The question below is retained verbatim as the finding R-8 rules on. 🔴 **M-A — DOES A DISABLED MODULE'S TRIAL KEEP COUNTING? TODAY IT DOES, AND IT IS NONE OF THE THREE OPTIONS DAVID NAMED.** Disable never clears `trial_started_at` (`20260802c:133-140` merges `config` with `'{}'` — a no-op), so on re-enable `start_module_trial` reports `was_already_running:true, restart_refused:true` (`20260801c:275-283`). ✅ **Both stated requirements ARE met — the term is not lost and not refreshed.** 🔴 **The third outcome nobody specified: the clock never paused, so a module off for a month burns a month.** ✏️ **RECOMMEND leaving the mechanism and fixing the COPY** — *"Turned off — its trial still ends [date]"* — because pausing needs accumulated-enabled-days, a THIRD field, and the 2026-08-01 pair ruling says the terms are that pair and nothing else. | MINIMUM — the off button cannot ship without this answer | ledger #201 · [tile-round-trip-recon-2026-08-23.md](audits/tile-round-trip-recon-2026-08-23.md) |
| 🔴 **M-C — DOES A DISABLED TILE GO *ABSENT*, OR RENDER *OFF*?** The six-state ruling says **absent** is correct for DOES NOT EXIST — but a module you turned off and can turn back on **does** exist, and a vanished tile leaves the owner no route back except the marketplace. Today it does neither: `useModules.ts:122-125` falls to `'available'`, the SAME state as never-enabled, and `Tile.tsx` gives `available` full colour, no badge and a live click target — **so the entire visible consequence of turning a module off is an 11px green dot disappearing.** | COHERENT — the fifth `TileState` | ledger #201 · [tile-round-trip-recon-2026-08-23.md](audits/tile-round-trip-recon-2026-08-23.md) |
| **M-D — DOES THE ROUTE LAYER READ ENABLEMENT, AND AT WHAT COST?** `router.tsx` declares 48 routes and a grep for `business_modules|module_key|useModules|enabled` returns **ZERO** — `/delivery-schedule` is gated on `deliveries:read`, a PERMISSION, so a disabled module's route renders normally and works fully. The six-state ruling already names the honest behaviour (*render and say so*), but `router.tsx` has **no access to the enablement overlay** — `useModules` fetches it, the router does not. **Not a one-line gate; that is why it sits in COHERENT.** | COHERENT — the honest route | ledger #201 · [tile-round-trip-recon-2026-08-23.md](audits/tile-round-trip-recon-2026-08-23.md) |
| **M-E — SHOULD THE MARKETPLACE BE VERTICAL-SCOPED AT ALL?** It is not today and **structurally cannot be**: `MODULE_CATALOG` (`tileRegistry.ts:384-400`) has **no `vertical` field** — the axis lives on `TILE_REGISTRY`, and the marketplace reads the catalog. ⚠️ **There is a real case for NO, and it is David's own thesis:** *"a business might need extra tiles due to some extra functions."* A nursery wanting Delivery Routing should not be told it is the wrong vertical. **Decide the direction before building the axis.** ✅ It does not bite today: **32 of 33 tiles are `vertical:'general'`** and the one exception draws nothing anywhere. | COMPLETE — moving the vertical axis | ledger #201 · [tile-round-trip-recon-2026-08-23.md](audits/tile-round-trip-recon-2026-08-23.md) |
| ✅ **C-A — ANSWERED 2026-08-23 BY RULING R-5, AND DISSOLVED RATHER THAN DECIDED: THE SERVER RESOLVES PRICE, THE CLIENT DISPLAYS IT.** Once the client is not computing price, the divergence **cannot occur** — the hazard was never about the wall, it was about two callers of one pure function receiving different inputs, and R-5 removes the second caller. ⚠️ **Its live consequence on owner-test card 24 stands until resolution actually moves server-side** (`CartReview.tsx:182` computes client-side today, which is why the #202 conditional narrowing is correct as-is and must not be changed). **The card closes when the move lands, not when this row flipped.** Original question: **WHEN A MEMBER WITHOUT `costs:read` REVIEWS AN `at_cost` ORDER, REVIEW SHOWS RETAIL AND SUBMIT CHARGES COST. WHICH GIVES?** `tierPricing.ts:228-233` degrades a null cost NEUTRAL to `sell_price` — correct on its own, and it becomes a divergence the moment a cost wall removes the input from the CLIENT only (`submit.ts:394` re-reads server-side with the service key). 🔴 **`tierPricing.ts:239-243` names this exact hazard two lines above the function** — *"Review and submit both call THIS one pure function over the same inputs, the two surfaces cannot diverge"* — and a wall breaks that premise. ⚠️ Scope stated rather than alarmed: `at_cost` tiers only, ungated sessions only, and it fails toward charging LESS than Review showed. Options: withhold the at_cost tier from an ungated session · a server preview endpoint (⚠️ api/ is 12/12) · **Review says *"priced at cost — figure withheld"*, which is D-9's own shape and the cheapest.** | the cost wall's COHERENT scope (C5) | recon 2026-08-23 · Q4 · 🔴 **UPDATED 2026-08-23 (11), ledger #202 — THIS IS NO LONGER THEORETICAL: MINIMUM SHIPPED, AND IT SHIPPED THIS DIVERGENCE FOR UNGATED SESSIONS.** Narrowing the three resolver callers means a MANAGER/STAFF session no longer receives `unit_cost` on the stock-line path, so `applyTierPrice` degrades an `at_cost` line **neutral to retail on Review** while `submit.ts:394` re-reads the true cost server-side and **charges cost**. ✅ **The OWNER is unaffected** — the fix is conditional on `costs:read` precisely so the owner's payload is unchanged, and an **unconditional** narrowing (which the build prompt asked for) would have created the divergence for the owner too. That was caught before shipping: `synthesizePlant:39` → `CartReview:182` disproves the prompt's *'merely receives'* premise. ⚠️ **It is RECORDED, not patched — inventing an answer here is what this table exists to prevent.** It is on **owner-test card 24** as a known-and-unruled note so a tester does not file it as a build defect. |
| ✅ **ANSWERED 2026-08-23 BY RULING R-9 — AND THE ANSWER IS *NO*. A CAP IS NOT OWED.** Confidential columns are enforced **in the database** (the `labor_resource_wages` child-table split, RLS keyed to the permission), not by a linter over select strings. 🔴 **The reason it must not be re-proposed is in this row's own class of evidence: a select-string cap is BLIND TO `select('*')` — which is exactly how C-D got in.** It would have been green over the defect it was commissioned to find. **The declaration-list design below is retired with the question.** 🔴 **C-B — IS THE CONFIDENTIAL-COLUMN CAP WORTH BUILDING, AND WHAT IS ITS DECLARATION LIST?** ✅ **The finding that makes it cheap: it is a JOIN of two caps we already own** — `verify-field-lists` already extracts select strings, capA already resolves a grant set per site. Nothing owns the join, and the proof is empirical: **`usePlant.ts::canViewCosts` IS baselined (`authority-grants-baseline.json:20-28`) and capA is GREEN, while the ungated leak sits 33 lines below it in the same file.** ⚠️ **Per #11's lesson the declaration list must ASSERT ITSELF IN BOTH DIRECTIONS** — a declaration for a column that no longer exists, or has since been walled, must FAIL the build, or it rots into the unread noise `OWNER_ONLY_PENDING` became. | the cap that keeps the wall standing (C6) | recon 2026-08-23 · Q10 |
| 🔴 **C-C — THE COST WALL HAS NO WRITTEN STORY, AND THE PLACEHOLDER THAT EXISTS ASSERTS THE OPPOSITE OF THE TRUTH.** `user_stories.md:292` is **`STATUS: gap`**, `ARC: identity-roles-sec`, and its `NEEDS:` line already names the exact deliverable: *"Narrate the owner/manager/staff lived need behind the wall (who sees costs, who can't, why). **BUILT but un-storied**."* 🔴 **It says BUILT — and #81 is the proof that the cost half never was.** Per §9 a `gap` placeholder is NO MATCH, so a story is created before any build spec. David dictates. | any cost-wall build spec | recon 2026-08-23 · story gate |
| ✅ **CLOSED 2026-08-23 AS RULED, NOT AS A DEFECT — RULING R-14: THE MANAGER MAY SEE MAINTENANCE COST.** Small business, most things discussed in the open, and the person scheduling maintenance arguably should know what it costs. **No child-split, no `costs:read` gate — the surface is correct as built.** ⚠️ **ONE DOC FIX IS OWED and is named rather than made: `PMI.tsx:70-80`'s header asserts a protection premise that is TRUE of the asset list and FALSE of the service log 145 lines below it.** 🔴 **C-D — DOES `business_service_log.cost` GET THE SAME CHILD-SPLIT, OR DOES THE PMI READ GATE ON `costs:read` TOO?** The #81 class on a **second table, and it is on no board**: `cost numeric(10,2)` (`20260612…:231`) on a row gated on **`pmi:read`**, which **MANAGER holds by default** (`permissionManifest.ts:1241`), read by `PMI.tsx:225` with **`select('*')`**. 🔴 **The asymmetry is already half-ruled in the module's own header** (`PMI.tsx:73-80`): the ASSET LIST *is* redacted for a `pmi:read`-without-`costs:read` session — *"a manager can legitimately hold pmi:read and NOT costs:read"* — and the service cost on the same screen is not. | the COMPLETE scope (P1) | recon 2026-08-23 · Q11 ② |
| 🔴 **C-E — WHAT HAPPENS TO A MONEY VALUE THE IMPORTER PUTS IN `business_inventory.attributes`?** ✅ Q7's good news first: `inventory:import_price` writes **RETAIL only** — `SpineField` is a closed set of five and `unit_cost` is not in it, so the string needs no splitting. 🔴 **But the vendor cost does not vanish.** A `Wholesale` column becomes `target:'attribute'`, `loadBearing:true` (`columnMap.ts:147-148`, whose comment reads *"🔴 the Wholesale case"*) and is written **verbatim** into `attributes` (`importPlan.ts:121-124, 314-315`) — the same member-readable row, one column from `unit_cost`. **#83 as scoped moves `unit_cost` and says nothing about `attributes`**, so the wall would ship and be defeated by the same week's work: **the owner-test fixture IS a grower price list with a Wholesale column, and the website person is the one who runs that import.** Options: refuse the import · wall the whole bag · a `costs:read`-gated attributes child. ⚠️ **Retroactive half: rows already carrying one need a decision too.** | the COMPLETE scope (P2) | recon 2026-08-23 · Q11 ① |
| ✅ **ANSWERED 2026-08-23 — THE LEDGER TAKES ONE ROW AT SESSION CLOSE, CARRYING WHATEVER SHE CHOSE. THE THREE PASSES ARE SESSION DETAIL, NOT LEDGER EVENTS.** This is the reading Thunder offered below and David took: the ledger records **what moved**, `inventory_counts` records **how she got there** — so nothing dates an assertion that was never physically true, and #70's genesis-row shape does not arrive through the UI. **MINIMUM is unblocked.** 🔴 **R-A — WHEN THREE PASSES MAKE ONE COUNT, WHAT DOES THE LEDGER GET: ONE ROW OF 14, OR THREE ROWS OF 3 / 9 / 14?** Lauren counts 3, finds 6 more down the row, then 5 more — **one block, three passes, one physical fact.** ONE ledger row is the honest reading (*the session asserted once*) but discards that she walked it in three passes; THREE rows put two assertions on an **immutable** log **that were never physically true at the moment they are dated** — 🔴 **#70's genesis-row shape arriving through a UI instead of a backfill**, and `20260720:203-210` means nothing can be corrected afterwards. **This is a D-50 question, not a UI question.** ✏️ **Thunder's read, offered not taken: ONE ledger row of 14, with the three passes living in `inventory_counts` — the ledger records what MOVED, the count record records how she got there.** ⚠️ **And the principle is not in the way, which is worth stating because it looks like it is:** *a count asserts, never adds* is enforced at the **LOT** level by the RPC (`20260720:528-531`, the delta is computed and never accepted from the client), and **nothing in `count_reconcile_inventory` cares how the client arrived at `p_counted_qty`** — accumulating three passes into ONE assertion before it is applied is the session asserting once, not the platform adding. 🔴 **#57's 114→118 was ROW PROLIFERATION, not summation — a different mechanism, and it is closed.** | 🔴 **MINIMUM — the smallest honest version cannot start without it** | scoping 2026-08-23 · ledger #198 · [recon](audits/count-session-multi-pass-recon-2026-08-23.md) · [[D-50]] · tech-debt #57 · #70 |
| 🔴 **S-A — DOES THE OFFLINE OUTBOX MOVE FROM `localStorage` TO IndexedDB, AND WHEN?** ⚠️ **DELIBERATELY NOT TAKEN 2026-08-24 (ledger #207), and the reason is a trade rather than a doubt: a store migration with LIVE QUEUED DATA on the eve of a demo is the wrong risk to take for a durability improvement that is not the day's defect.** The swallowed failure — the thing actually losing work — is fixed where it lived, and that fix is store-agnostic: it works identically whichever store sits underneath. ✅ **IndexedDB IS the correct long-term home and this codebase already knows it — `assets/assetBlobStore.ts` is a real IndexedDB store (`trace-assets`/`pending`), forty lines away, and its own header says why it was not put on `localStorage`.** What IndexedDB buys: it is not cleared with a Safari Private tab in the same way, it is not sharing a ~5 MB budget with everything else on the origin, and its failures are already asynchronous rather than a synchronous throw. 🔴 **THE DATA QUESTION, ANSWERED SO IT IS NOT REDISCOVERED AT MIGRATION TIME: YES, QUEUED DATA WOULD NEED MIGRATING, AND IT IS SMALL AND EASY — BUT ONLY IF IT IS DONE ON PURPOSE.** The queue is ONE key per (business, domain) — `trace:sync:<businessId>:<domain>:queue` — holding a JSON `OfflineOp[]`. A migration is: read that key, write the array into IndexedDB, delete the key, **in that order**, once at startup before the first drain. **The failure mode if it is skipped is silent and total: a phone holding an un-drained walk upgrades to a build that looks in a different store, finds nothing, shows `0 waiting to sync`, and the work is gone with no error** — the same shape as the defect #207 just closed, arriving through a deploy instead of through a throw. ⚠️ **`OfflineQueue.probe()`/`storageStatus()` and the `StoreWriteResult` contract are already store-agnostic, so the UI half needs no rework** — the migration is the adapter plus the one-time carry-over. **Ordered by David: after the demo.** | the outbox's durability against a Private tab / a full device; nothing blocking today | ledger #207 · recon 2026-08-23 Q3 · `assets/assetBlobStore.ts` |
| 🔴 **T-B — MAY AN APPEND-ONLY GUARD EVER BE DISABLED TO REMOVE A TENANT? IF THE ANSWER IS NO, HARD DELETE IS OFF THE TABLE BY CONSTRUCTION AND T-A ANSWERS ITSELF.** The 2026-07-20 ruling says an append-only ledger row is immutable **including against `postgres`**. 🔴 **And the mechanism nobody had stated: `audit_log` and `business_inventory_ledger` BOTH CASCADE from `businesses` and BOTH carry `BEFORE UPDATE OR DELETE ... FOR EACH ROW` — a FK cascade IS a DELETE, so the guard fires and the ENTIRE delete aborts.** The precedent cuts both ways: `wipe-for-person-spine.sql` DID disable every guard once, in one transaction, labelled an explicit one-time environment teardown — *"never as a quiet workaround, never a reusable normal-path move."* **Is a per-tenant delete a second such case, or is the guard absolute?** | any hard delete of any used tenant | recon 2026-08-23 · [tenant-delete-blast-radius-recon](audits/tenant-delete-blast-radius-recon-2026-08-23.md) Q4 · D-50 |
| 🔴 **T-A — DELETE OR TOMBSTONE FOR A TEST TENANT?** ✏️ **RECOMMEND TOMBSTONE, on the reasoning David has already used once — the campaign delete was ruled out because *a deleted thing destroys its own history*.** Here it is stronger: the platform has already encoded that belief in two triggers that will physically refuse the delete (T-B). A tombstone needs no guard disabled, destroys nothing, is reversible, and leaves `auth.users`, storage and every `SECURITY DEFINER` function untouched. ⚠️ **Its honest cost, stated not buried: every read that lists businesses must filter `deleted_at IS NULL`, and a tombstoned tenant still appearing somewhere is a WORSE bug than a slow cleanup — one column and N read sites, and the N is the work.** | tenant cleanup, and the shape of anything built for it | recon 2026-08-23 · Q12 |
| 🔴 **T-C — IS `Build B` STILL OWED, OR IS IT `scoped-out`?** `scripts/wipe-for-person-spine.sql`'s own header named it on 2026-06-25 — *"the per-tenant seed/reset function, queued — not built yet"* — and **it was never built.** It is the actual missing capability behind this whole recon: the reason the last cleanup reset the entire database is that **there was nothing else to reach for, and there still isn't.** 🔴 **The four-unmintable-deletes precedent applies exactly: an unfiled NO comes back as a question that costs a recon.** Build it, or file it with its `Reason:`. | the Wednesday walk-through, and every cleanup after it | recon 2026-08-23 · Q9 |
| **T-D — DOES WEDNESDAY'S THROWAWAY TENANT NEED REMOVING AT ALL?** ✏️ **RECOMMEND NO — create it, walk the provisioning path, and LEAVE it.** It costs nothing, it is the honest state (it existed; it was a test), and **it is the only option that requires no ruling and no new code before Wednesday.** | Wednesday only | recon 2026-08-23 · Q12 |
| **T-E — IF A TENANT-REMOVAL SURFACE IS EVER BUILT, WHAT GATES IT?** There is no permission string, and the obvious substitute is already disqualified: **`isOwner` was ruled DISPLAY-ONLY on 2026-07-30** (*"`owner_id` is a fact about who owns the business, NOT an authority mechanism"*). **This is the SAME open model question as `campaigns:delete`** — filed here so the two are answered together rather than twice. | any tenant-removal UI | recon 2026-08-23 · Q12 |
| 🔴 **R-B — WHERE DOES BLIND-CAPTURE MODE LIVE: A BUSINESS SETTING, A SESSION FLAG, OR ALWAYS-BLIND? AND THE REAL SUBJECT UNDERNEATH IT — IS THE QTY PREFILL AN ANCHORING BUG OR A FEATURE?** The D-50 story asks for *"a per-session mode, default blind"* — ⚠️ **that is a suggestion in a `NEEDS:` tag, not a ruling** (`user_stories.md:392`), and `blind_capture_mode` has **zero hits in any `.ts`/`.tsx`/`.sql`**. A **session flag is a MIGRATION**; a business setting may not be; always-blind is cheapest and forecloses the walk-with-the-book-visible case Lauren may want on a spot-check; a build-time switch is disqualified because it makes the behaviour untestable by David. 🔴 **The thing actually being decided is `InventoryCount.tsx:341-342`, which PREFILLS the qty box with the lot's current book on-hand** (and `:797` prints it on every size chip) — **the exact inverse of blind capture, and nobody has ever said whether it is a defect or a deliberate aid.** ✅ **Sizing fact that should inform the answer: blind capture needs NO new RPC** — it is the *absence* of the capture-time call, and `count_reconcile_inventory` already accepts `p_source_id` (`20260720:496`) so a desk applier links back with **no signature change**. The one exception is structural: a brand-new variety must still mint server-side or its count has no `inventory_id`. | the COHERENT scope — #67, and #68 with it | scoping 2026-08-23 · ledger #198 · tech-debt #67 · `user_stories.md:392` |
| 🔴 **R-C — DOES THE DESK APPLIER WRITE `inventory_count_sessions.status`, OR A NEW FIELD?** ✅ **Verified, not inferred: the table has EXACTLY ONE WRITER today** — three sites, all in `InventoryCount.tsx` (`:230` insert `in_progress`, `:690` `item_count`, `:701-703` `completed`); **`InventoryReconcile.tsx` never reads or writes it.** So this is a hazard the build would **CREATE**, not one standing. 🔴 **#71 is the live precedent for what happens when it does: one column, two authors, and D-42's qty-derive silently reverts D-52's manual `archived` — the reverting author wins and nothing says so.** ✏️ **The shape that avoids it:** `status` answers *"did the WALK finish"* and only the phone knows that; *"has the walk been APPLIED"* is a **different fact wanting a different field** — preferably an `applied_at` at the `inventory_counts` ROW level, which also survives a partial accept (**#69's class**). ⚠️ **`verify-write-paths` presents the debt either way** — it counts write paths **per TABLE**, so a second FILE writing the table fails the build unless declared, whichever column it writes. **The question is which debt, not whether.** | the COHERENT scope's applier — and whether it repeats #71 | scoping 2026-08-23 · ledger #198 · tech-debt #71 · #69 · RULINGS §A2 write-paths |
| 🔴 **R2 REVISITED — MAY A CAMPAIGN BE DELETED? AND THE FACT THAT REOPENS IT: THE OWNER ALREADY CAN, AT THE DATABASE.** `campaigns_owner` is **`FOR ALL`** (`20260529:19-20`, DELETE included) with `campaign_posts` on **CASCADE** (`:24`), and `20260727c:14-16` states it outright: *"DELETE stays owner-only through the existing `*_owner` policies."* **So David in the SQL editor is not reaching past a missing capability — he is reaching past a missing BUTTON, using a policy that is already his.** Against that: R2 ruled *"NO delete verb… per David's ruling **likely never**: a deleted campaign destroys its own history"* (`spec:91`), `permissionManifest.ts:195-202` lists `campaigns` among the FIVE UNMINTABLE DELETES, and **`verify-universals.mjs:1705-1712` fails the build on the literal string.** ⚠️ **AND THE PRECEDENT CUTS BOTH WAYS — named so nobody finds it later and reads it as permission: the rule forbids a STRING, never a delete OPERATION.** `service_offerings` is on the same list and ships a live, **ungated, unconfirmed** `.delete()` today (`Settings.tsx:415-419`; route gated only `settings:read`, member policy `FOR SELECT` only — **E5's shape**). **A hard delete on `campaigns` would break no cap. It would break the ruling, which is different and more serious.** ✏️ **THUNDER'S RECOMMENDATION, asked for: CANCEL IS SUFFICIENT FOR THE OPERATIONAL PROBLEM** — it is free (the value and its red styling already exist), needs no string, no migration and no reversal, and it takes the orphans off the active list honestly. 🔴 **ITS LIMIT, STATED: THAT IS NOT WHAT DAVID ASKED FOR.** If the requirement is *gone* rather than *stopped pretending*, R2 is what is in the way and reversing it is the task — with the tombstone build the spec already scoped (column + RPC + ledger row + audit row + read filters). | the DELETE piece, and therefore the COMPLETE scope | scoping 2026-08-23 · ledger #192 · `spec:91` |
| 🔴 **IF DELETE IS PERMITTED, WHAT GATES THE BUTTON? — A GENUINELY NEW MODEL QUESTION, NOT A NAMING ONE.** There is **no string and there may not be one** (`campaigns:delete` fails the build), and the obvious substitute is disqualified: **`isOwner` was ruled DISPLAY-ONLY on 2026-07-30** (*"`owner_id` is a fact about who owns the business, NOT an authority mechanism"*), with `BusinessProvider.tsx:735-736` recording that it survives in exactly two places, both display. **Three ways out: (a)** reverse R2 fully — mint the string **with** the tombstone the spec requires: MIGRATION + `verify-universals.mjs:1705` + `permissionManifest.test.ts:101` + read filters on four select sites; **(b)** hard delete, client-gated on `isOwner` as **DISPLAY**, with **`campaigns_owner FOR ALL` as the AUTHORITY** — ~1 prompt, no migration, no string, no cap change; **(c)** an owner-only RPC on the `set_business_module_state` precedent — audited, returns `applied`/`reason`, MIGRATION. ✏️ **RECOMMEND (b) now, (c) named as the durable form.** (b) is honest on its own terms — the authority IS the server-side policy, and deciding whether to DRAW a control is what `isOwner` is *for* under the 2026-07-30 ruling. ⚠️ **THE TENSION IS REAL AND IS FLAGGED RATHER THAN REASONED PAST:** this would be the first control in the platform whose only client gate is `isOwner`, and *"it is display, RLS is the authority"* is exactly the sentence someone will reuse later for a case where it is false. **If that worries David, (c) is the answer and it costs a migration.** | the DELETE piece's shape and size — 1 prompt vs 3–4 | scoping 2026-08-23 · ledger #192 |
| 🔴 **WHEN A CAMPAIGN IS EDITED, WHAT HAPPENS TO POSTS THAT ALREADY EXIST?** Not hypothetical drift: `generate.ts:104` instructs the model to *"write N posts **spread across the campaign window**"* with `target_category`/`description` interpolated at `:100-101`, so an edited window genuinely invalidates every post's `scheduled_date`. **And refusing the edit destroys something too** — posts carry owner-authored `edited_copy` (`CampaignDetail.tsx:88`) and have seeded `business_voice_samples` tone rows (`campaigns.ts:171-177`), the platform's accumulating record of how this owner writes. **Three options: (a) regenerate** — 🔴 destroys `edited_copy` and orphans the tone samples, and makes fixing a typo trigger a billed 15–25s regeneration; **(b) leave the drafts** — 🔴 silently, so the detail page shows posts scheduled outside the campaign's own window with no explanation (§6 r18's class); **(c) refuse the edit once posts exist** — 🔴 the campaign becomes unmaintainable exactly when it becomes useful, **and it does not even answer David's question** (the two orphans have ZERO posts, so (c) would permit editing them and forbid editing every campaign that worked). ✏️ **RECOMMEND (b) MADE HONEST:** allow the edit unconditionally, touch nothing, and make the divergence **ANNOUNCE ITSELF** — *"3 posts were written for May 1–11, before this campaign's dates changed"* with a **Regenerate** control beside it, so (a) is offered as a choice rather than imposed as a side effect. **It is the only option that destroys no information** (A9 on this surface: stale is not current), it puts the expensive destructive act behind an explicit choice, **and it reuses the same endpoint branch the L2 fix needs** — one build serving two needs. ⚠️ **Cost stated: it adds a divergence indicator with its own six-states obligation. (b)-without-the-indicator is (b)-silently, which is the option being recommended against.** 🔴 **UPDATED 2026-08-23 (3) — THIS IS NO LONGER A BARE OWED: THE CUSTOMER ANSWERED IT, AND THE ANSWER IS NARROWER THAN ANY OF (a)/(b)/(c).** Regina's bad-press scenario: a published campaign that lands badly is **ANSWERED AND RESTARTED**, not silently rewritten — *"my bad, we didn't give you all the details, thank you for bringing that to our attention."* **That splits the question on a boundary none of the three options had: PUBLICATION.** Before publication, EDIT is limited to dates and focus and the drafts follow. After publication, the posts are **out in the world and editing the campaign cannot reach them** — so the honest act is a reply and a new campaign, and (a)/(b)/(c) were all arguing about a database row while the artifact that matters had already left the building. ⚠️ **Still David's to rule** — the entry carries it as `NEEDS:` with the quote attached so the ruling is traceable to the customer who gave it, and **Lauren confirms the scene Wednesday.** | the EDIT piece, and therefore the COHERENT scope | scoping 2026-08-23 · ledger #192 · **customer answer: ledger #194** |
| 🔴 **DOES "GENERATE MORE POSTS FOR THIS CAMPAIGN" APPEND TO THIS CAMPAIGN, OR DOES THE BUTTON GO AWAY?** Today it does neither honestly: `CampaignDetail.tsx:143-167` POSTs the CREATE action, so it **mints a duplicate campaign and navigates onto it** (ledger #192b), with `catch { /* silent */ }` and no `else` on `resp.ok`. ✏️ **RECOMMEND APPEND** — it is what the label already promises, it removes the second silent orphan factory, and **it is the same `action` branch the Q4 Regenerate control needs**, so the two land as one build. **Rides the existing `api/campaigns` switch; api/ is 12/12 and no new file is required (§6 r11).** ✏️ **UPDATED 2026-08-23 (3) — THE STORY NOW ANSWERS IT BY IMPLICATION, WHICH IS NOT THE SAME AS DAVID RULING IT.** The Arbor Day entry says in Lauren's own scene: *"Halfway through she wants a few more posts, so she asks for more posts **for this campaign** and gets them, **in the campaign she already made**"* — **APPEND, stated as the requirement rather than as a preference**, and the board is the artifact a build spec cites. **Still OWED and still David's:** a story describes what the owner needs, it does not decide whether the button appends or is removed until it can. **The recommendation is unchanged and is now backed by a written story rather than by Thunder's reasoning alone.** | the L2 fix, and Q4's Regenerate control with it | scoping 2026-08-23 · ledger #192b · **story: ledger #194** |
| ✅ **ANSWERED 2026-08-23 (2) — THE STORY IS FILED; THIS BLOCKER IS CLOSED.** Three entries under `## NEEDED — cross-cutting` in `user_stories.md`: **Arbor Day** (`needs-input` — create·edit·cancel·generate-more·honest-read), **the generate-more defect** (`needs-input`), **Deleting a campaign — deliberately not built** (`scoped-out`, carrying its `Reason:`). All three parse and render (two in WHAT'S OWED, one terminal). 🔴 **`MAPS-TO: —` on all three is DERIVED, not omitted: the Campaign Scheduler is a LIVE, BUILT capability with NO row on the 24-capability board** — the nearest neighbour **4.3** is a different surface (`social_drafts` vs `campaign_posts`), and borrowing its id would have made 4.3 read as storied while its own surface has none. ⚠️ **The BUILD is still blocked — by the three rulings above, not by this row.** Arbor Day stays `needs-input` until David rules the EDIT question and Lauren confirms the scene. | ~~every piece of every scope~~ — the STORY half is closed; the RULING half is rows above | ledger #193 · scoping 2026-08-23 |
| 🔴 **WHERE DOES A PER-CHANNEL CAMPAIGN CODE LIVE AGAINST THE DISCOUNT MODEL — A NEW DISCOUNT KIND, OR AN EXISTING ONE CARRYING A SOURCE? AND IS A CODE PER-CHANNEL-PER-CAMPAIGN, OR PER-CAMPAIGN?** Regina, on the campaign she ran: *"a special code for Instagram, a special code for Facebook, a special code for X… I've got 85% of my people came from Facebook… I didn't get anything from Twitter."* **It settles a live disagreement rather than a theoretical one** — she watched four customers walk into LAWNS in one day, all older, no thirty-to-forty-year-olds except ones accompanying their parents, while TikTok and Instagram skew young. **Today that is two opinions about who the buyer is; with codes it is arithmetic.** ⚠️ **THIS IS NOT A CAMPAIGN-SURFACE QUESTION — IT IS A MONEY-PATH ONE, WHICH IS WHY IT IS NOT IN COHERENT:** a code redeemed at checkout touches `submit.ts` (the path David deferred #84 on days before a demo) and the existing discount model — `discountTypes` in `business_pricing_config` and `order_discount:apply`, with **D-55's AC-4 hold still in place**. **Sized at ~4–6 prompts, 1–2 migrations: COMPLETE at minimum, and honestly a fourth scope of its own.** | the attribution loop **campaign → post → code → order** — the first thing on the board that turns marketing effort into a number instead of a feeling | story filed ledger #194 · `user_stories.md` `## NEEDED` |
| 🔴 **CLAIM-CHECK RETENTION — HOW LONG IS THE SUGGESTION/EDIT RECORD KEPT? AND DOES THE CHECK RUN ON EVERY GENERATION, OR ONLY WHEN THE OWNER SUPPLIES A FACTUAL CLAIM?** The *Truth in advertising* story rules the BEHAVIOUR (never originate an unverified claim · never censor · three verdicts, not two · *"here's what changed", never "here's what you overrode"*) and leaves these two open. ⚠️ **Retention is not a housekeeping question here — the story says the record is HERS and VISIBLE, and D-51 already split logs by RETENTION** (event log = replay, audit log = accountability, kept for years). **Which of the two this is decides the answer**, and answering it by picking a default in code is what the OWED table exists to prevent. ✏️ **THE THIRD CLAUSE OF THE `NEEDS:` TAG IS ANSWERED HERE RATHER THAN LEFT FOR THE BUILD, because the premise was half-right:** it asks the record to reuse `receipts.accept_vs_edit`. **That column is a two-value CHECK (`20260612_receipts.sql:23-24`) computed as an OR of five booleans (`ReceiptKeeper.tsx:363-375`) — a boolean in string clothing; it records THAT something changed, never WHAT.** The before/after is its SIBLINGS `line_items_original` + `amount_original` (`20260614_receipts_reconciliation.sql:31,36`). 🔴 **And campaigns already carry the whole pattern: `business_voice_samples` (`original_text`/`edited_text`/`source`) written at `campaigns.ts:171-177` at the COPY moment — so the story's own honest limit (*"captures what LEFT TRACE, not what was published"*) is already the mechanism's real semantics.** **Two gaps, named not built:** `campaigns.ts:168` writes ONLY when `wasEdited`, so an accepted-as-is post writes **no row at all** (the one case receipts DOES record, and acceptance is a data point for this story); and **neither mechanism carries the VERDICT or its SOURCE** — that is the net-new `copy_suggestion_audit` piece and it wants a third column family, not a second mechanism. | the claim-check build, and whether it is one column family or three | story filed ledger #195 · `user_stories.md` `## NEEDED` · [[D-51]] |
| 🔴 **IS `UNDER PRODUCTION` A NEW VALUE IN THE LOT-STATUS VOCABULARY, OR A DERIVED STATE? AND IS UP-POTTING A TRANSFORMATION, OR A MOVEMENT OUT AND IN?** Lauren's correction is the model: a block goes round a LOOP — potted → **six to eight months under production, not for sale at any price** → sellable, holding ~a year → up a pot whether or not it sold. 🔴 **HALF OF THIS IS ANSWERED AT CODE LEVEL ALREADY, and the evidence points one way — but evidence is not a ruling, so it is filed here rather than acted on.** The only existing escape hatch is `MANUAL_CONDITION_STATUSES = ['damaged','returned','archived']` (`inventoryStates.ts:131`), and **a `status` value for UNDER PRODUCTION would be a THIRD author on the column tech-debt #71 already has two authors on** — the file's own comment (`:133-137`) says derived values are *"Never manually settable: the next qty write recomputes them"*, and **#71 records that D-42's qty-derive already reverts a manual `archived`.** So a status-modelled under-production would be **silently cleared by the next qty write**, while a clock derived from (potted date + rung maturation) cannot be. ⚠️ **Today the platform does not merely fail to withhold it — `deriveStatus(onHand)` (`:141-143`) ACTIVELY WRITES `status:'available'` onto a block six months from sellable**, and `checkSellable`'s three refusal reasons contain nothing that can say *not yet mature*. **Also owed and NOT a David question: Joel must supply the container sizes in order, which are never sold, and maturation + hold per rung (per variety where he knows it)** — a blank interval is UNKNOWN and stops the schedule by design. | the growing-ladder build, and with it the cost-accumulation and consumable-forecast stories that both depend on its schedule | story filed ledger #196 · `user_stories.md` `## NEEDED` · tech-debt #71 |
| 🔴 **IS BLOCK ORIGIN (GROWN / BOUGHT IN) A FIELD ON THE LOT, OR DERIVED FROM WHETHER AN ACQUISITION COST EXISTS? AND HOW DOES PER-TREE ACCUMULATED COST RELATE TO THE EXISTING `cost_objects` MODEL?** The two shapes are genuinely different: **bought in** is one line on a vendor invoice joining the ladder partway up (David captured exactly such an invoice on 2026-08-22); **grown from seed** starts at the bottom and picks up pot, soil, batch labour, water and power at every rung, so *"by the time a ninety-five gallon oak is sellable it has been through the loop four times."* **Labour is per BATCH, materials are per TREE** — three people for four hours across a thousand seeds is a fraction of a cent each and across two hundred it is not, so batch size is not optional. ✅ **Two halves are already settled and are recorded so the build does not re-derive them:** reusable kit (the timer, the happy pipe, the weed mat, the well) is **CAPITAL, shown separately and never inflating the per-unit figure** — Cost-to-Produce already does this on its own screen; and an empty cost box makes the number a **FLOOR that says so** — never a guess, never a zero, the same discipline as the 117-unquantified-costs banner. **Also owed and not a ruling: Lauren and Joel must supply real numbers for one batch.** | the accumulated-cost build — and the sentence Terry has never had: *this forty-five gallon Shumard has this much of you in it, it took three years, and you sell it for that* | story filed ledger #196 · `user_stories.md` `## NEEDED` · depends on the ladder above |
| ✅ **HALF ANSWERED 2026-08-23 — THE OUTBOUND HALF IS NOW A RULED ROW, PLATFORM-WIDE AND WIDER THAN PURCHASE ORDERS: *TRACE NEVER INITIATES AN OUTBOUND ACTION.*** A drafted order is **prepared work, not a sent one**, and must be visibly distinguishable from one. ⚠️ **STILL OWED, and it is the half nobody would notice was left: WHERE DO CONSUMABLES LIVE — a kind of inventory row, or their own thing?** Pots and labels are consumed by the WORK rather than sold, so they have no `sell_price`, no variant group and no maturity clock — putting them in `business_inventory` means every availability read, every sellable check and every count screen must learn to skip them. **That is a schema question, and it is the one that decides whether this is a small build.** Original row: 🔴 **IS A DRAFTED PURCHASE ORDER EVER SENT BY TRACE, OR ONLY EVER PREPARED FOR THE OWNER TO SEND? AND WHERE DO CONSUMABLES LIVE — A KIND OF INVENTORY ROW, OR THEIR OWN THING?** The story states the intent plainly — ***"TRACE prepares; Lauren sends. Nothing leaves the building on its own"*** — **but that is the story's assertion, not a ruling, and it is exactly the kind of outbound-action boundary that must be decided rather than inherited from a sentence.** The forecast itself needs no guessing: TRACE knows the consumption because it knows the work — *every QR label printed is a label used, every up-pot is a pot used* — and the ladder's schedule says how many pots and labels October needs **before October**. ⚠️ **Depends on the ladder: without the up-pot schedule there is no forecast, only a burn rate.** **Also owed and not a ruling: Lauren must name which consumables actually run out on her.** | the consumable-reorder build, and the platform's first outbound-document boundary | story filed ledger #196 · `user_stories.md` `## NEEDED` · depends on the ladder above |
| 🔴 **i18n SCOPE — CREW-FACING SURFACES ONLY, OR THE WHOLE APP? AND IS LOCALE PER-USER OR PER-DEVICE?** A man has worked at LAWNS ten years and does not speak English at home; he is one of the people who would be walking rows with a phone. **Regina named the timing, and that is the part that makes this urgent rather than nice-to-have:** *"if you don't put it in there, you can't really easily [add it later]."* **She is right — a locale switch designed in at the start is small; retrofitted across every surface it is a rewrite.** `SCOPE: platform`, not Cultivar: every vertical has crew. ⚠️ **Also owed and NOT a David question: someone must confirm which surfaces the LAWNS crew actually touches** — scoping this to "the whole app" without that is how a small build becomes a rewrite by choice rather than by necessity. | every crew-facing surface built between now and the ruling — each one built English-only is a surface the retrofit has to reach | story filed ledger #194 · `user_stories.md` `## NEEDED` |
| ~~🔴 **THE CAMPAIGN SURFACE HAS NO STORY, AND THE GATE SAYS ONE IS CREATED BEFORE ANY BUILD SPEC.**~~ *(closed by the row above; kept for its reasoning)* `user_stories.md` contains **ZERO campaign entries** — a grep across all 98KB returns nothing, and none of the eight `## ARC:` sections covers this surface. Per §9's story-reconciliation gate this is the **IN CODE BUT NOT ON THE BOARD** case: flag it, and write the story. **David dictates → Lightning specs → the board, with the tag schema.** ⚠️ **This blocks MINIMUM, COHERENT and COMPLETE alike, and it is not a Thunder prompt** — recorded here so it is not discovered at the start of the build session it would stop. | 🔴 **EVERY piece of every scope** | scoping 2026-08-23 · §9 story-reconciliation gate |
| ✅ **ANSWERED 2026-08-02 (2) — the fourth value is `core_optional`, and `enabledByDefault(billing)` is the one decider.** Row in the table above. **The rejected alternative is kept on the record:** a separate `default_enabled: boolean` field would make `'core'` mean two things, force a value onto all 11 rows, and require rewriting B6b — where a fourth union member needed one `BILLING_RULE` entry and no mapping change at all. | ~~contractor_tiers' live clock~~ — closed by ruling | David's ruling 3 |
| ✅ **ANSWERED 2026-08-02 (8) — YES, AND AS *ONE CALL, NOT TWO*. `set_business_module_state` CALLS `start_module_trial` inside its own transaction; the enable and the clock land together or not at all.** Row in the table above. 🔴 **FLIPPED 2026-08-21, NINETEEN DAYS LATE — the ruling shipped in `9da6790` and this row was never touched, which is the same failure as the missing ledger row #187 and was found by the same measurement (#188).** The question is closed; **`20260802c` is GATED and unapplied**, so the answer is in code and not yet in the database. ✏️ **The rejected option is kept on the record:** two sequential RPCs — a partial failure between them produces precisely the state the ruling exists to prevent (#69's class), so one call makes it UNREACHABLE rather than merely unlikely. | ~~revenue with no conversion date~~ — closed by ruling; **`20260802c` apply is what remains** | ledger #187 · `9da6790` · `20260802c` |
| 🔴 **SEED-IF-ABSENT ON MARKETPLACE LOAD — NAMED IN THE 2026-08-01 RULING AS THE RESIDUAL'S THIRD HOME, AND THE MARKETPLACE SHIPPED WITHOUT IT.** An owner who abandons onboarding with both seed calls failing has **no module rows**, and `useModules` renders a MISSING row and a disabled row identically — so he is invisible on every screen, including this one. The page now renders an empty catalog HONESTLY, but it does not REPAIR it, and repair-on-open is exactly what the ruling asked the marketplace to carry. `seed_business_modules` is idempotent, so the repair path IS the create path and this is a small build. **Stated as owed rather than quietly deferred, because the ruling named this surface by name.** | a tenant with no trial clock and no bill, permanently undetected outside `20260801c` V6 | ledger #186 · ruling 2026-08-01 |
| 🔴 **SOMETHING MUST START THE CLOCK WHEN A TILE GOES LIVE — A NAMED GAP, NOT AN IMPLICATION.** The seeder now refuses a clock for a module whose tile is `planned`, which is correct and which creates this: **a module that ships without its trial starting is one nobody ever pays for.** It is silent in the worst direction — the customer is delighted, and TRACE never bills. Today the ONLY clock-starter is `seed_business_modules` at tenant creation, so a module built after a tenant signed up gets nothing, ever. **Three mechanisms, none chosen, and they are genuinely different decisions:** (a) **flipping `status` to `'live'` triggers it** — the fact and the trigger are the same edit, but `TILE_REGISTRY` is client code and cannot write to any tenant, so it needs a backfill somebody runs; (b) **the marketplace does it on load** — the same seed-if-absent home already named for the abandoned-onboarding residual, so ONE mechanism covers both, but it starts a billing clock as a side effect of someone opening a screen; (c) **a manual act** — honest, auditable, and forgettable, which is this row's entire subject. ⚠️ **Do not build it before David rules; it fires when the first `planned` tile ships.** | every module built after a tenant signs up — revenue that is structurally uncollectable, exactly like the PMI 7-of-14 row one layer down | David's ruling, this session · `20260802b` §3 · ledger #184 |
| ✅ **ANSWERED 2026-08-02 (3) — the four COMING modules come OFF the clock**, alongside `contractor_tiers`, in one corrective migration. Recommendation (b) taken; (a) rejected because it would ship a countdown against a tile that does not exist, days before LAWNS. Row in the table above. | ~~four countdowns against nothing~~ — closed by ruling | David's ruling, this session |
| ~~🔴 **FOUR PRICED MODULES ARE ON RUNNING 30-DAY CLOCKS WITH NO FEATURE BEHIND THEM**~~ *(superseded by the row above; kept for its reasoning)* — `followup_engine` $19 (post-sale fertilizer service) · `business_insights` $19 (the AI/BI layer) · `online_shop` $19 · `seasonal_module` $29 (Mother's Day and special sales), all four `status:'planned'`. **This is the 2026-08-02 defect in its other form:** that one was a clock over a module the SERVER refused; this is a clock over a module that WAS NEVER BUILT. **David has ruled these are LISTED as COMING and TAPPABLE — the tap is the REQUIREMENT HOOK, an input surface rather than a promise — and a countdown is a promise.** 🔴 **THUNDER'S RECOMMENDATION, asked for: `trial_days: 0` NOW, and the clock starts when the tile ships.** Three reasons. (1) It needs no model change and no new value — A7/A7b already prove `add_on` + no clock seeds dark, and the price still renders, so the marketplace shows *"$19/mo when it ships"*. (2) **The alternative — a clock that starts on ship — is the SAME BUILD either way**, because something must start it then regardless; deferring costs nothing and stops four countdowns in the meantime. (3) **Decisively: a trial's purpose is to produce a conversion decision, and an owner cannot evaluate what does not exist.** At day thirty the platform would ask him to pay for four things he has never seen — which is precisely the *"no basis on which to convert"* that ruling 2026-08-02 (1) was made to end. Existing rows need the same corrective repair as `contractor_tiers`; the clock-clearing UPDATE is already written and proven in `20260802b` §3. | whether the marketplace can honestly show a countdown next to four unbuilt features | this session · `MODULE_CATALOG` · `TILE_REGISTRY` |
| 🔴 **FIVE OF ELEVEN `module_key`s SIT ON TILES THAT NO TILE-RENDERER EVER DRAWS** — `qb_invoicing`, `online_shop`, `contractor_tiers`, `seasonal_module` (`placement:'settings'`) and `cost_to_produce` (`placement:'admin'`). **`tilesForPlacement()` has ZERO CALLERS**; the only consumer is `dashboardTilesForVerticals`, which filters `placement === 'dashboard'`. All five ARE reachable — every one is in `NAV_IA` — so this is a missing SURFACE, not a missing feature. ✏️ **This corrects a premise, and the correction is the useful part:** the suspicion was a *phantom catalog entry* (a priced module with no tile, and a cap blind to it). **There is no phantom.** `qb_invoicing` has a tile at `tileRegistry.ts:202-203`; the catalog and the registry are 11 ↔ 11; and `verify-tile-fields` **already asserts BOTH directions** — C1 forward, **C2 reverse** (*"a catalog entry NO tile references"*), 35/35 probes green. **The instinct was right and the mechanism was not: the module the dashboard cannot render is real, and the reason is `placement`, not a missing row.** So the *"catalog entry for a capability surfaced elsewhere, permitted with a declared reason"* case is **correctly NOT BUILT — there is no instance of it**, and a declaration mechanism with no member is a list that rots (#73's shape). Build it the day one exists. | the marketplace's own placement — and whether a settings/admin tile renderer is owed at all | this session · `tileRegistry.ts:529-536` · `verify-tile-fields` C1/C2 |
| 🔴 **IS THE SETTINGS/ADMIN TILE RENDERER OWED? — now with a number, and the build nags for it.** `tilesForPlacement()` and `dashboardReadouts()` both have **ZERO CALLERS**, so `placement:'settings'`, `placement:'admin'` and `kind:'readout'` are three declared states that draw nothing anywhere. **FOUR of the five `UNRENDERABLE_DECLARED` entries exist only because that renderer is missing** — `online_shop`, `seasonal_module`, `contractor_tiers`, `business_insights`. ⚠️ **The declaration list is self-clearing BY DESIGN: the day the renderer ships, those four declarations go STALE and FAIL THE BUILD**, forcing their own deletion (probe C24). So this is no longer a note that rots — it is a debt the gate will present. Only `qb_invoicing` is a genuine permanent declaration (ruled: a customer buys QuickBooks sync whether or not it has a square on the grid). | four modules that ship invisible on their ship day — and the `[ENABLE]`-with-no-surface class arriving one at a time | this session · `verify-tile-fields` assertion 3 |
| ✅ **`qb_invoicing` — THE INTENT IS RIGHT AND IT NEEDS NO CAP CHANGE, WHICH IS THE ONLY REASON NOTHING WAS BUILT.** David: *"a customer buys QuickBooks sync whether or not it has a square on the grid."* **Correct, and already true** — the marketplace renders from `MODULE_CATALOG`, which has no notion of placement, so it lists `qb_invoicing` with no change to anything. ⚠️ **The instruction was to declare it as a catalog-without-tile case and make the assertion permit it. THAT WAS NOT DONE, DELIBERATELY, because the premise is false: `qb_invoicing` HAS a tile** (`tileRegistry.ts:202-203`, `placement:'settings'`), the catalog↔registry join is **11 ↔ 11**, and C2 passes today. **Weakening C2 to permit a case with no instance would retire a working guard against the exact class it was built to catch, and leave a declaration list with no valid member — `OWNER_ONLY_PENDING`'s shape (#73), which this same session already declined once.** The real fact — *no renderer draws a settings-placement tile* — is about `placement`, and it is the row above. **Build the declaration the day a catalog entry genuinely has no registry row; the cap will say so on that day, in the build that causes it.** | nothing — recorded so the instruction is not re-issued as though it were dropped | David's ruling, this session · `verify-tile-fields` C2 |
| ✅ **ANSWERED 2026-08-02 — `unpriced`: PRICE THEM, OR RULE THEM CORE?** Both, one each: **`inventory_intake` → CORE** (it is how stock gets captured; nothing to buy) and **`cost_to_produce` → `add_on` at $29/mo, trialled** (a placeholder number, to be set from what Cost-to-Produce says the platform costs to run). Rows in the table above. **`unpriced` now has ZERO members** — it stays in the union as the honest answer for the next module that arrives without a decision, which is what it was minted for. | ~~two working modules the owner cannot reach~~ — closed by ruling | ledger #182 · David's rulings 1 + 2 |
| **TRIAL COUNTDOWN — THE URGENCY THRESHOLD.** At what point does a countdown stop being informational and start reading alarming? The tile renders **one neutral treatment at every value** today. Deliberately not chosen: picking a number in the component is answering a ruling with a constant. *(Small, and it is genuinely a display question — the 2026-08-02 ruling moved the trial-day boundary out of ACCESS and into DISPLAY.)* | nothing structural — the annotation ships and works without it | ledger #182 · `Tile.tsx` |
| **TRIAL LENGTH: 14 OR 30?** MASTER_BRIEF:243 and BD-2 say **14**; `Help.tsx:524` tells a customer **30**; **30** is what the Ignition donor code assumed and what `MODULE_CATALOG` carries, its own header calling it *INHERITED, NOT RATIFIED*. 🔴 **This is now a SCHEDULING question, not a live hazard** — the term is snapshotted at seed, so changing the catalog governs NEW trials only and cannot reach a tenant mid-trial. **Answer it before the first real signup, not before the demo.** | what every trial seeded from tomorrow is worth — and one customer-facing sentence that currently disagrees with the code | ledger #181 · `MODULE_CATALOG` · `Help.tsx:524` |
| **D-54 GEOFENCE POLICY** — the radius that counts as "at LAWNS," and the horizontal accuracy required before a position is trusted to unlock a price. A phone reporting ±2km is not evidence of presence. **An accuracy-unknown read must fail CLOSED** (#75's class — a check whose error path is "allow" is not a check). **The ONLY open item in D-54; the KEY model itself is settled.** | the on-lot guest scan's price gate, and therefore the whole on-lot guest story | [D-54](decisions/2026-06-03-gated-price-key-D54.md) · flow spec §6.6 |
| **US-C HAND-OFF MECHANISM** — how does the customer's mobile cart reach Lauren's screen for pay-in-office? The flow spec names three candidates and **picks none**: order queue on submit · Lauren searches by name or number · a code the customer shows. **Unanswered since 2026-06-03**, in the spec's own words *"an open question noted in Section 9."* | the exit half of the on-lot guest story; today *"I'll pay at the office"* is a STRING with no hand-off behind it | flow spec §6 "Pay-in-office sync question (open)" + §9.4 |
| ✅ **RULED IN SHAPE 2026-08-23 BY R-11, AND FIRST-INSTANCED THE SAME DAY (ledger #206, `3d0944b`): the union exists (`utils/readResult.ts`) and the `stockLineResolver` case named in this row's own evidence is CLOSED — 3 reads, 5 call sites, 36 net-new `tsc` errors proving the compiler finds the careless caller. ⚠️ THE 30-SITE BUILD REMAINS OWED, `readPricingConfig`'s SEVEN INCLUDED — they were deliberately out of scope.** 🔴 **READ HONESTY IS A TYPE, NOT A DISCIPLINE.** The enforcement is a **discriminated union** (`Result<T> = {ok:true; value:T} | {ok:false; error:Error}`), so the compiler refuses to let a caller reach the value without handling the failure — and **`tsc` is already in the gate**, so the class becomes a build failure rather than a rule someone has to remember. 🔴 **THIS ROW'S OWN DECISIVE FINDING IS WHY A HELPER WAS REJECTED: `readPricingConfig` already returns `{data, error}` correctly and all seven callers throw the error away.** ⚠️ **The BUILD stays OWED** (30 confirmed, ceiling 91) **and the 7 auth reads are explicitly NOT covered by the ruling.** The finding below is retained verbatim. 🔴 **DRAFT RULE, WRITTEN BEFORE THE CODE THAT WOULD NEED IT — A READ WHOSE ERROR PATH RETURNS A VALUE MUST KEEP "FAILED" DISTINGUISHABLE FROM "EMPTY."** **A9 says *absent is not empty*; this is A9 on the READ side, where we never carried it** — enforced for what a surface DISPLAYS, never for what the code BELIEVES. A read that fails and returns a fallback **has manufactured a fact**, and every consumer treats it as observed. **The minimal form is what makes it cheap: it does NOT require knowing the correct value, only that information is not DESTROYED.** `SocialSetup.tsx:67-74` and `Campaigns.tsx:50-56` each take THREE distinct inputs (loaded-and-narrow · absent · errored) and emit ONE output. 🔴 **COUNTED per #174 before proposing anything: 30 CONFIRMED, and it is a FLOOR — 61 further sites match the shape and were not individually read (ceiling 91).** **The decisive result: `readPricingConfig` ALREADY returns `{data, error}` correctly and ALL SEVEN of its callers throw the error away** — *the shared helper did the right thing and every call site undid it, so a helper cannot fix this class.* ⚠️ **Auth reads (7) may warrant a DIFFERENT answer and `callerPermission.ts:148` is a SECURITY path — that is #75's open ruling arriving at a second location.** | whether this is two call sites, a lint, or a cap — **and it IS mechanically detectable, unlike §5** | `ui-control-standards.md` §6/R1 · [recon](audits/social-campaign-path-recon-2026-08-22.md) |
| 🔴 **A CLAIM ABOUT THE DATABASE IS SOURCED FROM THE CATALOG OR IT IS NOT MADE — proposed after it cost a wrong 🔴 headline, committed and pushed.** 2026-08-22: a recon read *"GATED AND UNAPPLIED"* from `CLOSE-OUT-LEDGER.md:22` and published a **platform-wide write outage**; `pg_proc` showed the 7-arg `set_business_module_state` live and the console showed `applied:true`. **The recon had NAMED that exact fork and taken the other branch, and its own STD-021 blind spot said *"applied-state is a DOC read, not a fact"* — naming a limit does not discharge it.** **Where there is no catalog access the honest artifact is THE QUERY AND ITS BRANCH TABLE, never a headline.** Three instances now feed the same gap (**tech-debt #92**): #22 and `20260802c` both recorded-pending-while-applied, plus ~129 inline CHECKs a `conname` grep cannot match. **The record is unreliable in BOTH directions.** | every future recon whose conclusion depends on whether a migration is applied — **which, at the current rate, is the next one** | ledger #191b · tech-debt #92 |
| 🔴 **FOUR OF THE FIVE UNMINTABLE DELETES HAVE NO `scoped-out` ENTRY — SHOULD THEY BE FILED, AND HOW FAR DOES THE SWEEP GO?** `campaigns:delete` was ruled twice AND cap-enforced (`verify-universals.mjs:1705-1712`) and **still came back on 2026-08-23 as a question that cost a recon**, because it was not on the board — the exact recurrence `scoped-out` exists to prevent. **The mechanism worked as designed, one instance late.** 🔴 **The cheap count says it is not one instance:** the list is one literal (`permissionManifest.ts:195-202`) behind one cap line, and campaigns is now the ONLY member with a board entry — **`customers:delete`, `service_offerings:delete`, `deliveries:delete`, `assets:delete` have NONE.** ⚠️ **Deliberately NOT batch-filed, for a reason found while counting: `service_offerings:delete` would contradict its own code the day it was written** — `Settings.tsx:415-419` ships a live, ungated, unconfirmed `.delete()` (E5's shape, the row below). **The BROADER question is the one that is genuinely owed and it is NOT cheap:** *every* ruled-and-enforced deliberate non-build with no `scoped-out` entry is a per-row judgement over ~65 rows of this file, not a grep — **and this file is admittedly incomplete** (created 2026-07-30; three rulings landed six weeks to two months late), so any number over it is a floor with an unknown ceiling. **Named as a question rather than answered with a false count** (#174). | four standing rulings that can each come back as a "gap" and cost a recon, exactly as the fifth just did | ledger #193 · `permissionManifest.ts:195-202` |
| ✅ **RULED IN SHAPE 2026-08-23 BY RULING R-12 — A WRITE MUST PROVE IT WROTE: use `RETURNING` / the returned representation and CHECK THE COUNT.** No: a write that matches zero rows must not report success. **THE BUILD IS NOW PART-LANDED (2026-08-25, ledger #217) AND THE ROW'S OWN CLAIM NEEDED CORRECTING TWICE.** ✏️ **(i) *"Today every write on `customers` does"* IS NO LONGER TRUE, and was already only half true when written:** `customerUpsert.ts` now asserts `!== 1` on its fill-update, and `customerEdit.ts:84` + `Customers.tsx:167/193` were ALREADY row-count-checked (the cap lists them under CHECKED). What remains unchecked on this table is the INSERT-side and the sites the ratchet still holds by baseline. ✏️ **(ii) card 7's STATUS reads `owed`, not `needs-test`** — this cell said otherwise and it was stale. ⚠️ **CARD 7 WAS DELIBERATELY NOT FLIPPED BY #217: it tests `customerEdit.ts`, a DIFFERENT writer**, and #217 hardened `customerUpsert.ts`. Flipping it would assert a proof nobody performed. **E5** — should a write that matches ZERO rows report success? Latent under owner-only RLS; live the moment a non-owner writes. | customer-edit card 7 (**`owed`** — written, never run; and it tests `customerEdit.ts`, not the writer #217 fixed) | ledger #167 · **ledger #217** · [ui-control-standards.md](standards/ui-control-standards.md) §4 |
| **#75** — a device check that FAILS OPEN. Convenience → fine. Authorisation → security defect. Which is it, and which call fails? | the guarded surface, unknown | tech-debt #75 |
| **R-STATUS** — is the 4-state order lifecycle right? Should cancel auto-release (today: yes) and un-cancel auto-re-reserve (today: no)? | a DB CHECK constraint on `orders.status`; #71's lifecycle-field fix | DECISIONS-INDEX |
| **R-QBSTALE** — an edited order's QB invoice goes stale. Leave the banner, or auto re-issue? | the edit→QBO loop | DECISIONS-INDEX |
| **#71** — one `status` column, two authors (D-42 derive vs D-52 tombstone). Lifecycle state wants its own field — a migration. | a deleted lot reads `depleted`; the grid offers Delete on a tombstone | tech-debt #71 |
| **OP-13 amendment** — the CLAUDE.md budget measures LINES, and line 3 was ONE line and ~1,400 tokens. Switch to characters? | the still-open §4 "lean CLAUDE.md" item | CLAUDE.md §4 |
| **#59** — `TRACE-SESSION-BOOTSTRAP.md` has the duplicate-header disease AND is loaded every session. Does the pointer-not-summary clause extend to it? | a per-session token tax | tech-debt #59 |
| **#87** — `navPermission()` falls back to **`view_dashboard`**, a string R3 RETIRED into the `member` sentinel (`tileRegistry.ts:432-433`). **UNREACHABLE TODAY — 0 of 26 nav nodes hit it** (every node declares a `tileKey` or its own permission), so this is a landmine rather than a live defect: **the next nav node added without either becomes invisible to everyone including the owner**, and nothing will say why. Fallback to `member` (the sentinel that means "any active member"), or delete the fallback and make the field required? | the next nav node anybody adds | ledger #174 · capA A5 |
| **#90** — REMOVE `team:create/update/delete` rather than leave them mis-described. They are a DELIBERATE ARCHITECTURAL NO (the funnel is owner-only, enforced by a trigger), not a roadmap — *"never", not "unbuilt"*. **Blocked on the same trap #175 solved from the other side:** capQ reconciles the R-B2 `NOT IN` list in an APPLIED migration, so a string leaving BOTH manifest sets fails the retained `migration ⊆ manifest` direction. That reconciliation must be solved first. | three strings that describe the model wrongly | ledger #175 · David's ruling 3 |
| **#67 / #68** — blind capture as a per-session mode, and session-scoped reconcile. Named in D-50's own story as build inputs, explicitly owed. | the count-then-review loop the story describes |
| **R-GRANTDIFF** — is a STANDING before/after grant-set cap worth building, now that the one-off has proved feasible? The one-off found **1 lossy conversion, 2 silent widenings, and 2 same-legacy-string-to-different-verb splits** across 15 sites. **If the answer is no, capA's output must SAY it cannot see this class** — a cap silent about its own blind spot is exactly what #164 is about. | capA's blind spot, permanently | ledger #172 | tech-debt #67, #68 |
