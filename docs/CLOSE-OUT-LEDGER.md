# CLOSE-OUT LEDGER

**Last updated:** 2026-06-29 (**row #66 CORRECTED — PII REMOVED [DOCS-ONLY]**: removed Trinten's phone number from `docs/domain/field-notes-barryhill-2026-06.md` (kept the contact FACT, details now held by David outside the repo); added a standing **no-PII-in-the-repo** rule to the field-notes header + domain-KB README; confirmed the number did NOT propagate to README/ontology/bootstrap/ledger. ⚠️ HISTORY TRADEOFF (awaiting David's call): the number remains in commit `4f213b1`'s git log — current-file removal is the practical fix for a private/trusted repo; full scrub = `git filter-repo`/BFG history rewrite + force-push (rewrites shared history, affects anyone who has pulled), NOT done this pass. `npm run verify` exit 0 zero NET-NEW; no code/schema/migration/`[TRACE:*]`. Prior 2026-06-29: row **#67** added — **SESSION-PERSISTENCE / OFFLINE-LOGOUT FIX [BUILDER-COMPLETE, iPhone owner-proof owed — the airplane-mode test]**: built David-approved Option B + keep-last-known on top of recon #65, closing the demo-floor offline-logout bug (#64-ELEVATED). **AUTH-RESOLUTION PATH ONLY** — ONE file `packages/shared/src/context/BusinessProvider.tsx` (+63/-2 on feature branch `fix/session-persistence-offline-keep-last-known`); NO RLS / token-validation / tenant-isolation / schema change. **PART 1**: swapped network `getUser()` (`:237`, error discarded → offline=logout) for LOCAL `getSession()` (`session.user` = same User: id/email/user_metadata, consumed locally) — matches the route guard's existing-correct getSession pattern (configureAuth.tsx:65,96). **PART 2 keep-last-known**: new `isTransientReadError(err)` (codeless PostgREST error ⇒ transport/offline; coded ⇒ a server responded ⇒ existing read_error) — in the `resolved.length===0` block, a transient owner/member read error WITH last-known businesses present KEEPS them (no wipe, no booting error, `setLoading(false)`) → businessId populated + businessError null → `Dashboard.tsx:359` does NOT bounce. Current state read via new `resolvedRef` (resolve() closes over stale mount-time state — ref is the only correct read); BusinessProvider mounts above the router (App.tsx:11) so state survives multi-stop nav. **NO Dashboard edit** (keep-last-known keeps the existing :359 guard passing). **REAL LOGOUT INTACT**: getSession local → null user = genuinely-absent session = correct wipe; SIGNED_OUT (useSession :70) + explicit sign-out (AppLayout.tsx:25) untouched; guard fires only on a transient READ error, never a null session. **NO-SECURITY-RELAXATION confirmed**: returns a session the user already holds; token expiry/refresh unchanged; bearer still sent every PostgREST call; RLS still server-side. Cold-start-offline-no-cache keeps existing read_error (degenerate edge, NOT the reported bug, flagged). New `[TRACE:SESSION]` ON (session-read / no-session-wipe / transient-keep branches). `npm run verify` exit 0 zero NET-NEW (tsc 10 / eslint 266 / knip 10·14·15). **The exact live test that closes this**: David on iPhone — sign in → working screen (count/rhythm) → airplane mode mid-session → STAYS logged in, context survives a token-refresh/nav offline → connectivity returns → resumes clean → only a real Sign out / expired token logs out; `[TRACE:SESSION]` shows the kept-last-known branch fired. SHA `9cb6d8e`. Prior 2026-06-29: row **#66** added — **BARRYHILL / TRINTEN FIELD-RESEARCH BANK [DOCS-ONLY]**: banked David's ~2-hour recorded field visit with Trinten, a category buyer at Barryhill Garden Center (Leander, TX, 4.5★ full-service), into the domain KB as primary-source grower evidence — NEW `docs/domain/field-notes-barryhill-2026-06.md` (8 confirmed findings: buyer-per-category cognition · no-system + sales-only POS = the reconciliation hole · per-category seasonal rhythm · pot-size vernacular · suggest-LESS field-validated · grocery crossover · warm interview contact). **ATTRIBUTION CAVEATS HONORED** — Happy-pipe/Happy-hose = DAVID so dual-lexicon stays SINGLE-SOURCE (Lauren, no cross-grower confirmation); anti-Nelson story = DAVID (not the grower); seasonal rhythm = co-constructed (D proposed, T confirmed + added tropical inverse). **WIRED** into domain KB MAP (README §3/§7), ontology §3 reconciliation-rhythm flipped open-research→**CONFIRMED (field-validated)** + §7 suggest-LESS, bootstrap Capture Index row + FIELD FINDINGS flipped to ✅ field-confirmed. `npm run verify` exit 0 zero NET-NEW; no code/schema/migration/`[TRACE:*]`. Prior 2026-06-29: row **#65** added — **SESSION-PERSISTENCE / OFFLINE-LOGOUT RECON [READ-ONLY, doc-only, `docs/decisions/2026-06-29-session-persistence-recon.md`]**: acts on #64's ELEVATED offline-session bug. **CAUSE CONFIRMED** — `BusinessProvider.tsx:237` `getUser()` is a **NETWORK** call whose `error` is discarded; offline it returns `user:null` → the provider WIPES `resolvedBusinesses=[]` + `loading=false` (`:241-245`) → `Dashboard.tsx:359` boots to `/onboarding` on `!businessId`. TRIGGER is structural: `resolve()` re-runs on mount/remount, `reload()`, AND every `onAuthStateChange` incl. `TOKEN_REFRESHED` (`:452`), each starting with the network `getUser()` — so a flicker during any re-run nukes a still-valid local session. **getSession-vs-getUser finding:** the route guard ALREADY uses offline-safe `getSession()` (local, `configureAuth.tsx:65,96`) — proves the pattern; `BusinessProvider` is the lone resolution path on `getUser()` (network) where `getSession()` gives the same `session.user` (id/email/metadata) it consumes. Client is bare `createClient` → `persistSession`/`autoRefreshToken` defaults ON (session IS in localStorage). **Boot points enumerated** (PrivateRoute safe; #2 useSession unlikely; **#3 BusinessProvider primary**, #4 onAuthStateChange trigger, **#5 Dashboard:359 symptom — boots on ANY `businessError` incl. `read_error`**, #6 OnboardingWizard:499 secondary, #7 AppLayout correct). **Proposed minimal fix = Option B** (getSession swap **+** don't-wipe-good-state-on-transient-read-error); Option A swap-only is necessary-but-not-sufficient (residual `read_error` boot); Option C adds Dashboard distinction + offline banner + write-queue. **Explicit NO-security-relaxation**: `getSession` returns a session the user already holds; token expiry/refresh unchanged; every data call still carries the bearer + RLS still enforces tenant isolation server-side — we only stop treating a transient network failure as permanent logout. The `getSession()` swap diff shown (trivial, security-clean) but **NOT applied** — second half needs David's state-preservation pick. **HARD STOP — awaiting David's approval before any auth change.** No code/schema/migration/`[TRACE:*]`; `npm run verify` exit 0 zero NET-NEW. Prior 2026-06-29: row **#64** added — **27–29 JUNE MASTER BANK [DOCS-ONLY]**: banked the whole two-day strategy+build session from the Lightning chat into the repo, wired to every standing surface — NEW `NORTH-STAR.md` [top-of-hierarchy; spotlight brain · catch-the-threads · four shapes · TIMING LAYER · two-tier trust architecture w/ Exhibit-A evidence · hearing-aid path · build-the-brick discipline], DECISIONS.md **OP-10** Structure-Last + **D-23..D-26** [faithful/connected · rigid-spine/flexible-edge · intelligence tiers · dual lexicon], NEW `docs/domain/` knowledge base [9-section MAP + ontology], user_stories.md +2 [no-system grower + the spoken-count spec], Capture Index + bootstrap doc-hierarchy + ⚡ ACTIVE STATUS + ARC MAP arc-8 wired. **PROOFS flipped OWNER-PROVEN: #61 grower-resolve L4** [`6e75b66`, David on iPhone — Shoal Creek Vitex → L4 → DISC-1105 → count wrote 45; the LAWNS demo-blocker FALSE-UNKNOWN is dead] + **#60 field-debug capture** [`e8c9998`, trail exported from iPhone]; **#63 rhythm logger** annotated PARTIAL [clean 40-min attended segment proves rhythm legible when captured] + the attended-only / offline-session finding. OPEN items recorded: A/B next-build decision, offline-session bug ELEVATED to priority, receipt discount-line recon, Barryhill transcript parked, buyer-per-category note. `npm run verify` exit 0 zero NET-NEW; no code/schema/migration/`[TRACE:*]`.) Prior 2026-06-28: row #62 BUILT [`9f1063e`, BUILDER-COMPLETE, owner-proof owed after David applies the gated migration] — DISCOVERY SIZE-VARIANT CAPTURE, B-clean: the catalog scraper now CAPTURES WooCommerce size variants instead of dropping them. 4 pieces — (1) GATED migration `20260628_inventory_size_variants.sql` [`ADD COLUMN size text + variant_group text`, nullable, NO CHECK, + grouping index; David applies as postgres, schema-verification gate OWED, verify A-C embedded]; (2) `catalog.ts` reads the variants via a bounded `/product/<slug>` crawl [`fetchProductVariants`]; (3) DETERMINISTIC [no-AI] `extractSizeVariants(rawHtml)` — `data-product_variations` JSON primary + size `<select>` fallback; `normalizeSize` collapses the gallon family ["5-gallon"/"5 gal"/"#5"/"5G"→"N Gallon"], non-gallon passes through as TEXT; (4) `populate.ts` matches sizes→varieties by the resolver's `canonicalNameKey(slug)==canonicalNameKey(variety)` and writes ONE `business_inventory` row per (variety × size) [size=grower value, variant_group=parent slug, name stays the variety so L4 still resolves, qty/sku independent]; simple products stay single parent rows. DEPLOY-WINDOW SAFE [collapses to parent rows if columns absent → `populated-no-sizes`, mirrors deliveries]. New `[TRACE:POPULATE]` variant emits ON. PROVEN: `catalog-variants.test.ts` 31/0 [JSON+select both yield 5/15/30/45 on the LAWNS Vitex shape; per-(variety×size) expansion; parent row omits size keys], `catalog.test.ts` 35/0 unchanged, `npm run verify` exit 0 zero NET-NEW [tsc 10/eslint 266/knip 10·14·15], ingest.ts bundles clean. COUNT-SIDE size-picker = REQUIRED FOLLOW-ON [flagged, NOT built]: per-size rows make a Vitex scan return >1 same-name candidate → L4 `InventoryCount.tsx:263` AMBIGUOUS→UNKNOWN [the L5 NEED_CLARIFICATION seam the code anticipates]; sequence the picker before populating per-size rows live. Prior 2026-06-28: row #63 added — CUSTOMER-ZERO RHYTHM LOGGER [`8839502`, BUILDER-COMPLETE, phone owner-proof owed]: a data-gathering INSTRUMENT, not a product feature, modeled on the proven field-debug capture [#60, `e8c9998`]. NEW shared `packages/shared/src/rhythm/{rhythmBuffer.ts,index.ts}` — bounded localStorage ring [5000 entries, survives reload/close via pagehide+visibilitychange flush] capturing per-entry LOCATION [lat/lng/accuracy], NARRATION [voice/text], SHAPE [buy|task|inventory-check|project] + free-text thread; `haversineMeters` for move-detection; `getRhythmText`/`getRhythmJSON`/`downloadRhythm`/`shareRhythm` export. NEW `packages/cultivar-os/src/components/RhythmLogger.tsx` — `?rhythm=1`-gated [sticky] bottom-LEFT 🟢 panel [start/stop logging, mic/text note, shape chips, thread input, live pts/notes count, Share/JSON/TXT/Clear], mounted beside DebugPanel in App.tsx. `watchPosition` logs a point on >10m move + a 60s heartbeat while still [so "sat at desk 2h" shows]; voice via Web Speech [feature-detected, text always the fallback]. HONEST CONSTRAINT stated in code+UI: FOREGROUND-ONLY [web can't track a locked phone; background=future native]. Data stays LOCAL until David explicitly exports [nothing auto-uploads]; David-only, not wired into any tenant, not in nav. New `[TRACE:RHYTHM]` ON by birth. `npm run verify` exit 0 zero NET-NEW [tsc 10 / eslint 266 / knip 10·14·15]; `build:cultivar` clean 2237 modules +2. NO schema/migration/RLS/auth. Prior 2026-06-27: row #62 added — DISCOVERY SIZE-VARIANT CAPTURE [LOOK + RECOMMEND, doc-only, `docs/decisions/2026-06-27-discovery-size-variants.md`]: root-caused why the catalog scraper drops WooCommerce size variants [confirmed live on lawnstrees.com Shoal Creek Vitex — sizes 5/15/30/45 Gallon live deterministically in `data-product_variations` JSON + `<select name="attribute_pa_tree-size">` options]: TWO independent causes — (a) crawl is listing-FIRST [`catalog.ts:173`] so it reads category-card text where the size selector doesn't exist, and (b) even on a product page `stripHtml` [`website.ts:44`] destroys the variation attribute and the AI schema `{variety,botanical,category,confidence}` [`catalog.ts:225`] has NO size slot + is told "never report a quantity"; `business_inventory` has NO size/variant/parent column today [`20260612…`], `populate.ts:78` writes parent-only `DISC-####`; CAPTURE RECOMMEND = deterministic `extractSizeVariants(rawHtml)` parse [NOT AI] of the variation-form, which forces the adapter to retain raw product-page HTML + the crawl to descend to `/product/` pages; DATA-MODEL CALL = **Option B** [one `business_inventory` row per plant-per-size — A can't hold per-size counts, the whole point], B-clean = `ADD COLUMN size text` [no-CHECK, per-grower, AC-1] + grouping key [`variant_group text` lean, or `parent_id`] = **gated migration FLAGGED NOT applied**, B-minimal = name/sku encoding [no-migration, fragile]; per-grower size list honored [`SELECT DISTINCT size`, derived from their own variations, no global enum]; STRUCTURAL → STOPPED for David's A/B + grouping-key pick, did NOT build; no code/schema/`[TRACE:*]`. Prior 2026-06-27: row #61 added — GROWER-RESOLVE L4 BUILT [`6e75b66`, BUILDER-COMPLETE, phone owner-proof owed]: the EQUALITY-FIRST cut of recon #59 — NEW shared `packages/shared/src/utils/canonicalName.ts` token-set canonical key [`nameTokenSet`/`canonicalNameKey`/`tokenSetsEqual`, barrel-exported so voice/typed/QR share ONE key] wired as **L4** in `InventoryCount.handleScan` [after tag_id/sku, before UNKNOWN: scan-slug tokens == candidate name tokens, order-insensitive; 1 match→resolve, >1→UNKNOWN never auto-pick = the NEED_CLARIFICATION seam]; fixes the LAWNS false-UNKNOWN [`vitex-shoal-creek` ↔ "Shoal Creek Vitex"] with zero false-match risk; L3 stored-slug + L5 guarded-subset + L6 stemmed DEFERRED with the seam at L4; `[TRACE:RESOLVE]` shows the layer hit; NO schema/migration/auth/RLS; 21/21 `canonicalName.test.ts`; `npm run verify` exit 0 zero NET-NEW. **FAST-FOLLOW flagged:** guarded-fuzzy L5/L6 + NEED_CLARIFICATION candidate-picker UI is the next resolver build. Prior 2026-06-27: row #59 RE-VERIFIED — full-corpus churn confirms the grower-resolve design [recon, doc-only; ADDENDUM appended to `docs/decisions/2026-06-26-grower-resolve-design.md`, no new dated file — single-source]: re-ran the recon as a COMPLETE churn [every CSV parsed via Python, not the original 3-grower sample] to harden the canonical-key recommendation before any build is greenlit; every load-bearing number reproduced independently [**1,382** name-rows · **172** slug-rows = **12.4%** from 3 growers · **0** real SKUs, all `DISC-` synthetic · collisions = **2** token-set groups, BOTH same-plant punctuation artifacts = ~0 distinct-plant collision · trademark/PPAF = **22 ≈ 1.6%**]; added a SHARPER why-token-set proof [93-Nursery's botanical-prefixed slug → `slugify(common)`==url-slug in **0/96** but token-SUBSET in **92/96** ⇒ a normalized-name-EXACT key is INSUFFICIENT; L4 equality + L5 guarded-subset together are required to span all 3 slug regimes — ATX common-first-exact / Calloway's near-exact / 93 botanical-superset]; verified both §6 code claims at SOURCE [`catalog.ts:77-101 discoverCatalogLinks` matches `/product/<slug>` via `CATALOG_LINK_RE` and harvests the href for crawl-nav then DISCARDS the slug · `CatalogItem catalog.ts:48-58` carries category-page `sourceUrl` only · `populate.ts:87-98` writes synthetic `DISC-####` + buries sourceUrl in free-text `notes` · NO `slug`/`source_slug`/`match_key` column in ANY `supabase/migrations/*.sql` → §6 `source_slug` = a NEW column = gated migration + schema-verification gate, flagged NOT taken]; design + recommendation UNCHANGED, ready for David's go/no-go; LAWNS fix = L4 token-equality alone, no-schema; build-nothing-until-David-confirms-the-key; no code/schema/`[TRACE:*]`. Prior 2026-06-26: row #59 added — GROWER RESOLVE DESIGN [recon, doc-only, `docs/decisions/2026-06-26-grower-resolve-design.md`]: churned the full `data/grower-scan/` corpus [28 catalogs, 1,382 rows]; root-caused the walk-and-count FALSE-UNKNOWN [resolver joins tag_id/sku only; discovery writes synthetic DISC- skus + no cultivar_plants row → QR slug and catalog name share only the name's *words*, never compared]; identifier distribution = **12.4% slug-capable / 87.6% name-only / 0% real SKU** ⇒ canonical key MUST be the **normalized name token-set [common ∪ botanical]**, empirically ~97% resolve / ~0 collision across the corpus, token-set EQUALITY alone fixes LAWNS' word-order flip; layered resolver tag_id→sku→stored-slug→token-equality→guarded-subset[>1⇒NEED_CLARIFICATION]→stemmed→UNKNOWN; discovery-capture recommend = carry `sourceSlug` [catalog.ts already harvests the hrefs at `:79` then discards] + store a `source_slug` column [populate.ts buries it in notes today], gated-migration flagged not taken; landmines ®™/quotes/parens/`&`→amp/cultivar-collisions/typos/plural-possessive/name≠slug/87.6%-no-identifier; recommend build-nothing-until-David-confirms-the-key, LAWNS fix = L4 token-equality alone no-schema; no code/schema/`[TRACE:*]`). row #57 added — SOMETIMES-CONNECTED SYNC + OFFLINE WALK-AND-COUNT [`73d49a0`, BUILDER-COMPLETE, owner-proof owed on a phone]: NEW shared `packages/shared/src/sync/` [namespaced store + typed offline-op queue + write-through-or-enqueue + reconnect drain, idempotent via `clientId` = insert PK so replay-23505 = already-applied, NO existing-table schema change] — the minimum slice from recon #55, **NOT** a move of `DataBridge.js` [44 Ignition imports left untouched, donor-reference]; all 5 `InventoryCount.tsx` writes routed through it → the dead-zone `:181` abort is GONE [offline=queued never-fails, online=applied-now]; identity-stamp = every op envelope carries `userId`+`clientTs`, count START guarded authenticated+online [offline-cold-start blocked], DB who via `inventory_count_sessions.counted_by` [per-row `inventory_counts.counted_by` would need an ALTER — flagged NOT applied]; same-lot-twice-this-session Save SURFACES a conflict [first-vs-now, sale/miscount/moved framing + optional reason → Keep-first / Keep-new, never silent overwrite, client-state tracked = no query/schema change]; I&A heavy-sync NOT built [identity-stamp only]; `npm run verify` exit 0 zero NET-NEW [tsc 10/eslint 266/knip 10·14·15 unchanged], build:cultivar clean 2230 modules; `[TRACE:SYNC]` added + `[TRACE:COUNT]` kept ON. row #56 added — STORY BOARD SYSTEM [BUILDER-COMPLETE, owner-proof owed; two standalone root files, no app code/schema/`[TRACE:*]`]: `user_stories.md` [living source — format header + all 8 `## ARC:` sections + `## ARCHIVED`, seeded with the inventory/OCR stories using ONLY David's draft metadata + handoff-grounded narratives flagged for expansion, NO invented content] + `stories.html` [PURE RENDERER mirroring status.html — file-picker/drag-drop/auto-fetch/view-tabs/empty-state, tiny inline block-markdown so it works on file:// w/ no CDN; groups by canonical 8-arc order, cards show STATUS pill + `→ <cap id>` MAPS-TO badge (red GAP when none) + monospace PIECE chips + narrative; Active/All/Archived toggle; tally from parsed data]; the second lens beside the Status Board — **status.html = what's BUILT, stories.html = what we're building toward**, linked by MAPS-TO capability id + arc; parser proven vs live `.md` [4 stories parse, format-example does NOT leak], `npm run verify` exit 0 zero NET-NEW. row #55 added — DATABRIDGE → SHARED PROMOTION RECON [read-only, doc-only, for D-h sometimes-connected capture]: headline = local-first *persistence* in `packages/ignition-os/DataBridge.js` is real/clean/promotable but the *sync-on-reconnect* half DOES NOT EXIST [write-only queue, NO drain, NO `online` listener, stub `smartSync`] → moderate BUILD not lift-and-shift; min slice for the count loop = new `shared/src/sync/` [namespaced store + typed offline-op queue + write-through-or-enqueue + reconnect drain] routing the 5 `InventoryCount.tsx` direct-supabase writes; DON'T move the file [44 Ignition imports → extract NEW shared module, leave donor-reference]; confirmed Tech-Debt #2 leak `oauth.ts:18 IGNITION_OS_DATA` [note-only, Off-Limits] + #10 dangling `SavingsReport` import; I&A intertwine flagged separate; `docs/decisions/2026-06-26-databridge-promotion-recon.md`; no code/schema/`[TRACE:*]`. row #54 added — WALK-AND-COUNT inventory loop [`ea2ea14`, BUILDER-COMPLETE, owner-proof owed on a phone after David applies the gated count-tables migration]: scan→resolve→qty→save→next→complete; new `QrScanner` (jsQR live decode, URL-strip, manual fallback) + `InventoryCount` page (`/inventory/count`) + "Start count" on `/inventory`; SETS `business_inventory.qty` AND records each count to the GATED `20260626_inventory_count_sessions` migration [`inventory_count_sessions`+`inventory_counts`, HANDED TO DAVID — NOT applied]; UNKNOWN scan handled (quick-enter or skip-&-flag); RECONCILIATION DEFERRED but record model leaves room; `[TRACE:COUNT]` ON; verify-first answers homed in `docs/decisions/walk-and-count-inventory-verify-first.md`; `npm run verify` zero NET-NEW; NO auth/RLS/existing-schema touched. row #53 added — NAV CLEANUP [`e4fc913`, BUILDER-COMPLETE, owner-proof owed on one deploy]: 5 built + 1 investigated under D-21/D-22 — (1) single-render breadcrumb [ROOT CAUSE: dual full/mobile DOM spans, inline display:flex defeated the responsive display:none → both rendered on phones = the garbled "X / Y ‹ Z" + dup-leaf strings; fixed to ONE clickable trail], (2) `/admin` landing index [AdminIndex, gated manage_settings, cards = Admin IA children], (3) `/settings` lands on a short SettingsIndex not the wall [full page → `/settings/all`; RESOLVES #51's no-breadcrumb-on-/settings + part of its fuller-split], (3.5) visible Show/Hide collapse pill on Settings cards, (4) QBO consolidated to one `useQboConnect` [dropped the redundant full-page Accounting card via `accountingHasOwnDestination`], (5) PMI recolored to Cultivar light forest-green [shared/modules/PMI.tsx, Cultivar-only renderer]; (6) role-mismatch INVESTIGATED only — header/Settings OWNER = `businesses.owner_id` (structural), `/roles` reads `business_members.role` into an assignable dropdown with no owner match → display inconsistency; recommended display-only locked-OWNER-badge fix deferred for David, NO auth/RLS/schema/role-data touched. `npm run verify` zero NET-NEW; `[TRACE:*]` stays ON. row #52 added — DESIGN-LAW capture [docs-only]: banked the two principles ledger #51 flagged → **D-21** [DESIGN LAW: screen real estate is sacred; direct access over scroll — density default, 4 rules, canonical home for direct-access-over-scroll, enforcement = doctrine-not-gate] + **D-22** [Admin = business-entity config / Settings = user-self — the nav gating axis] in `docs/DECISIONS.md` `[CAPTURED]`; CAPTURE INDEX rows added to `TRACE-SESSION-BOOTSTRAP.md`; retro-applications [reveal-cards collapsible, Settings genuine collapse + fuller split] tracked in D-21, not built; no code/schema/`[TRACE:*]`. row #50 added — ARC MAP rendered as a 4th `status.html` board view [Active · 24 · Arcs · Archived], renderer-only, parsers proven 8/8 vs the live bootstrap; also lands the #46 step-6 maintenance-loop edit to operating-doctrine/end-of-session-protocol.md. row #47 added — front-door arc PROMOTION (synchronous, AUTH-FREE): verify-first corrected the premise [Cultivar onboarding already two progressive flows; reveal already wired] → built only the genuine gaps: wired `compareEnteredVsSite`+ADDRESS into the reveal as a hedged conflict (folded into `api/discovery/ingest.ts`, owner-RLS write-back), wired `populateCatalog` as `action=populate` on the same ingest fn (12-fn ceiling, foreground seed), + 4 bug-fixes [demo route bookend in `OnboardingWizard.DeliveryWizardPath`, `businesses.tax_rate` driving CartReview display + authoritative `orders/submit.ts`, "Delivery Routing"→"Delivery", shared `useQboConnect` hook fixing the broken Settings QBO path + reused in Dashboard]; auth boundary never crossed [no signUp/businesses-insert/acceptInvitation]; `npm run verify` exit 0 zero NET-NEW, eslint 267→266 locked; SHA `dc74ebc`. row #46 added — continuity spine: built the 🧵 ARC MAP [8 arcs as end-to-end flows, piece-statuses verified file:line] + 📚 CAPTURE INDEX [~70 pointer rows swept from the actual docs] into `TRACE-SESSION-BOOTSTRAP.md`, + a self-maintaining step-6 loop in operating-doctrine/end-of-session-protocol.md; docs-only, no code/schema/`[TRACE:*]`; flagged 2 conversation-only captures owed a home; status.html needs no render change. row #45 added — front-door arc recon [read-only, doc-only]: mapped the builtwithCAI register→invite→scrape-while-away→prepared-reveal arc against docs + code; ANSWERED the June-8 identity landmine [full async arc CANNOT promote clean — forces the auth/account-ordering reconciliation; sync reveal + 3 bug-fixes CAN promote auth-free]; map = async-invite/scrape-while-away/address→Google/address-conflict-surface/catalog-auto-populate ABSENT, engine/DiscoveryGlimpse/compareEnteredVsSite[zero callers]/populateCatalog[CLI-only] BUILT-partly-unwired; flagged recognition-moment status contradiction; `docs/decisions/2026-06-26-front-door-arc-recon.md`; no code/schema/`[TRACE:*]`). row #44 added — surfacing/suggestion-engine doctrine capture into MASTER_BRIEF PART 4 [thesis sharpened to AI-sees-the-pattern/presents-not-decides; governing posture pull-not-push/justify-each-ask/derive-don't-survey/flag-specific-gap; the suggestion engine IS cost-to-produce run FORWARD = D-009 moat's purpose; capacity gate Path-A-slack vs Path-B-investment per D-19; routing-as-utilization-readout; Lauren's-fertilizer second anchor with the 30%-yes≠70%-failure risk-flip; why-the-map-IS-the-demo], doc-only, no code/schema/`[TRACE:*]`; family of #15/#41. row #43 added — opportunity-cost layer doctrine capture: extended the cost thesis with the THREE-cost-layer model [direct / decomposed-price-legibility / opportunity-cost-of-pulled-labor] as **D-19** in `docs/DECISIONS.md`; chose DECISIONS.md over MASTER_BRIEF as the canonical cost-thesis home [that is where OP-5 anti-Nelson, D-9 ladder, D-14/D-16/D-17 all live, cross-linked]; resolves the MASTER_BRIEF PART 4 services-model open question [planting=JOB-like / product=PRODUCT]; net-new value-at-primary-work primitive + JOB-like service object flagged DEFERRED; doc-only, no code/schema/`[TRACE:*]`; MASTER_BRIEF one-line "→resolved" pointer flagged for a future edit, not taken). row #42 added — delivery-route bookend ADDRESS DATA FIX [`businesses.address` for Test Lawns completed `770 Co Rd 284`→`770 Co Rd 284, Liberty Hill, TX 78642`, the blocker that kept FIX 4 (#36) from proving]; FIX 4 code [A multi-stop assembly + B round-trip bookend] confirmed already-built, no code delta; bookend URL proven business→stops→business; row #36 unblocked; row #41 added — surfacing-thesis doctrine capture into MASTER_BRIEF PART 4 [warranty principle split, did-we-plant-it flag, customer-photo-in channel, 2×2 touch matrix], doc-only, Regina anchor conflict flagged; row #40 added — bootstrap edit: Lightning load-menu §7b + cold-start ritual + 2 standing WORKFLOW rules, doc-only; row #39 added — routing-seeder seam recon, doc-only, read-only; rows #33–#38 added — demo-fixes batch: discovery fail-loud+persistence `c8094e1`, onboarding address round-trip `2c7bf08`, leakage relabel `0a18ca1`, delivery round-trip anchor `769933f` [all BUILDER-COMPLETE, owner-proof owed on one deploy] + OCR-into-inventory reuse-verify recon + canonical status front-page. Earlier: #31/#32 Person-spine CP1/CP2) · **Author:** Thunder (read-only reconciliation from repo truth)

> **STANDING RULE (binding):** This file is the SINGLE close-out record for the platform. Every future build/recon prompt ends by **updating its row here** (bar + SHA + owner-proof status) — not just its own `built-inventory.md` line. `built-inventory.md` answers "was X built?"; THIS ledger answers "is X closed, and if not, what exact live test closes it?" A deliverable whose ledger row is missing or stale is an incomplete task. This is the process fix that stops the tab-pile from re-accumulating.

> **Method:** reconciled against REPO truth — `git log`, `built-inventory.md`, the `verify-universals.mjs` matrix (exit 0, full Cultivar column green), and migration apply-state — NOT editor tabs or memory. Where the handoff text and the code disagreed, the code won (see Fabrication check).

> **The two bars:** **BUILDER-COMPLETE** = committed (SHA), compiles, structural/unit test passes — NOT proven live. **OWNER-PROVEN** = David confirmed live under a real session with a proof artifact (HAR / screenshot / live test). Only OWNER-PROVEN (or doc-only/superseded) closes a tab. **OPEN** = not built, not run, or half-done.

---

## The table

| # | Work item | Deliverable (one line) | Commit / SHA | Bar | Owner-proof owed (exact live test) | Blocker |
|---|---|---|---|---|---|---|
| 1 | Verify / deprecate AIEngine | AIEngine deprecated IN PLACE (BACKEND-DEAD / MODULE-STRANDED, removal blocked); 3 live Ignition callers documented | `db23ee6` | BUILDER-COMPLETE (doc/ledger decision) | None — ledger decision, not a live feature | Removal blocked: port 9 Ignition tasks + tier-gating to Vercel fns, repoint 3 modules |
| 2 | Role-based financial permissions | Chokepoint + backfill (Gate-1 25/0) + hard wall Phase 1/2; Gate-2 data-layer proof 6/0 | `00dc641`, `3ef3101` | READ-side **OWNER-PROVEN** (Staff HAR); render-layer **owed** | Part A render-layer: Staff session shows no cost requests on dashboard, gated routes redirect (folds into #4) | none (code in dist; needs live deploy) |
| 3 | RLS migrations | `is_active_member()` canonical primitive + standardize 14 member policies + `md_self` leak fix; oauth-secret relocation + `has_permission()` cost wall | `07dc88a`, `e81ba6e` | **OWNER-PROVEN** (applied as postgres; read-wall HAR; matrix #3/#4/#6 PASS) | None for read-wall (proven). Write-wall live proof rides #5 | none |
| 4 | Gate 3 close-out | Read-wall + OAuth secrets relocation APPLIED + owner-proven; Part A render layer committed | `ee2edcb`, `d7ee20f`, `b1b4ed3`, `8918fe8` | Read-wall **OWNER-PROVEN**; Part A render layer **owed-after-deploy** | Deploy to Vercel → fresh Staff HAR: cost requests absent from dashboard, inventory query qty-only, direct nav to gated routes redirects | Vercel deploy |
| 5 | Gate-3b write-wall | `cost-apply` endpoint permission-gated (caller must hold `view_costs`); write-tamper proof; matrix cap #7 live | `6259f43`, `5e075e3` | BUILDER-COMPLETE (cap #7 PASS; `verify-write-wall.ts` 7/7 deterministic) | Live role-driven HAR, two real JWTs: no-`view_costs` session → 403 on cost-apply; owner → succeeds | Vercel deploy |
| 6 | costDiscovery (banked) | Cost-to-produce reasoning engine, 6 paths, 26 unit tests | `219e264` | BUILDER-COMPLETE (26 unit tests) | `verify-cost-discovery.ts` (live service-key) **UNRUN**; then owner-prove cost-apply under Role Machine | cost-apply now gated (#5); live proof still owed |
| 7 | Citation convention (MB_D- / D_D-) | Source-prefix decision citations to disambiguate the two D-number namespaces | `9add1c3` | BUILDER-COMPLETE (convention/doc) | None — doc convention | none |
| 8 | Audit Ignition RBAC enforcement | Ground-truth recon: identity/invites shared, enforcement render-only & Ignition-side; action-permission trace | `ignition-rbac-ground-truth.md`, `ignition-action-permissions-trace.md`, `role-enforcement-ground-truth.md` (untracked, by design) | Recon COMPLETE (doc-only) | None — read-only recon | Docs intentionally untracked in `data/grower-scan/` (David's call) |
| 9 | Tile registry (single source, MB_D-012) | ONE `tileRegistry.ts`; MODULE_META/MODULE_ORDER/Dashboard switches killed; every surface reads it; caps #a + #e live | `c36e562` | BUILDER-COMPLETE (caps #a + #e PASS; build clean) | Registry-driven grid live: owner sees all live tiles incl. 6 financial surfaces; Staff (no `view_costs`) sees neither cost tiles nor today's-sales/inventory-value readouts | Vercel deploy |
| 10 | Vertical field (registry) | Additive `vertical` field on every entry; vertical-aware enablement (general + business_type's vertical); Ignition-reconnection bridge | `c7cc410` | BUILDER-COMPLETE (cap #a extended PASS; no-migration) | Live grid unchanged for nursery/generalist today (no regression); split proves when first `cultivar`/`ignition` tile registered | Vercel deploy |
| 11 | Persistent identity header (cap #1) | Shared `<AppHeader>` mounted once in `AppLayout` inside PrivateRoute; business name + email + role badge; cap #1 FAIL→PASS | `e590621` | BUILDER-COMPLETE (cap #1 PASS; build clean) | Header live on multiple pages under a real session; role badge correct owner vs Staff | Vercel deploy |
| 12 | Role-config recon | Verify-first recon: role→FIELD visibility (config absent, needs owner UI + role-aware RLS) + cost-discovery-on-Opus sizing | `role-and-discovery-recon.md` (untracked, 2026-06-21) | Recon COMPLETE (doc-only) | None — read-only recon | Doc intentionally untracked (David's call) |
| 13 | Campaign double-representation diagnostic | — | **NOT RUN** (no doc exists) | OPEN | n/a until run | Not started |
| 14 | Doctrine commits (Role Machine, MASTER_BRIEF D-010..D-015) | Role architecture doctrine + write-wall invariant + acceptance tests (SKIP-until-green) | `789568d` | BUILDER-COMPLETE (doc/doctrine) | None — doctrine; acceptance tests flip live when each piece is built | none |
| 15 | Design captures (Regina Principle · Delivery · social-intelligence) | Regina Principle thesis (MASTER_BRIEF PART 4); social-scheduling/measurement concept; delivery loop (built + deploy-verified live) | `54f7511`, `0d1293a`; delivery `253cf49`/`a6cb831`/`af3b16c` | Doctrine doc-only = BUILDER-COMPLETE; **Delivery loop = OWNER-PROVEN deploy-verified live** (phone owner-prove of full snap→schedule→route still owed) | Delivery: snap Marcus Webb invoice → schedule → lands under day → "Route this day" plots (phone) | none |
| 16 | Role-config console (visibility axis) — PART A | `bm_self_update` self-grant hole closed (authority-immutability trigger + tightened WITH CHECK) + `role_definitions` three-tier store (floor/override/custom) + 3-role floor seed | `20260623_role_definitions_and_self_grant_fix.sql` | **APPLIED + catalog-verified 8/8** (A)-(H) by David (no PAT minted) | OWNER-PROVEN owed: live self-elevation-refused test (bundles with deploy pass) | none |
| 16B | Role-config console (visibility axis) — PART B | Cultivar-native RolesTab console at `/roles` (registry-fed chips, clone-not-mutate, locked system roles, factory-reset) wired to single `can()` + existing `updateMemberRole()`; verify-universals #s/#f/#g live + #e exercised | `661dcfa` | BUILDER-COMPLETE (build clean 2211 modules; changed files tsc-clean; matrix exit 0) | OWNER-PROVEN owed = David configures a STAFF role live, switches to it, confirms cost tiles + today's-sales/inventory-value readouts vanish, cost writes refuse, self-elevation refuses | Vercel deploy |
| 17 | Navigation system (breadcrumb + hamburger from one IA) | STAGE 2 BUILT (Model C2, ratified): IA encoded in the SINGLE registry (`NAV_IA` in `tileRegistry.ts` — no parallel nav list); ONE breadcrumb + ONE hamburger/nav-rail, both reading the registry, mounted once in `AppLayout`; all 8 hand-rolled back variants + the inline one deleted (PMI/RoleConfig now GAIN a breadcrumb), wizard step-backs untouched; Admin section permission-gated (owner-only children); Orders rename; Cost-to-Produce → Admin **owner-only** (nav + `/costs` route); header display-name fix (member `name` / auth metadata); verify-universals cap #n added | `b03d8a1` | **STAGE 2 BUILDER-COMPLETE** (build clean 2215 modules; matrix exit 0 incl. cap #n PASS, #a/#e still PASS; changed files tsc-clean — only the 4 documented pre-existing errors remain) | OWNER-PROVEN owed = David navigates the surfaces: consistent breadcrumb everywhere incl. PMI + `/roles`; working hamburger on mobile; Admin hidden from a Staff session; display-name in header; **Cost-to-Produce gone from a Staff session (nav AND `/costs` URL)** | Vercel deploy |
| 18 | Audit-spine recon | Read-only findings: NO audit table exists (greenfield spine); ~8 audit-worthy governance writers (role.*/member.*/cost.*/self-elevation-denied) record nothing durable; `[TRACE:*]`=debug, `business_service_log`/`pmi_service_logs`=operational. Proposed `audit_log` envelope (append-only RLS, INSERT+SELECT, no UPDATE/DELETE) + redirect order (factory-reset FIRST) + signing separate-vs-part-of flag + self-elevation rollback caveat | `data/grower-scan/audit-spine-recon.md` | Recon COMPLETE (doc-only) | None — read-only recon; **DECISION OWED**: David ratifies envelope + author-trust model (client-INSERT vs service-key) + redirect order + signing question before any migration/wiring | Doc in `data/grower-scan/` (untracked by convention) |
| 19 | `audit_log` spine — PART A | Canonical append-only audit log: tenant-scoped envelope (who/what/when/target/detail/outcome), RLS INSERT+SELECT only (NO UPDATE/DELETE policy), immutability trigger + REVOKE backstops, controlled-action convention (no CHECK), denied-attempt index (A4), client-INSERT accountability-grade | `20260623_audit_log_spine.sql` + revoke `20260624_audit_log_truncate_revoke.sql` | **OWNER-PROVEN** (2026-06-24, David at postgres prompt) | None — closed. Evidence: **9/9 catalog (A)–(I)** [A RLS on; B envelope NOT-NULL action/outcome/detail, nullable actor/target; C exactly 2 policies audit_insert(a)/audit_owner_read(r), no update/delete; D trigger trg_audit_log_immutable enabled tgtype=27 BEFORE/ROW/UPDATE+DELETE; E reject_audit_log_mutation SECURITY DEFINER owner=postgres; F UPDATE/DELETE absent from grants; G both indexes; H FK ON DELETE CASCADE; I UPDATE refused 42501] + **both behavioral halves**: UPDATE **and** DELETE proven refused → 42501 "append-only" (inside BEGIN…ROLLBACK, real rows). **FLAG 1** (TRUNCATE bypasses RLS + row-triggers): TRUNCATE was held by anon/authenticated/service_role/postgres → REVOKEd from the 3 untrusted roles, grantee now postgres-only; repo record `20260624_audit_log_truncate_revoke.sql`. **FLAG 2** (CASCADE vs immutability): tested via throwaway business → audit row → DELETE FROM businesses in BEGIN…ROLLBACK; cascade DELETE SUCCEEDED (did not abort against the DELETE guard) → audit rows are immutable in place but ride the business lifecycle + clear on tenant teardown — supports the customer-departure policy, proven by test not assumed. **#19B (factory-reset audit writer) now UNBLOCKED** (spine proven) | none |
| 19B | `audit_log` PART B — factory-reset first writer | Wire `deleteTenantRole`/console factory-reset to emit a `role.factory_reset` audit row (audit-then-delete order); rollback-safe catch-then-write for denied events (self-elevation, cost.apply_denied); verify-universals immutability + factory-reset assertion | `__PENDING__` (after apply) | **UNBLOCKED** — NOT STARTED (spine #19 OWNER-PROVEN 2026-06-24) | OWNER-PROVEN owed: live factory-reset produces an audit row; a denied attempt is recorded out-of-transaction | None — ready to build (spine proven) |
| 20 | PMI deep recon (Ignition donor vs Cultivar) | Read-only findings: Ignition PMI is localStorage-only (no DB key needed); enumerated 1.1–1.8 (no calendar, no parts→asset, interval-based schedule, no downtime state, override authority PROVEN elsewhere not in PMI, service-log PROVEN, AI suggest DARK, jobs→asset the one cross-link). Cultivar = mature Supabase rewrite with 2 strands (AI never infers `interval_days`; no review→accept gate) + latent `business_pmi_schedule.overrides jsonb`. Port-vs-build map + `override_maintenance` permission proposal (fits role-console via the `ALL_FINANCIAL_PERMISSIONS` catalog seam) + 3 net-new cross-links for capture + shared-`<Calendar>` observation. **Donor-completeness honesty check: PASS** — `packages/ignition-os/PredictiveKey.jsx` (710 ln) is a SUPERSET of `CAI-archive`'s original (537 ln; renamed, not deleted) — diff is Tailwind→inline-style port + instrumentation only, DataBridge PMI methods 6/6 identical; recon stands on the complete donor. CLAUDE.md CAI pointer fixed (CAI→CAI-archive, donor=`packages/ignition-os/`, Ignition donor-reference-only) | `data/grower-scan/pmi-recon-ignition-cultivar.md` | Recon COMPLETE (doc-only) | None — read-only recon. **DECISIONS OWED (David):** (a) override-permission name + default role assignment; (b) port-scope this pass (minimal: finish accept-flow + interval_days · vs full: + downtime + override); (c) which cross-links graduate to a design capture | Doc in `data/grower-scan/` (untracked by convention); no Ignition DB key needed |
| 22 | PMI accept-flow + `interval_days` fix (minimal) | The AI suggest path now opens a **preview→accept gate**: shared `pmiInterval.ts` derives `interval_days` (soonest convertible task cadence; usage-based intervals honestly produce no auto due date — no fabricated `\|\| 30`), and on ACCEPT writes `tasks` AND `interval_days` so `getPMIStatus` finally returns OVERDUE/DUE_SOON/OK (the stuck-"NO SCHEDULE"/NONE bug). Donor = Ignition `PredictiveKey.jsx` interval LOGIC only (NOT its tool-registry structure). | `ecedf49` | BUILDER-COMPLETE (27/0 unit; build + tsc-clean changed files; matrix green) | OWNER-PROVEN owed = David selects an asset, "Suggest Schedule" → preview shows derived cadence (+ usage-based tasks flagged) → Accept → a real OVERDUE/DUE_SOON status appears (given a last service) | none — shared/cultivar code only, no migration |
| 22B | `override_maintenance` permission — DECLARED (mechanism not built) | New shared action-permission catalog `actionPermissions.ts` (`ALL_ACTION_PERMISSIONS`); `override_maintenance` default OWNER ✓ / MANAGER ✓ / STAFF ✗; unioned into the role-config chip catalog (assignable / clone-not-mutate / factory-resettable today). Mechanism (defer/reason-required `overrides jsonb` write + audit) NOT built — declared only. | `ecedf49` | BUILDER-COMPLETE (declared; catalog-visibility + role-defaults tested in `pmiInterval.test.ts`) | OWNER-PROVEN owed = chip appears in `/roles`, grantable to a role; **mechanism owed → PMI↔Delivery build (ledger #21)** | Mechanism deferred to PMI↔Delivery coupling |
| 21 | PMI Operational Intelligence — design capture | Design capture (doc-only, completes recon #20): the surfacing engine (Regina Principle / OP-9) pointed at **equipment** — 3 cross-links as ONE family (PMI→Receipts cost attribution · PMI→Assets downtime state · PMI↔Delivery schedule-at-risk + override-to-run), the deferred `override_maintenance` mechanism homed here (reason-required/audited; Ignition donor bypassLog/AccessGatekeeper; `business_pmi_schedule.overrides jsonb` store; writes `audit_log`), shared-`<Calendar>` component note, honest dependency ledger + build path | `__PENDING__` — `docs/CONCEPT-pmi-operational-intelligence.md` + MASTER_BRIEF PART 4 `[POINTER]` | BUILDER-COMPLETE (doc/design capture) | None — design capture, cites doctrine (not built). Graduates off the bench per the build path when a consumer is built for real | David decides what graduates; build deps net-new (downtime state, receipt→asset attribution, shared `<Calendar>`) |
| 24 | Personal profile (`/profile`) + header avatar menu | **RESOLVES nav Decision A (duplicate nav).** New `/profile` page (branch on `isOwner`): OWNER edits name only → `auth.updateUser({ data:{ full_name } })` (owners have no member row; login email read-only) — this adds the missing WRITER for the owner display-name (fix-pass #23 found the read path correct but no writer); STAFF edits name/phone/email → own `business_members` row (name/phone/email ONLY — authority columns role/permissions never touched; `bm_self_update` RLS). `nav_profile` NAV_IA node added (Settings / Your Profile, `view_dashboard` → every role). Header avatar/name made clickable → account menu (card: name+email+role; items Your Profile · Help · + Business[owner] · Sign out; Settings deliberately omitted — it's a rail section). **Old Dashboard account-action row DELETED** (Help · Settings · Receipts · + Business · Sign out) → kills the Settings-listed-twice bug; `handleSignOut` removed. **FLAG:** + Business now appears in BOTH the avatar menu AND Admin (`add_business`) — reported, NOT changed (interacts with pending Cost-to-Produce/Admin fix). | `33f8324` | **BUILDER-COMPLETE** (build clean 2216 modules; changed files tsc-clean — only the 4 documented pre-existing errors remain; authority boundary held — profile writes touch name/phone/email only) | OWNER-PROVEN owed = David sets his name on `/profile` via the avatar menu → header switches from email to "David" (the display-name fix finally proves on the owner session); a STAFF session opens `/profile`, edits own contact, and can NEVER touch role/permissions | Vercel deploy |
| 23 | Nav STAGE 2 fix-pass (3 decided + 2 decisions) | **FIX 1** Cost-to-Produce reachable in Admin rail: AppNav now renders a landing-less section (Admin, route===null) as a heading + each visible child as a sub-link (was collapsed to first child → Add Business, stranding the rest). Root cause was NAVIGATIONAL, not a permission gap — `'owner-only'` resolves correctly (owner→true via `can()` short-circuit); kept owner-only (a Manager-with-view_costs still gets Admin hidden + `/costs` redirect). **FIX 2** `/help` orphan: added `nav_help` NAV_IA node (parent Dashboard → breadcrumb "Dashboard / Help"); Help mounts `<AppNav/>`+`<Breadcrumb/>` itself (kept PUBLIC, not moved into AppLayout). verify-universals cap #n now requires `nav_help`. **FIX 3** header name: read path confirmed correct (no code change); write-path gap reported (OwnerSignup sets no `user_metadata.full_name`). One source held (NAV_IA). | `eee3146` | **BUILDER-COMPLETE** (build clean 2215 modules; matrix exit 0 incl. cap #n PASS; changed files tsc-clean — only the 4 documented pre-existing errors remain) | OWNER-PROVEN owed = David live: (1) owner sees **Cost-to-Produce** under Admin in the rail; a Staff/Manager-with-view_costs session sees it in NEITHER the rail NOR `/costs`; (2) `/help` shows the section rail + "Dashboard / Help" breadcrumb. Decisions A (duplicate dashboard nav row) + B (parents list children) **awaiting David's ratification** — recon posted, NOT built | Vercel deploy |
| 25 | Identity name-layer alignment (5 divergences) | Aligned the name write/seed layer to the LOCKED identity model (full_name = person source of truth; member.name = bootstrap/display-fallback; precedence full_name→member.name→email). **FIX 1** OwnerSignup `signUp` now seeds `options.data.full_name` from the typed ownerName (was stranded). **FIX 2** Profile member self-edit writes `auth.updateUser({full_name})` first (unifies with owner path), member.name kept in sync as fallback; authority boundary held (name/phone/email only). **FIX 3** BusinessProvider:393 member precedence inverted → `authName ?? memberName ?? userEmail` (full_name first). **FIX 4** acceptInvitation seeds `user_metadata.full_name` (new user) + bridges existing user with no full_name from the invite name (never overwrites a real name). **FIX 5** configureAuth + OnboardingWizard legacy signup paths seed full_name. NO schema/RLS/owner_id changes. | `73498ca` | **BUILDER-COMPLETE** (build clean; changed files tsc-clean; verify-universals exit 0) | OWNER-PROVEN owed = (a) new owner signup → header shows typed name not email; (b) member self-edits name → persists + displays via full_name; (c) invite accept → name carries to the new person | Vercel deploy |
| 26 | Onboarding bounce loop — surface masked read error + 2 swallow fixes | World-A confirmed (rows EXIST: business `6fca6063…`, member, owner_id `4af4053d…`; the dashboard bounce is a read that ERRORS and was read as empty). **FIX 1 (primary/diagnostic)** BusinessProvider owner+member SELECTs now capture `{ error }`, emit `[TRACE:BUSINESS] owner/member path ERROR {code,message,details,hint}`, and set a DISTINCT `businessError='read_error'` (vs genuine `'no_business'`) so an errored read no longer masquerades as empty. **Query shape + RLS UNCHANGED** (surface-only; RLS root-cause fix is the NEXT prompt, after David reports the surfaced error text). **FIX 2 (independent, gated)** migration `20260624_nursery_profiles_business_id_unique.sql` adds `UNIQUE(business_id)` so the onboarding `onConflict:'business_id'` upsert stops 42P10→400 (WRITTEN, NOT applied). **FIX 3 (independent)** OnboardingWizard `finalize()` now checks the upsert `{ error }`, emits TRACE, and blocks the false "is live" screen with a `finalizeError` message instead of advancing to DONE over a failed write. Pre-existing bug, NOT caused by `73498ca` (recon-verified). | `97118ef` | **BUILDER-COMPLETE** (build clean 968kb bundle; changed files tsc-clean — only the 4 documented pre-existing errors remain; verify-universals exit 0) | (1) David deploys + reloads dashboard as the new owner (uid `4af4053d…`) → reports the surfaced `[TRACE:BUSINESS] owner/member path ERROR` line (closes the read root-cause in the next prompt); (2) FIX 2 migration applied as postgres + catalog-verified UNIQUE; (3) onboarding finalize over a failed write shows an error, not "is live" | Vercel deploy + David applies FIX-2 migration |
| 28 | Phone-field consolidation — kill dead R2 + dup-create guard + owner-profile phone + shared normalizePhone | Phone recon mapped 3 inputs → 2 real destinations + 1 dead field + 1 latent dup-business hazard. **FIX 1 (OnboardingWizard)** removed the "Your Nursery" Phone field (state/type/display/input) — it only wrote inside `finalize`'s `if(!businessId)` legacy branch, skipped on every normal signup → value dropped; business phone already collected at signup (R1) + editable in Settings (R3). Also **guarded the legacy `businesses` INSERT**: re-resolve the user's business at finalize before inserting (settled re-read closes the write-then-read race window where mount-time `checkExisting()` left `existingBusinessId` null and a blind INSERT created a SECOND business). Genuinely business-less manual-nav still creates → branch GUARDED not removed (load-bearing). **FIX 2 (Profile owner branch)** added an editable Phone row wired to the EXISTING `saveMemberField('phone')` writer (`business_members.phone`, own row) — owner DOES have a member row (OwnerSignup creates it; BusinessProvider just dedupes it out of resolution, `:341`); the proven writer now has an owner trigger. Contact only — role/permissions never touched. **FIX 3** new shared `normalizePhone(raw)` (rule-of-three): `businesses.phone` (R1 OwnerSignup, R3 Settings) + `business_members.phone` (Profile) all call ONE storage normalizer; distinct from the SMS E.164 normalizer (`send.ts`) and the live-input display formatter (`CustomerCapture.formatPhone`) — writers stay separate, only normalization shared. NO RLS / owner_id / full_name / login-email changes. | `b7ed4b7` | **BUILDER-COMPLETE** (build clean 2217 modules; changed files tsc-clean — only the 4 documented pre-existing errors remain; verify-universals exit 0) | OWNER-PROVEN owed = David live: (a) no phone field on onboarding "Your Nursery" step; (b) owner `/profile` shows an editable phone that persists to `business_members.phone`; (c) no duplicate `businesses` row creatable from finalize | Vercel deploy |
| 27 | Onboarding bounce loop — RESOLVED as write-then-read RACE (not RLS) | **HAR closed the diagnosis #26 left open:** two HARs (latest create 21:58:32) show `POST 201 /businesses` → `GET 200 /businesses?owner_id…` **1ms later returns EMPTY** → `POST 201 /business_members` 555ms after the read. Every read 200; #26's `read_error` ERROR line NEVER fired. So the bounce is a WRITE-THEN-READ RACE: the dashboard resolution runs on the fresh session before the create writes are visible to that request; a beat later the same query returns count:1. **NOT RLS / NOT the `businesses(*)` embed / NOT permissions — RLS untouched.** **FIX 1 (BusinessProvider)** owner+member resolution wrapped in `attemptResolution()` and run in a BOUNDED retry loop (`RESOLVE_MAX_ATTEMPTS=3`, ~500ms apart, ~1s/≤2s total); stops early on first non-empty result OR a real query error (errors surfaced by `read_error`, not masked); `loading` stays true across retries; settled `no_business` set only after retries exhausted; emits `[TRACE:BUSINESS] resolution retry {attempt, reason:'empty_with_session'}`. **FIX 2 (Dashboard ~:418)** redirect to `/onboarding` only when SETTLED (`businessLoading===false`) — the retry window holds `loading` true → loading state renders, not a bounce; genuine new users still reach onboarding promptly. `read_error` capture from `97118ef` preserved; tightly bounded (no unbounded poll / infinite spinner). | `56c7f81` | **BUILDER-COMPLETE** (build clean; changed files tsc-clean — only the 4 documented pre-existing errors remain; verify-universals exit 0) | Brand-new nursery signup → completes onboarding → lands on dashboard WITHOUT bounce; header shows typed name; rail populates. (Also completes name-layer proof (a).) | Vercel deploy |
| 29 | Standing build gates — ESLint + knip (baseline-and-ratchet) | Two standing gates mapped to bug classes we've actually hit, additive-only (no app logic/RLS/phone-fix). **ESLint** (`eslint.config.mjs`, flat, typescript-eslint + react-hooks): `no-unused-vars`, `no-unreachable`, type-aware `no-floating-promises` + `no-misused-promises` (the swallowed-async class; scoped to active TS src dirs), react-hooks; `no-explicit-any` OFF (type-hygiene, not a bug class — kept baseline honest). **knip** (`knip.json`): dead files + unused exports on cultivar-os/trace-app; `shared`=all-entry deep-import lib (untraceable api/ cross-imports → false-positive-free), ignition/empty pkgs ignored. **Ratchet** (`quality-baseline.json` + `scripts/quality-gate.mjs`): records debt (tsc 10 · eslint 267 · knip 10 files/14 exports/15 types), `npm run verify` fails on NET-NEW only, `npm run quality:baseline` re-snapshots. CLAUDE.md §6.8 semantic-dup rule + §6.9/§9.11 verify gate added. | `73d82a0` | **BUILDER-COMPLETE** (fail-on-new PROVEN both ways: probe → eslint 267→269 + knip 10→11, gate exit 1; removed → green exit 0; full `npm run verify` exit 0 at baseline) | OWNER-PROVEN owed = David runs `npm run verify` in his env → exit 0 at baseline; adopts the gate in the build loop | None — local tooling, no deploy needed |
| 30 | Standard-by-value rule → CLAUDE.md build gates | Added standing operating rule as **CLAUDE.md §6 rule 10**: default to the established/industry-standard pattern WHEN it earns value for our scope (catches a bug class we hit / enables a needed capability, at proportional cost); diverge in EITHER direction (below the standard OR above the need) only by explicit recorded decision — "it's the standard" is never sufficient justification on its own. Thunder must proactively flag both "this differs from the known-correct pattern" (debt drift) AND "this standard may not be worth it for our scope" (over-engineering trap). No silent divergence; no standard for its own sake; no "fix it later" without a written decision. Doc-only, no code. | `__PENDING__` | **BUILDER-COMPLETE** (doc/rule) | None — operating-principle rule; governs build decisions going forward | none |
| 31 | Person-spine CP1 — `people` table + person_id overlay + full-nuke wipe | NEW global `people` spine (one row per human): id (person_id), `auth_user_id` nullable UNIQUE FK→auth.users ON DELETE SET NULL, name fields, email/phone, NO business_id. Self-only RLS `people_self_all` (auth_user_id=auth.uid()) + updated_at trigger + auth-less email/phone lookup indexes. Nullable `person_id` overlay (FK→people ON DELETE SET NULL) on business_members/customers/labor_resources/invitations + `customers.price_tier` (text, default 'retail', no CHECK, AC-4). **person_id is an OVERLAY, NEVER the auth principal — RLS stays on user_id=auth.uid()**; migration ALTERS no existing policy/function/trigger (verified in-file). `findOrCreatePerson()` shared fn + barrel export + `[TRACE:PERSON]` emits (defined CP1, wired CP2). Full-nuke wipe script (`scripts/wipe-for-person-spine.sql`: enumerate business_id tables from live catalog → TRUNCATE CASCADE → DELETE auth.users — David chose FULL NUKE; demo returns via Build B). Recon: `data/grower-scan/person-spine-recon.md`. Migration `20260625_person_spine.sql` STAGED. | `8eda8e3` | **BUILDER-COMPLETE** (verify zero NET-NEW: tsc 10/eslint 267/knip 10·14·15; universals exit 0). Migration + wipe STAGED — NOT applied (schema-verification gate OWED, runs after David applies as postgres) | **David (gates CP2 owner-proof):** (1) run `scripts/wipe-for-person-spine.sql` as postgres — inspect STEP-A enumeration + guard discovery, run STEP B2 (privileged teardown: disable append-only guards → TRUNCATE CASCADE → re-enable), then STEP C must show **every guard back ON** (`tgenabled <> 'D'`) + **0 rows everywhere incl. audit_log** + 0 auth.users; (1b) **owner-prove the guard is restored** — a manual `UPDATE audit_log` post-reset must be REJECTED (append-only); (2) apply `20260625_person_spine.sql` as postgres + run the in-file verification (A)-(G); (3) fresh OWNER signup → `[TRACE:BUSINESS]` resolution lands on dashboard + `[TRACE:PERSON] create: new person (auth-linked)` fires; (4) confirm is_active_member resolves (SQL/HAR, Gate-3 style) | David applies wipe + migration as postgres |
| 32 | Person-spine CP2 — wire create-or-link at all 3 sources | ONE shared `findOrCreatePerson` (§6.8) wired into every human-minting path, all non-blocking (overlay; rule 6). **customerUpsert**: resolve global person at SOURCE (email→phone among auth-less people) → dedup customer WITHIN business by person_id → FIXES the email-only-dedup bug (phone-only repeat matches by phone → no second customer; Marcus-Webb-dupe class); graceful fallback to legacy email-dedup + null person_id if person layer unavailable. **OwnerSignup**: create-or-link owner's person under RLS (people_self_all, auth_user_id=self) → link business_members.person_id (business_members path only). **acceptInvitation**: create-or-link accepting user's person (service key) → set invitations.person_id + activated business_members.person_id. `[TRACE:PERSON]` on every create/link/resolve. | `c1f8be3` | **BUILDER-COMPLETE** (verify zero NET-NEW; universals exit 0; build:cultivar clean 971kb) | **David (after CP1 applied):** fresh CUSTOMER via QR-scan / OCR-invoice → `[TRACE:PERSON]` fires on person create-or-link → customer links to a person AND a phone-only repeat does NOT create a 2nd person/customer; optionally a 2nd business membership for the same person resolves as ONE person, N memberships | CP1 wipe + migration applied first |
| 33 | Discovery: fail-loud + persistence (FIX 1) | DiscoveryGlimpse silently collapsed no-profile/API-error/network-failure into `phase='done'` and rendered canned `NURSERY_SEED_INSIGHTS` as "Strengths" under "Here's what we found" (canned copy as findings; the `'error'` Phase had no render branch). Now a real returned profile is the ONLY path to `done`; failures land on a new honest `error` phase (retry / manual-entry / skip); analyzing teasers reframed "What we look for"; **businessId now passed in the POST body** so `ingest.ts:171 seedServiceOfferings` persists (was omitted → analysis ran, nothing saved). Verify-first: real cause was client-side (DiscoveryGlimpse), not ingest (which already fails loud). `[TRACE:DISCOVERY]` run/success/failure. | `c8094e1` | **BUILDER-COMPLETE** (verify zero NET-NEW: tsc 10/eslint 267/knip 10·14·15) | OWNER-PROVEN owed = David deploys → a site that can't be read shows the honest error/manual-entry (NOT canned strengths); a successful analysis persists service_offerings (`[TRACE:DISCOVERY] success seeded>0`) | Vercel deploy |
| 34 | Onboarding address round-trip (FIX 2) | Load effect SELECTed only `id,name` → `businesses.address` never populated the field/review and the normal `finalize()` path never wrote a typed address back (only the legacy create branch did) → typed address silently dropped. Now load effect SELECTs `address` (both branches) + seeds `nurseryInfo.address`; `finalize()` writes it back via owner-scoped `businesses.address` UPDATE on every path (idempotent on legacy create). RLS untouched. `[TRACE:ONBOARD]` load/save. | `2c7bf08` | **BUILDER-COMPLETE** (verify zero NET-NEW) | OWNER-PROVEN owed = David types an address at the onboarding step → it persists to `businesses.address` and reloads pre-filled (`[TRACE:ONBOARD] address saved`) | Vercel deploy |
| 35 | Leakage calc relabel (FIX 3) | Slider-driven LeakagePath result "Annual revenue left on the table" read like a finding about the user's real business — relabeled "Estimated annual leakage at your volume" + weekly line reframed as a projection. Wording only, no logic. | `0a18ca1` | **BUILDER-COMPLETE** (verify zero NET-NEW) | OWNER-PROVEN owed = David confirms the leakage figure reads as an estimate/projection, not "we found this in your business" | Vercel deploy |
| 36 | Delivery route round-trip anchor (FIX 4) | `buildMapsUrl` joined only customer stops (no origin/destination). Now `load()` reads `businesses.address` as origin (both scheduled + cart modes); `buildRoute()` injects it as BOTH start and end (round-trip: farm → stops → farm; stops preserved in entered order). **SEAM (AC-4, deferred, NOT built):** `endpointMode` var (round_trip default \| one_way \| custom_end) + stop-order optimization. `[TRACE:ROUTE]` on anchor injection. | `769933f` | **BUILDER-COMPLETE** (verify zero NET-NEW); **address blocker RESOLVED 2026-06-25 (#42)** | OWNER-PROVEN owed = David builds a route on phone → Google Maps URL starts and ends at the farm address with stops between (`[TRACE:ROUTE] anchor injected`). **1-stop bookend now provable on live data; multi-stop owner-proof needs a day with 2+ scheduled stops (only 1 delivery exists live — David snaps more invoices, no seeder).** | Vercel deploy |
| 37 | OCR-into-inventory reuse-verify (recon) | Verdict 🟡 reuse-and-wire (~70% reuse), NOT net-new. OCR provider chain (`api/receipts/ocr.ts`, writes nothing, `shape:'invoice'` extracts line_items+sku/qty/unit_price) + mobile capture/compress = 100% reusable; net-new = small `line_items → business_inventory` mapper + N-row confirm grid + multi-insert (`business_inventory` already reserves `receipt_id`). No migration. Scopes the NEXT build. | `docs/decisions/OCR-into-inventory-reuse-verify.md` | Recon COMPLETE (doc-only) | None — read-only recon; OCR-into-inventory BUILD is the next prompt | none |
| 38 | Canonical status front-page | Elevated `TRACE-SESSION-BOOTSTRAP.md` to the canonical status front-page: ⚡ ACTIVE STATUS (in-flight + demo-critical, one screen, colored, statuses+pointers) + 📋 24-CAPABILITY BOARD (L1–L5, reconciled to today: 8 live/9 partial/8 net-new) + §A DONE/ARCHIVED lifecycle. Standing rule wired into CLAUDE.md §9 (update the affected line every build/query/recon; archive 🟢-proven) + §10 Session Starter ("open ⚡ ACTIVE STATUS first"). Feeder headers added to PLATFORM_STATE / built-inventory / customer-onboarding-capability_v1 / CAPABILITY-PACKAGE-GROUNDTRUTH pointing up. | `__PENDING__` | **BUILDER-COMPLETE** (doc/process) | None — process scaffold; self-maintains via the §9 standing rule | none |
| 39 | Routing-seeder seam recon (read-only) | Verify-before-build recon for the geo-seeder (onboarding routing-capability → 3–4 geocode-VERIFIED nearby stops from the business's OWN address). Findings: **(Q1)** discovery holds identity/catalog/offerings + a coarse `location` "city+state" string ONLY — NO street address; `business_discovery_profiles.raw_extract` holds catalog items+counts, no address. Business street address lives on `businesses.address` (single free-text col, onboarding-captured, often incomplete). **(Q2)** `seedServiceOfferings` = name-based idempotency, `is_active=false`; the re-runnable marker discipline to mirror is populate.ts's prefix/`source`-marker + clear-by-marker — `deliveries` already has `source` + structured `address_line1/city/state/zip`, so a routing seeder writes `deliveries`/`customers` with `source='discovery-seed'`, no schema change to WRITE. **(Q3)** widget hook EXISTS: OnboardingWizard CHOOSE_PATH → DELIVERY path → `DeliveryWizardPath`, where the business address is already in wizard state (`nurseryInfo.address`) and `DEMO_STOPS` (hardcoded Leander/Cedar Park/Round Rock) is the seam to replace. **(Q4)** NO geocoding anywhere + NO Maps/Geocoding key (only GEMINI for OCR) → geocode-verify is entirely net-new (the gating piece; San Marcos mis-geocode = Google-side ambiguity). **(Q5)** gated builtwithcai surface NOT built (v1/future); live entry points = DiscoveryInspect (URL-gated, David-only) + in-signup DiscoveryGlimpse → near-slice rides live Cultivar signup, full-arc needs v1. DELTA SCOPE: smallest build = seed geo-verified stops into `deliveries` at DELIVERY-path selection, marked `source='discovery-seed'`, render in place of `DEMO_STOPS`; net-new = geocoder+key, origin-address completeness, nearby-stop generation. | `docs/decisions/2026-06-25-routing-seeder-seam-recon.md` | Recon COMPLETE (doc-only) | None — read-only recon; routing-seeder BUILD is hard-gated on a geocoder + key (next prompt) | none |
| 40 | Bootstrap edit — Lightning load-menu + cold-start ritual + 2 standing rules | DOCS-ONLY edit to `TRACE-SESSION-BOOTSTRAP.md`: (1) **COLD-START ritual** at top (paste this file + CLAUDE.md Part 3 HANDOFF; drag §7b files for the subject) — corrected the prescribed `docs/handoffs/` reference (no such folder; canonical handoff = CLAUDE.md Part 3, archive = `docs/handoff-archive.md`) per the doc's own VERIFY-BEFORE-BUILD principle; (2) **§7b LIGHTNING LOAD-MENU** = subject→verified-.md-files table (Discovery / OCR / Address-delivery / Cost-margin / Identity-roles-security / Architecture / Working-method) — every path ls-verified at write-time; relocated 2 paths to reality (`lightning-david-partnership.md`→`docs/operating-doctrine/`, `dual-inventory-cultivar-ignition.md`→`data/grower-scan/`); (3) **2 standing WORKFLOW rules** banked: prompt format = ONE copy-paste block; humor = Cleese/Fawlty/Python deadpan, full mode EARNED on a named/proven problem. No code, no schema, no `[TRACE:*]` touched. | `4a595f4` | **BUILDER-COMPLETE** (doc/process; all §7b paths verified present) | None — process scaffold; self-maintains via the "add new subject-area docs here" note | none |
| 41 | Surfacing-thesis doctrine capture (MASTER_BRIEF PART 4 extend) | DOCS-ONLY extension of the Regina Principle / surfacing thesis (PART 4; family of ledger #15). FOUR additions, reconciled into existing voice (split/extend lines 330/336/338, no duplication): (1) **split the warranty principle in two** — *Warranty / courtesy-by-window (PLANTED-by-us)* = warranty FULFILLMENT, an obligation (data: planted-by-us flag TRUE) vs *Proximity-to-buyer addon (PURCHASED-only, self-planted)* = pure ADDON opportunity, engine MUST NOT say "warranty"; the old "plant date ≈ sale date" approximation only holds when LAWNS planted; (2) **"did-we-plant-it" flag** as a claim-governor (decides which principle fires + what may be claimed); (3) **customer-photo-in channel** (customer snaps tree → remote warranty health-check [planted] + AI care advice [any tree] in owner's voice) — flagged as the **5th instance** of the image→AI-extract→confidence-flagged primitive (receipts/inventory/assets/VIN), confidence-honesty applies; (4) **2×2 touch matrix** (plant-status × remote/in-person, 4 cells, one engine). Sharpened the line-338 services-model open question: PLANTING leaves a warranty behind (JOB-like) vs PURCHASE leaves only a relationship → services model must carry service-rendered vs product-only. Captured (not scoped): photo-in needs an inbound intake path (link/text/QR-profile reuse). **CONFLICT FLAGGED for David:** the new Regina parenthetical frames her as self-planted/no-warranty, but the anchor story at line ~318 says "we planted them" — David to reconcile which Regina is canonical. No code, no schema, no `[TRACE:*]` touched. | `ff2cc25` | **BUILDER-COMPLETE** (doc/doctrine capture) | None — doctrine capture; surfaces when the suggestion engine is built. **DECISION OWED:** David reconciles the Regina planted-vs-purchased anchor conflict | none |
| 42 | Delivery-route bookend — address data fix (closes FIX 4 #36 blocker) | **VERIFY-FIRST (read, file:line):** (A) multi-stop assembly ALREADY built — "Route this day" ([DeliverySchedule.tsx:152-166]) → `/deliveries?date=` → DeliveryRoute scheduled mode loads ALL of a day's `deliveries` rows ([DeliveryRoute.tsx:88-124], created_at order, limit 50) and `buildRoute()` ([:166-192]) passes all selected stops; (B) round-trip bookend ALREADY built — origin ← `businesses.address` ([:80-82]), `endpointMode='round_trip'` unshift+push ([:177-191]) with the AC-4 seam (one_way/custom_end/optimization deferred) already in place. **No code delta.** (C) the genuine gap = address completeness: `businesses.address` for Test Lawns (`ed2e5933…`) was the bare fragment `"770 Co Rd 284"` (no city/state/zip → ungeocodable). **DATA FIX applied** (service-key DML, read-back confirmed): → `"770 Co Rd 284, Liberty Hill, TX 78642"` (David-confirmed city/zip). Bookend proven with the exact page logic: live day 2026-06-25 + a simulated 3-stop set both render `business → stops → business` (BOOKEND CHECK OK). NO geocoding / API key / seeder added. `[TRACE:ROUTE]` stays ON. | data write (no source diff) | **BUILDER-COMPLETE** (address written+confirmed; `npm run verify` exit 0 zero net-new; bookend URL proven both directions) | OWNER-PROVEN owed = David opens a delivery day on his phone, taps Route, sees the Maps trip start AND end at the farm. **⚠️ DATA DISCREPANCY (flagged to David):** live DB has **1 delivery / 1 day / 2 receipts**, NOT the prompt's "6 invoices, 2–3 per day" — so the *live multi-stop* owner-proof needs David to snap 2+ invoices onto one future day (seeder barred); the 1-stop bookend is provable now. | Vercel deploy + (for multi-stop) more captured invoices |
| 43 | Opportunity-cost layer — cost-thesis doctrine capture (D-19) | DOCS-ONLY extension of the cost thesis (family of D-8/D-9/D-14/D-16/D-17). Captures the **THREE cost layers** a priced service carries, and names the hidden third. (1) **Direct cost** — what the job consumes (labor+materials+visit); already modeled. (2) **Decomposed price** — same number, shown as parts; legibility/trust, the customer-facing VIEW of layer 1 = D-17 surface 3 ("one source, many views"). (3) **Opportunity cost — cost of ABSENCE** (the novel layer): pulling crew (Juan/Jose) off the farm to plant trees is a real, job-caused cost that appears on no invoice; a $400 install profitable vs $100 direct may be thin/negative once lost farm throughput is counted. Framed as the **anti-Nelson thesis applied to opportunity cost** (OP-5/OP-7/OP-9): owner FEELS the scramble, can't QUANTIFY it, prices as if free; tool computes it. **New primitive (DEFERRED, not built):** resource's *value-at-primary-work* ("a farm crew-day ≈ $X"), carried honestly on the D-9 ESTIMATED ladder. **Resolves** the MASTER_BRIEF PART 4 services-model open question — planting=JOB-like (labor+visit+warranty clock) vs product=PRODUCT; the JOB-like service object is the single shared foundation (AC-4/AC-1) under all three layers + invoice-audit + Regina-chain services. Placed in `docs/DECISIONS.md` as **D-19** (chosen over MASTER_BRIEF: the cost thesis's cross-linked decisions all live there). No code, no schema, no `[TRACE:*]` touched. | `646df3c` | **BUILDER-COMPLETE** (doc/doctrine capture) | None — doctrine capture; surfaces when the services model + cost-to-produce service costing is built. **FLAGGED for David:** add a one-line "→ resolved, see D-19" pointer to the MASTER_BRIEF PART 4 services-model passage on a future edit (not taken here, single-doc scope) | none |
| 44 | Surfacing engine — suggestion-engine doctrine capture (MASTER_BRIEF PART 4 extend) | DOCS-ONLY extension of the Regina Principle / surfacing thesis (PART 4; family of ledger #15/#41). Captures the fullest statement yet of WHAT THE PLATFORM IS FOR + WHY THE MAP MATTERS, reconciled into existing voice (no duplication of the warranty-split/photo-in/touch-matrix already there). SIX additions: (1) **thesis sharpened** — AI's job is to SEE THE PATTERN latent in what the owner already knows (not know MORE); the platform PRESENTS data + observation context, does NOT advise/decide — judgment stays human (anti-Nelson "un-instrumented owner"). (2) **governing posture** — pull-not-push (connect dots when asked) · justify-every-data-ask (paired with the capability it unlocks) · derive-don't-survey (headcount from invite-flow, throughput from one inventory figure ÷ per-unit rate) · flag-the-SPECIFIC-missing-input (the gap is the on-ramp; D-9 ladder applied to suggestions). (3) **the substrate** — the suggestion engine IS cost-to-produce run FORWARD (same engine: backward "what did this cost?" → forward "what would a new service cost + would it pencil?"); this is what D-009's moat is FOR. (4) **capacity gate (responsible-adult rule)** — Path A slack-capacity = near-pure upside, suggest confidently; Path B maxed-out = a HIRE/pulled-labor INVESTMENT decision with ROI + risk (opportunity-cost layer, D-19); if utilization can't be SEEN, say so. (5) **routing IS the capacity readout** — schedule density = utilization; the route does 3 jobs (logistics + opportunity-surface + utilization gauge) → licenses the Path-A suggestion. (6) **Lauren's fertilizer service** — the second anchor (Regina's economic twin): the risk-flip (30% yes ≠ 70% failure — it's a 30% gain at no new cost + word-of-mouth on the 70% no); the platform makes the risk profile VISIBLE, Lauren decides. Plus **why the map IS the demo** (visualizer makes the engine legible at a glance — TELLING vs SHOWING her she's standing in an opportunity). No code, no schema, no `[TRACE:*]` touched. | `9916e58` | **BUILDER-COMPLETE** (doc/doctrine capture) | None — doctrine capture; surfaces when the suggestion engine is built (forward-run cost-to-produce + capacity gate + list→map lens) | none |
| 46 | Continuity spine — ARC MAP + CAPTURE INDEX into the bootstrap + maintenance loop (docs-only) | Built the two MISSING continuity structures into `TRACE-SESSION-BOOTSTRAP.md`, populated from VERIFIED ground truth (file:line, not memory): **🧵 ARC MAP** = the platform as 8 end-to-end FLOWS (front-door / OCR-doc-routing / cost-to-produce / suggestion-surfacing / delivery-routing / discovery / identity-roles-security / asset-inventory-PMI), each with a status-per-piece spine, a one-line ARC-STATUS coherence verdict, HOME DOCs, a LANDMINE marker, and an OFF-COURSE/EXTRA lane (template = the front-door pilot, ledger #45). Six arcs re-verified from code this pass (3 parallel recon agents, file:line); arcs 7-8 stated from ledger/migration file:line and flagged "not re-swept this pass." **📚 CAPTURE INDEX** = single retrieval table (~70 rows) of every D-/OP-/doctrine/concept/recon swept from the ACTUAL docs (DECISIONS.md D-1..D-19 + OP-1..9, MASTER_BRIEF PART 4, DISCOVERY_MODULE_BRIEF, CONCEPT-customer-url, all docs/decisions/*, all docs/DECISION-*, operating-doctrine) — POINTER-only, no content restated. **Maintenance loop** = new step 6 in `docs/operating-doctrine/end-of-session-protocol.md` (did a piece-status change? did anything get captured to a doc? did anything new get discussed and not placed? — update both structures every close; captures without an index row are not done; never populate from memory). Existing bootstrap sections all intact. Flagged 2 conversation-only captures still owed a doc home (async-invite choreography + structured-query architecture). status.html unaffected (pure renderer; no follow-up render change needed — new sections are standard markdown). No code, no schema, no `[TRACE:*]` touched. | `__PENDING__` | **BUILDER-COMPLETE** (doc/process; arc statuses file:line-verified) | None — process scaffold; self-maintains via the step-6 loop. **OWED A HOME (David):** the 2 conversation-only captures (async-invite choreography, structured-query architecture) need a doc when built | none |
| 47 | Front-door arc promotion (sync, AUTH-FREE) — wire reveal-conflict + catalog-seed + 4 bug-fixes | **VERIFY-FIRST corrected the premise (file:line):** Cultivar onboarding is NOT a wall-of-fields — it is ALREADY two progressive flows (`SignUp`→`OwnerSignup` step-machine with `DiscoveryGlimpse` reveal as a vertical step → `OnboardingWizard` 5-step demo chooser); the reveal is ALREADY wired/live at `/signup`. So Steps A+B were already satisfied (NOT rebuilt). The genuine gaps: `compareEnteredVsSite` (compare.ts:192, **0 callers, lacked `address`**) + `populateCatalog` (populate.ts:128, **CLI-only**). **C (conflict):** added `address` to `EnteredBusinessData`/enteredAsStrings/FIELD_LABELS/prompt; folded the compare into `api/discovery/ingest.ts` normal flow (reads businesses entered-data service-key, reuses the already-fetched `content` via injected `_fetchContent` — no 2nd fetch, returns `discrepancies`); `DiscoveryGlimpse` 'done' phase renders hedged conflicts (silent-partner tone) with "Use site value" → owner-RLS `businesses.<col>` UPDATE / "Keep mine". **D (seed):** new `action=populate` branch on the SAME ingest function (12-fn ceiling → no new endpoint; ungated like the sibling `seedServiceOfferings`; degrades gracefully if `business_discovery_profiles` unapplied); `DiscoveryGlimpse` fires it foreground on reveal + shows "Added N items" status. **E bookend:** demo route builder `OnboardingWizard.DeliveryWizardPath.buildRoute` now bookends business→stops→business (live `/deliveries` already did both modes — premise corrected). **E tax:** `businesses.tax_rate` now drives BOTH the CartReview display AND the authoritative `orders/submit.ts` (was 2 hardcoded `0.0825`); Confirmation label derived from amounts. **E naming:** "Delivery Routing"→"Delivery". **E QBO:** new shared `useQboConnect` hook (popup+poll+breaker) — ONE action mounted in BOTH Dashboard AND Settings (rule-of-three); shared Settings now triggers it via a button (was a dead `<a href>` = the broken path). **AUTH BOUNDARY NEVER CROSSED** (no `OwnerSignup.signUp` / businesses-insert order / `acceptInvitation`). Async invite / scrape-while-away / address→Google stay DEFERRED (🔴). | `dc74ebc` | **BUILDER-COMPLETE** (`npm run verify` exit 0, zero NET-NEW; eslint 267→**266** locked; build:cultivar clean 2219 modules; ingest.ts + submit.ts esbuild-bundle OK) | OWNER-PROVEN owed (David, one deploy): fresh business (Dave's Trees + a URL with a conflicting address) → progressive screens → reveal **raises the address conflict** + "Use site value" persists → **catalog seeds** ("Added N items") → dashboard alive → onboarding **demo route bookends** business→stops→business → **QBO connects from Settings** (popup+poll, not a dead link) → tax rate not hardcoded (set a non-8.25% tax in Settings → checkout reflects it) | Vercel deploy |
| 45 | Front-door arc recon (read-only) — builtwithCAI register→invite→scrape-while-away→prepared-reveal | Verify-first recon mapping the front-door arc against docs + code + the identity landmine. **LANDMINE ANSWERED (the June-8 open question):** the full async invite arc **cannot promote clean** — its first move (scrape **before** account) inverts how the code is wired (account-before-scrape), so it FORCES the auth/account-ordering reconciliation (slow, owner-decision). Proof line: discovery's first query `DiscoveryGlimpse.tsx:48 .eq('id', businessId)`, and `businessId` is born only at `OwnerSignup.tsx:282` after `signUp` `:397`. **The synchronous reveal + 3 bug-fixes CAN promote without touching auth.** **MAP:** async invite / scrape-while-away / address→Google validation / address-conflict-surface / catalog-auto-populate-on-create = **ABSENT** (net-new); two-pass engine / `DiscoveryGlimpse` reveal / `compareEnteredVsSite` (built, **zero callers**) / `populateCatalog` (built, **CLI-only**) = **BUILT, partly unwired** (reuse). Docs: async invite/structured-query/address→Google fork = **only implied or absent**; recognition-moment has a **status contradiction** (`CONCEPT…md:108-113` committed vs `THOUGHTS.md:15` "do not build"). Recommended smallest promote: wire `compareEnteredVsSite`+address-field into `DiscoveryGlimpse`, wire `populateCatalog` into the post-create path, add the Google/address branch behind a geocoder key — **none touches `OwnerSignup` signUp / businesses-insert order / `acceptInvitation`**. No code, no schema, no `[TRACE:*]` touched. | `docs/decisions/2026-06-26-front-door-arc-recon.md` | Recon COMPLETE (doc-only) | None — read-only recon. **DECISIONS OWED (David):** (1) sequence sync-reveal-first vs full-async-arc; (2) reconcile recognition-moment contradiction; (3) approve geocoder + key; (4) build a pre-auth prospect token (the auth reconciliation) or accept account-before-scrape | none |
| 48 | Geocoder decision bank (D-20, docs-only) | DOCS-ONLY capture of the verified shape from the 2026-06-26 geocoder-endpoint recon. **Verdict:** enabling a Google geocoder for its three consumers needs **ZERO new serverless functions** — function count stays **12/12**, so the Hobby 12-fn ceiling (tech-debt #41) is NOT a prerequisite and the Pro upgrade is NOT required. Wiring (file:line-grounded): (1) **address-validation @ signup** → new `action='validate-address'` branch on the EXISTING `api/discovery/ingest.ts` multiplexer (same additive pattern as populate/compare/cost-apply/cost-discovery/send; entered address already available in the compare branch), returns `{valid, formatted, lat, lng, confidence}` for the reveal — ZERO new functions; (2) **customer map** → client-side Google Maps JS in the browser (no map page today; DeliveryRoute only builds a string URL) — not a function at all; (3) **geo-seeder** → CLI/batch script under `scripts/` (never deployed) — ZERO new functions. **The real net-new infra = TWO distinct keys, not functions:** a SECRET server-side Geocoding key (`process.env` inside ingest.ts, never bundled, trust class = ANTHROPIC/SERVICE keys) + a referrer-locked BROWSER Maps-JS key (exposed by design like the anon key, MUST be HTTP-referrer-restricted to cultivar-os.vercel.app + API-restricted to Maps JavaScript API); do NOT reuse the secret key in the browser; owner sets a daily quota cap. **TIMING:** stand up the two keys AS PART OF the front-door re-staging build (when the validate-address branch is first used on the no-website path); customer map is independent; current queue (nav fix, datasheet inventory view) is geocoder-free. Banked as `DECISIONS.md` **D-20** + CAPTURE INDEX row in `TRACE-SESSION-BOOTSTRAP.md`. No code, no schema, no keys, no `[TRACE:*]` touched. | `__PENDING__` | **BUILDER-COMPLETE** (doc/decision capture) | None — decision capture; surfaces when the front-door re-staging build stands up the two keys | none |
| 49 | Nav fix — persistent chrome + full IA tree + Delivery sub-pages findable (AUTH-FREE) | Three issues from the 2026-06-26 owner-prove, ONE root cause (nav structure). **VERIFY-FIRST (file:line):** (1) the nav hid under the banner because `AppHeader` (sticky top:0 z-20, [AppHeader.tsx:80-83]) and `.appchrome` (sticky top:0 z-15, [globals.css:156-159]) were two INDEPENDENT sticky siblings both pinned to top:0 → the lower-z nav slid behind the higher-z header. (2) Nav was ALREADY registry-driven (`AppNav` reads `navSections()` from the single `NAV_IA` in [tileRegistry.ts:322]) — no parallel list — BUT `navSections()` returns only the 3 parent-null nodes [Dashboard·Settings·Admin] and `renderSection` collapsed any section WITH a route to one link, so the whole dashboard branch (Delivery/Operating Costs/Social/PMI/Orders + sub-pages) was never surfaced. (3) all routes real (router.tsx:74-108) — no dead links. **BUILD:** (A) **persistent** — AppLayout wraps `<AppHeader>`+`.appchrome` in ONE `.appchrome-stack` (sticky z-20); `.appchrome`→`position:relative` (keeps drawer anchor, no second sticky) → they pin together, page scrolls beneath. (B) **structure** — AppNav rewritten to render the full `NAV_IA` tree: Dashboard home + the dashboard branch's surfaces PROMOTED to top-level peers, each EXPANDED to its sub-pages (Delivery→Route; Operating Costs→Assets/Inventory/Receipts; Social→Campaigns) + Admin + Settings; still reads the ONE registry IA, every node gated via `can(navPermission)`; hamburger now visible at EVERY width (3-level tree = vertical drawer on phone+desktop), old desktop rail dropped. (C) **delivery** — sub-pages in the tree; `nav_delivery_route` label `Route a day`→`Route` (KEY unchanged → cap #n + breadcrumb intact). Help kept out of the structural nav (lives in header account menu). **AUTH/schema/RLS NEVER touched.** `[TRACE:NAV]` stays ON (now emits `[TRACE:NAV] menu`). **DENSITY FOLLOW-UP `8370e56` (CSS-only):** owner found the drawer too tall/airy (ran nearly full page height) → tightened `.appnav-drawer` rows ONLY — per-row vertical padding 10px→6px + line-height 1.5→1.3 (~40px→~30px/row), inter-row gap 2px→1px, ADMIN heading 10px/4px→6px/2px; the 22px sub-page indent vs 12px top-level is PRESERVED (hierarchy still reads). No structure/route/gating/persistence change. **DENSITY-2 + ADMIN-HEADER `0985643` (CSS + group-header note):** live owner-prove found the drawer STILL too loose + "ADMIN reads as a dead link." (1) tightened harder — per-row padding 6px→3px, line-height 1.3→1.2 (~30px→~22px/row), gap 1px→0, drawer frame 6px→4px; 22px sub-page indent still preserved. (2) ADMIN was ALREADY a `<span className=appnav-heading>` (the ONLY route-null node — no onClick/button/link wrapper), but looked tappable → styled as an unmistakable divider (`border-top` + `margin-top` + muted micro-caps + `cursor:default` + `user-select:none`); clicking does nothing-by-design; real children (Add Business, Cost-to-Produce) stay working links. `[TRACE:NAV] menu` now reports `groupHeaders` (nodes rendered as labels, not links). No IA/route/gating/persistence change. | `4c6308a` · density `8370e56` · density-2+admin `0985643` | **BUILDER-COMPLETE** (`npm run verify` exit 0 zero NET-NEW; quality:gate exit 0; build:cultivar clean; cap #n PASS) | **STRUCTURAL ITEMS OWNER-PROVEN** (stays visible / full tree / Delivery › Route reachable). **Remaining owner-proof (one deploy):** David opens the nav → (a) it's tight/compact (dense rows, not page-height), rows still tappable, sub-pages still indented under their parent; (b) ADMIN is plainly a label, not a broken link (clicking it does nothing-by-design). | Vercel deploy |
| 50 | ARC MAP rendered as a 4th board view in `status.html` + lands the #46 step-6 maintenance loop | The 🧵 ARC MAP (ledger #46, written into `TRACE-SESSION-BOOTSTRAP.md`) was invisible on the status board — `status.html` is a pure renderer that only sliced 3 headings. **BUILD (renderer only, no app code/schema/RLS/`[TRACE:*]`):** (A) added a 4th view toggle → four pills **Active · 24-Capability Board · Arcs · Archived** (same `.tab`/`data-view`/`setView` pattern; Archived promoted from a `<details>` appended under Active to its own tab, same card markup, `open` by default). (B) new `parseArcs()` slices `## 🧵 ARC MAP` (stops cleanly at `## 📚 CAPTURE INDEX`) and parses each `### N.` arc — title, `**SPINE:**` (split on ` → ` so `invoice→delivery` inside `fan-out{}` stays intact; a dot per status emoji, in flow order), `**ARC STATUS:**`, `**LANDMINE:**`, `**HOME DOC(S):**`. Renders each arc as a card: flow-order spine chips with green/amber/red/grey dots (✅/🟢→green, 🟡→amber, ❌/🔴→red, ⚪→grey), the coherence line, a colored LANDMINE strip (🚨→red `lm-crit`, other→amber `lm-warn`, none→grey) so the front-door **AUTH** + identity **RLS** landmines read at a glance, and the home-doc pointer. **RENDERER, not hardcoded** — parses from the `.md`; re-slices correctly if the heading text is unchanged. (C) CAPTURE INDEX deliberately NOT given a view (owner asked for 4 views; it stays in the `.md`). **PROVEN (node-extracted shipped fns vs live `.md`):** 8/8 arcs parse with correct per-piece dots + arc colors + landmine classes (front-door amber/lm-crit, cost+delivery+identity green, suggestion grey/lm-none); the 3 existing slices unchanged (Active 3 bands/12 items · Board 6 layers/25 rows · Archived 1); `arcCardHTML` emits valid markup, no throw. | `c7d1cd9` | **BUILDER-COMPLETE** (parsers proven vs live bootstrap; renderer-only) | OWNER-PROVEN owed (David): open `status.html` → sees four working tabs **Active / 24 / Arcs / Archived**; the **Arcs** view shows all 8 arcs with the spine-level dot statuses + the AUTH landmine flag; the other three views unchanged | none |
| 52 | Design-law capture — D-21 (direct access over scroll) + D-22 (Admin vs Settings axis) (docs-only) | DOCS-ONLY capture of the two durable principles ledger #51 flagged for a DECISIONS home. **D-21 (DESIGN LAW, platform-wide):** *screen real estate is sacred; direct access over scroll* — density is the default not the polish; 4 concrete rules (DIRECT ACCESS = menu is the table of contents, land on JUST that section, parents land on a short index not a data wall · COLLAPSIBLE SECTIONS = visible user-operable collapse, no forced accordion, opening one never closes another · DENSE LISTS = datasheet/grid for 100+ items · COMPACT CHROME). Captured as the **canonical home for "direct access over scroll" (principle (ii))** — the SUPERSET/platform-wide promotion. Enforcement = DOCTRINE (cited in build prompts + verified at owner-prove), NOT an automated gate (mechanizing "too-scrolly" is hard + lossy; recorded divergence-below-standard under §6 rule 10); gate-teeth deferred. Origin = the LAWNS-era scroll problems (3-page reveal · long Settings · 111-item inventory list · loose nav drawer) as ONE re-invented principle. Retro-applications OWED (tracked, not built): onboarding/discovery reveal cards → collapsible (front-door re-staging) · Settings genuine user-operable collapse + landing (fuller split still owed per #51) · any other long-scroll surface. **D-22 (gating axis, principle (i)):** *Admin = business-entity config (gates manage_settings / owner-only); Settings = user-self (gates view_dashboard, every user incl. Staff)* — split on whose thing it configures; sibling to D-21 (D-22 = what-goes-where, D-21 = how-it's-presented). Both written to `docs/DECISIONS.md` (D-21, D-22, `[CAPTURED]`); CAPTURE INDEX rows added to `TRACE-SESSION-BOOTSTRAP.md` (arc: cross-cutting / UX). No code, no schema, no `[TRACE:*]` touched. | `cd6323d` | **BUILDER-COMPLETE** (doc/decision capture) | None — decision capture; surfaces as cited doctrine in every UX build + checked at owner-prove. **RETRO-APPLICATIONS OWED (tracked in D-21):** reveal-cards collapsible, Settings genuine collapse + fuller direct-destination split | none |
| 51 | Separate Admin (business) from Settings (user) + direct-access menu sections (AUTH-FREE) | Two owner rules. **RULE 1** (business-entity config = Admin; user-self = Settings): **VERIFY-FIRST (file:line):** NAV_IA had Roles+Your Profile under SETTINGS and only Add Business+Cost-to-Produce under ADMIN ([tileRegistry.ts:352-362]); the long `/settings` page composed Business Profile + Accounting + Services in shared [pages/Settings.tsx] + cultivar verticalSection (CostToProduceSettings/Nursery/Team). Perm keys (cultivar `auth/roles.ts`): `manage_settings` = owner-default (NOT a MANAGER default → delegable via /roles), `owner-only` = owner-exclusive, `view_dashboard` = every user; `can()` ([BusinessProvider.tsx:492]) returns true for owner, else checks the member's list. **BUILD:** regrouped NAV_IA → **ADMIN** = Add Business (owner-only), Business Profile (manage_settings), Accounting (manage_settings), Roles & Permissions (manage_settings, MOVED from settings), Cost-to-Produce (owner-only); **SETTINGS** = Your Profile (view_dashboard). `sec_settings`→header (route null, view_dashboard) so Your Profile shows for every user; Staff holds neither manage_settings nor owner-only → sees NO Admin item. **RULE 2a** (direct access over scroll): Business Profile + Accounting were buried in the one long page → now each is a direct menu destination (`/settings/business`, `/settings/accounting`) landing on JUST that section. Shared Settings gained an optional `section` filter prop (undefined=full page, UNCHANGED for every caller incl. Ignition); router adds `/settings/:section`; cultivar Settings routes the param + isolates verticalSection; `[TRACE:NAV] settings section` on resolution. **RULE 2b** (never force-collapse): shared `SectionCard` is now independently collapsible (own state, default-open) — opening one never closes another, multiple open, expand-all allowed; no accordion. **Leftovers** (Services, cost config, install price, Team) stay on the full `/settings` page (UNCHANGED content/saves), reached via a "More business settings →" link from the isolated views — nothing orphaned. **NO auth/oauth/schema/RLS, no page functionality/save change.** **TWO DURABLE PRINCIPLES flagged for a DECISIONS capture (Lightning may bank):** (i) *business-entity config = Admin; user-self = Settings* — the gating axis; (ii) *direct access over scroll — the menu is the table of contents; land on one section, never force-collapse where blocks coexist.* | `2ee2853` | **BUILDER-COMPLETE** (`npm run verify` exit 0 zero NET-NEW; build:cultivar clean; cap #n PASS) | OWNER-PROVEN owed (David, one deploy): picks Business Profile (or Accounting) from the menu → lands on JUST that section, no long scroll; Admin holds the 5 business items + Staff sees none of them; Settings holds only Your Profile (every user); on the full page no card force-collapses another. **FULLER SPLIT OWED (reported):** Services / Team / cost config / install price as their own direct destinations (currently reachable via "More business settings →"); base `/settings` has no breadcrumb (no IA node routes there). | Vercel deploy |
| 53 | Nav cleanup — single-render breadcrumb + Admin/Settings landing indexes + QBO consolidation + PMI colors + role-mismatch investigation (under D-21/D-22) | Six findings from the #51 owner-prove. **Item 1 (breadcrumb):** VERIFY-FIRST found the ROOT CAUSE — `Breadcrumb.tsx` rendered TWO DOM spans (`.breadcrumb-full` + `.breadcrumb-mobile`) and an inline `display:'flex'` on the full span DEFEATED the responsive `.breadcrumb-full{display:none}`, so on phones the collapsed "‹ parent" span rendered ALONGSIDE the full trail → the garbled "Dashboard / Operating Costs / Receipts ‹ Operating Costs", "Cost-to-Produce" twice, "…/ Dashboard" strings (all = full+mobile concatenated); "neither clickable" because Cost-to-Produce's only ancestor was the route-null Admin heading. **FIX at the render level:** ONE trail (dropped the dual-span mechanism), ancestors clickable `<button>`, current plain, narrow screens scroll horizontally (globals.css) — the whole double-render class is gone. **Item 2 (Admin landing):** new `/admin` `AdminIndex` (gated manage_settings); `sec_admin` is now a clickable nav node (route `/admin`, perm manage_settings → Staff never sees it). Cards ARE `navChildrenOf('sec_admin')` (one source), each gated by its own `can(navPermission)`. Also makes Cost-to-Produce's Admin ancestor clickable (fixes Item 1's "neither clickable"). **Item 3 (Settings index):** `/settings` now lands on a short `SettingsIndex` (Your Profile + manage_settings → Business administration / All-business-settings), NOT the wall; full page moved to `/settings/all` (existing `/settings/:section` route, sectionParam 'all'→full); `nav_all_settings` IA node added for its breadcrumb ("Settings / All settings"), excluded from the drawer (NAV_EXCLUDE, like nav_help). Repointed stale `/settings` links (Dashboard "Complete →"→`/settings/business`; CostToProduce ×3→`/settings/all`; onMoreSettings→`/settings/all`). **RESOLVES #51's "base /settings has no breadcrumb" + part of its "fuller split".** **Item 3.5 (collapse):** shared `SectionCard` collapse control is now a VISIBLE Show/Hide pill (was a faint 0.7rem gray ▾ that read as decorative); free expand/collapse unchanged. **Item 4 (QBO):** confirmed all 3 surfaces already share `useQboConnect`; added `accountingHasOwnDestination` prop → cultivar passes it so the redundant full-page Accounting card is dropped (connect stays at Dashboard + `/settings/accounting`, one hook). Ignition unaffected (doesn't use the shared page; prop default false). **Item 5 (PMI):** recolored `shared/modules/PMI.tsx` from Ignition's dark theme to Cultivar's light forest-green (only Cultivar renders it; 0 dark hexes remain). **Item 6 (role mismatch — INVESTIGATED ONLY, no change):** header/Settings "OWNER" = owner-by-`businesses.owner_id` (`BusinessProvider` owner path, `role:null`, structural & authoritative); `/roles` reads `business_members.role` into an assignable `<select>` whose options are the role_definitions floor — the owner has no matching/assigned value there → "no role". OWNER is owner-by-being-owner, NOT owner-by-assignment; the owner should never be a reassignable dropdown. **Recommended display-only fix (deferred for David's ratify):** in RoleConfig's member list, detect the owner (`member.user_id === business.owner_id`) and render a LOCKED "OWNER" badge instead of the select. NO auth/RLS/schema/role-data touched. | `e4fc913` | **BUILDER-COMPLETE** (`npm run verify` zero NET-NEW: tsc 10 / eslint 266 / knip unchanged; verify-universals all green incl. cap #n; build:cultivar clean 2222 modules) | OWNER-PROVEN owed (David, one deploy): (1) breadcrumb correct + ancestors clickable on Cost-to-Produce/Operating Costs/Inventory/Assets/Receipts — no dupes, no stray `‹` segment; (2) Admin → `/admin` index, gated (Staff can't reach); (3) Settings → short index, not the wall; (3.5) Settings cards have a visible operable collapse, free expand/collapse; (4) QBO shows as one connect action not three cards; (5) PMI is Cultivar-colored; (6) role-mismatch fix is David's call (not built) | Vercel deploy |
| 54 | Walk-and-count inventory loop (scan → qty → save → next → complete) — RECONCILE DEFERRED | The cap-2.3 count loop. **VERIFY-FIRST (file:line, home `docs/decisions/walk-and-count-inventory-verify-first.md`):** GATE 1 — scan-to-RESOLVE did NOT exist; only `qrcode` generation (`qr/generate.ts` encodes `${baseUrl}/plant/${id}`) + OS-camera scan-to-PROFILE-via-URL (`/plant/:tagId`→`PlantProfile`, `usePlant` resolves `cultivar_plants ilike tag_id` + `business_inventory` FK join). NO in-app decoder anywhere (no jsqr/zxing/BarcodeDetector). GATE 2 — `business_inventory.qty int` IS the on-hand (directly updatable via existing owner/member RLS); NO count-event/session/on_hand/adjustment notion exists anywhere → durable record needs a new table. **BUILD:** (A) new `components/inventory/QrScanner.tsx` — live `getUserMedia`+`jsQR` decode loop (standard-by-value §6 r10: BarcodeDetector unsupported on iOS Safari = our target → jsqr), `extractTag()` strips the `/plant/<tag>` URL keeping the tag, never navigates; always-present manual tag-entry fallback so the loop never dead-ends. (B) new `pages/InventoryCount.tsx` (`/inventory/count`, under the existing view_costs gate) — state machine idle→scanning→reviewing/unknown→done; resolve = `cultivar_plants.tag_id` then `business_inventory.sku` fallback then UNKNOWN; Save→Next SETS `business_inventory.qty` for the resolved lot AND records the count; UNKNOWN branch = quick variety/size entry OR skip-&-flag; Complete ends the session. (C) "Start count" button on `/inventory`. (D) `[TRACE:COUNT]` on session-start/scan-decode/resolve/save/unknown/complete. **MIGRATION (GATED — handed to David, NOT applied):** `supabase/migrations/20260626_inventory_count_sessions.sql` — `inventory_count_sessions` + `inventory_counts` (the durable record reconciliation will read: counted_qty, item_label, inventory_id, plant_tag_id, was_unknown, session, timestamp); RLS owner_all + `is_active_member` member_all (AC-2/AC-3); CASCADE on session/business, SET NULL on inventory_id. Deploy-window safe: if tables absent, on-hand still updates, count-record skipped with a loud `[TRACE:COUNT]` warning (mirrors deliveries `service_type` fallback). **RECONCILIATION NOT BUILT** (counted-vs-expected / sold-dead-missing) — record model leaves room for it. NO auth/RLS-on-existing-tables/schema-on-existing-tables touched. | `ea2ea14` | **BUILDER-COMPLETE** (`npm run verify` exit 0 zero NET-NEW: tsc 10 / eslint 266 / knip unchanged; verify-universals all green; build:cultivar clean 2226 modules) | OWNER-PROVEN owed (David, on a phone, AFTER applying the migration as postgres + catalog-verify (A)–(E)): Start count → scan a plant QR (URL stripped, item shown, e.g. "Shoal Creek Vitex, 30 gal") → enter qty → Save→Next reopens camera → an unknown scan handled gracefully → Complete saves the session; `business_inventory.qty` updated + an `inventory_counts` row recorded per item; `[TRACE:COUNT]` trail visible | Migration apply (David, as postgres) + Vercel deploy |
| 55 | DataBridge → shared promotion recon (sometimes-connected capture) — READ-ONLY | Verify-first recon for D-h (sometimes-connected capture: capture offline unconditionally, reconcile on sync). **HEADLINE:** the local-first *persistence* half of `packages/ignition-os/DataBridge.js` (1123 ln) is real/solid/cleanly promotable; the *sync-on-reconnect* half **does not actually exist** → NOT a lift-and-shift, a **moderate build** (extract persistence slice + BUILD the reconcile loop). **(1) What it is:** `save`/`load`/`hydrate` (`:363/:417/:450`) over `memoryStore` + localStorage(web)/AsyncStorage(mobile), ONE blob key `IGNITION_OS_DATA` (`:38`); NO IndexedDB. **Reconnect detection = NONE** (grep: zero `online`-event listeners / `flushQueue`/`processQueue`/`drainQueue`; only `navigator.onLine` is a one-shot in stub `smartSync:338`). **Offline queue = WRITE-ONLY** — `queueAction:352` enqueues + persists `IGNITION_SYNC_QUEUE:356`, but **nothing ever reads it back / replays it** (`IGNITION_SYNC_QUEUE` = 2 grep hits, both writes). **(2) Promotable vs coupled:** promotable = the store interface + queue *shape* + write-through-then-fallback template; coupled = `storageKey`/`getShopId`/`db.*` (all `shop_id` + Ignition tables `jobs/shops/tools/pmi_schedules` `:266-331`) + FastAPI/Railway fallback + all business methods `:475-1123`. **LEAK CONFIRMED (Tech Debt #2):** `shared/quickbooks/oauth.ts:18 STORAGE_KEY='IGNITION_OS_DATA'` — in-scope to note, OUT (Off-Limits). **(3) Count-loop seam:** 5 direct `supabase.from()` writes in `InventoryCount.tsx` (`:77,:176,:240,:249,:259`); on-hand SET aborts the Save on `upErr:181` = the dead-zone failure. Route through a shared wrapper = mostly thin (count inserts → UUID = idempotent replay; on-hand SET → carries clientTs, last-writer-wins is a real conflict surface, surfaced not hidden); NO existing-table schema change. **(4) "Both":** today does NEITHER fully — `save` is local-first not write-through (cloud push only for hardcoded `active_jobs:402`); queue-and-sync is a queue with no consumer (`pushCloudSync` failure logs "queued for retry" but enqueues nothing `:258`). **(5) I&A intertwine (flag only):** DataBridge DOES carry identity/role/session (`current_user`+pin+permissions `:59`, `authenticate:775`, `getShopId`, `autoEnrollDevice:733`) — separate spec, do not solve. **(6) Landmines:** DON'T move the file (44 Ignition imports) → extract a NEW `shared/src/sync/` module, leave DataBridge as donor-reference (zero Ignition breakage); outside ignition-os nothing live depends on it (Cultivar imports it nowhere; 3 shared mentions are comments; `SavingsReport.jsx:14` is a DANGLING import to a nonexistent `../DataBridge`, Tech Debt #10); `useDataBridge.js` is a red herring (useState shell, no sync). **MIN SLICE for the count loop:** namespaced local store + typed offline-op queue (`{op,table,payload,clientId,clientTs,businessId}`) + write-through-or-enqueue + reconnect drain (`online` listener), route the 5 write sites through it. **WAIT:** full multi-vertical bus, I&A sync, the oauth leak, the dead FastAPI fallback. No code/schema/migration/`[TRACE:*]` touched. | `docs/decisions/2026-06-26-databridge-promotion-recon.md` | Recon COMPLETE (doc-only) | None — read-only recon. **DECISION OWED (David):** confirm scope — minimum-slice (count-loop offline half: new `shared/src/sync/` + route the 5 InventoryCount writes) vs fuller (multi-vertical bus + I&A). Build nothing until confirmed. | none |
| 56 | Story Board system — `user_stories.md` + `stories.html` (pure renderer) | The second lens beside the Status Board. **status.html = what's BUILT (capabilities, green/amber/red); stories.html = what we're BUILDING TOWARD (day-in-the-life need)** — linked by `MAPS-TO` capability id + arc. **FILE 1 `user_stories.md`** (living source of truth, repo root beside status.html): format header explaining the tags, all **8 `## ARC:` sections** (front-door · ocr-doc-routing · cost-to-produce · suggestion · delivery · discovery · identity-roles-sec · asset-inventory-pmi) + `## ARCHIVED`. Each story = `### title` + `STATUS:`/`ARC:`/`MAPS-TO:`/`PIECES:` tags + markdown narrative. **Seed** = the inventory + OCR stories from David's working draft, using ONLY the provided metadata (status/arc/maps-to/pieces) with narratives grounded in handoff facts (ledger #54, board 2.3) and flagged `_(David to expand)_` — no invented scenarios: *Count the lot without paper* [in-build · 2.3 · inventory_count, inventory_count_offline], *Receive the truck* [active · 2.3 · inventory_truck/invoice/receipt], *Reconcile counted vs expected* [active · 4.2 · inventory_reconcile], *Snap a document, let TRACE route it* [active · ocr-doc-routing · GAP]. **FILE 2 `stories.html`** (PURE RENDERER, never edited — mirrors status.html: file-picker + drag-drop + auto-fetch + view tabs + empty state + matching styling; tiny inline block-markdown renderer so it works on file:// with no CDN): parses the tag format, groups by canonical 8-arc order, renders each story as a card with a STATUS pill (in-build amber / active blue / archived muted), prominent `→ <capability id>` MAPS-TO badge (red `→ no capability yet` when GAP), monospace PIECE chips, and the narrative; **Active** (active+in-build) · **All** · **Archived** view toggle (default Active); tally computed from parsed data (in-build / active / gap counts), never hardcoded. One-source discipline: stories added to the `.md`, page re-opened — zero HTML edits. **PROVEN:** parser node-extracted vs the live `.md` → 4 stories parse with correct arc/status/maps/pieces, format-example inside the blockquote correctly does NOT leak as a story; `npm run verify` exit 0 zero NET-NEW (tsc 10 / eslint 266 / knip unchanged — standalone root files don't trip the gate, like status.html). No app code, no schema, no migration, no `[TRACE:*]`. | `29487f1` | **BUILDER-COMPLETE** (parser proven vs live `.md`; renderer mirrors status.html; verify zero NET-NEW) | OWNER-PROVEN owed (David): open `stories.html` → pick `user_stories.md` → see the inventory stories grouped by arc with their `→ 2.3`/`→ 4.2` capability badges + piece chips → toggle Active/All/Archived → add a story to the `.md` and re-open to confirm it appears with no HTML edit | none |
| 57 | Sometimes-connected sync + offline walk-and-count + identity-stamp + double-count surfacing | NEW shared `packages/shared/src/sync/` (the minimum slice from recon #55 — NOT a move of `DataBridge.js`, which keeps 44 Ignition imports): **`store.ts`** namespaced local store (`trace:sync:<businessId>:<domain>`, de-keyed from the single `IGNITION_OS_DATA` blob; localStorage → in-memory fallback), **`offlineQueue.ts`** typed FIFO op queue persisted + idempotent-enqueue by `clientId`, **`syncEngine.ts`** write-through-WHEN-online OR enqueue-WHEN-offline + reconnect drain (`online` listener + manual `syncNow()`; FIFO replay; idempotent — insert PK = `clientId` so a 23505 on replay = already-applied, **no existing-table schema change**), `types.ts`/`index.ts`. **Wired:** all 5 `InventoryCount.tsx` writes route through it — the dead-zone abort (`:181`) is gone (offline → `queued`, never fails; online → `applied` immediately). **Identity-stamp:** every op envelope carries `userId` (WHO) + `clientTs` (WHEN); count START guarded to authenticated + online (offline-cold-start blocked, reported); DB who via `inventory_count_sessions.counted_by` (session-level; per-row `inventory_counts.counted_by` would need an ALTER — flagged, NOT applied). **Double-count:** a same-lot-twice-in-session Save SURFACES a conflict sheet (first vs now, "sale / miscount / moved" framing, optional reason) → Keep first / Keep new — never silent overwrite; tracked in client state (single-device session, no query/schema change). I&A heavy-sync NOT built (identity-stamp only). | `73d49a0` | **BUILDER-COMPLETE** (`npm run verify` exit 0 zero NET-NEW — tsc 10 / eslint 266 / knip 10·14·15 unchanged; build:cultivar clean 2230 modules; DataBridge.js untouched, Ignition unbroken) | OWNER-PROVEN owed (David, on a phone): start a count (authenticated + signal) → count a lot → airplane-mode → count more (held, no failure) → back online (queue drains, DB + desktop current) → re-count a lot already done this session → conflict surfaces and resolves → each record shows who+when. `[TRACE:SYNC]`+`[TRACE:COUNT]` stay ON until owner-proven. | none |
| 59 | Grower resolve design — format-agnostic scan→catalog (FALSE-UNKNOWN root cause) | READ-ONLY churn of the full `data/grower-scan/` corpus (28 catalogs, **1,382 rows**, 8 with real catalogs). **Root cause of the LAWNS FALSE-UNKNOWN:** the resolver joins on `tag_id`/`sku` only, but discovery writes synthetic `DISC-####` skus + no `cultivar_plants` row, so a QR product-slug shares **zero chars** with the catalog except the name's words — which are never compared. **Identifier distribution (the decider): per-item URL/slug = 12.4% (171 rows, 3 growers); name-only = 87.6%; real POS SKU = 0%.** So slug-as-key is impossible as the base. **Canonical key = normalized NAME token-set (common ∪ botanical)** — present ~100%, **empirically resolves ~97%** of slug-bearing rows (93=96% / ATX=100% / Calloway's=95%, 167/172) with **~0 false collisions** across 1,258 rows (token-set EQUALITY alone fixes LAWNS' word-order flip). **Layered resolver:** tag_id → sku → stored-slug-exact → token-set equality → guarded subset (>1 candidate ⇒ NEED_CLARIFICATION, never silent) → stemmed → UNKNOWN. **Discovery-capture change (recommend, not built):** `catalog.ts` already harvests per-product hrefs (`:79`) then discards them → carry `sourceSlug` onto `CatalogItem` + store a `source_slug` matchable column in `populate.ts` (today buried in free-text `notes`); gated migration ⇒ schema gate applies, flagged not taken. **Landmines evidenced:** ®/™ + quotes + parens + `&`→`amp` entity leak, cultivar collisions (7× Cercis Redbuds — keep ALL tokens, never genus+common), source typos, plural/possessive, name≠slug, 87.6% no-identifier growers. Residual TRUE-unknown ≈3% + genuinely-new stock = exactly the promote-to-inventory population. Extends the strip-URL/tenant-scoped QR doctrine into "normalize every grower's scheme onto one catalog key." **Recommend: build nothing until David confirms the canonical key; LAWNS fix = token-set equality (L4) alone, no schema.** | `docs/decisions/2026-06-26-grower-resolve-design.md` | Recon COMPLETE (doc-only; matcher empirically tested vs the live corpus, not asserted) | None — read-only recon; David confirms the canonical key, then build splits into (a) resolver L3–L6 in `InventoryCount.tsx` (no schema) + (b) `source_slug` capture column in `catalog.ts`/`populate.ts` (gated migration) | none |
| 58 | Story-coverage gap recon (capabilities with NO story) | READ-ONLY diff of `user_stories.md` (commit `29487f1`) ↔ the 24-CAPABILITY BOARD. **Coverage = 2/24** (only `2.3` walk-and-count + `4.2` reconciliation have a story); **6 of 8 arcs have ZERO stories** incl. owner-proven cost-to-produce, the north-star suggestion arc, demo-critical front-door, live delivery, discovery, security. **(A)** covered: 2.3, 4.2. **(B)** GAP = 23 caps, bucketed: ~16 LIKELY-NEED-A-STORY (highest value = the empty high-strategy arcs: 3.2/3.1/3.3 suggestion, 1.1/1.2/1.4 front-door, 1.3 discovery, 3.5 delivery, 2.2 netting/Regina anchor, 5.2 PMI) + 3 future-vertical aspirational (5.3/5.4/5.5) + 1 LIKELY-NO-STORY infra (0.1 vertical-as-pointer) + 2 UNCLEAR (1.5 handshake, 5.1 inventory). **(C)** NO dangling MAPS-TO. **(D)** 1 story maps to `—` (Snap-a-document; OCR type-infer+fan-out is the unbuilt cap). 2 structural flags: no arc home for checkout/sale (2.1/2.2/5.6 → needs a 9th arc or assignment); the Story Board is currently blind to the most-built/most-strategic parts. **Recommendation: build nothing — narrate the high-value gaps, bank 0.1 as infra-no-story, decide the rest.** | `docs/decisions/2026-06-26-story-coverage-gap.md` | Recon COMPLETE (doc-only) | None — read-only recon; the worklist is David's (narrate B1 stories / bank 0.1 / decide B3 + the 2 structural flags) | none |
| 63 | Customer-zero rhythm logger (instrument) | A data-gathering INSTRUMENT (not a product feature), modeled on the proven field-debug capture (#60). NEW shared `packages/shared/src/rhythm/` — bounded localStorage ring (5000 entries, reload/close-survive via pagehide+visibilitychange flush) capturing per-entry LOCATION (lat/lng/accuracy), NARRATION (voice/text), SHAPE (buy\|task\|inventory-check\|project) + free-text thread; `haversineMeters` move-detect; `.json`/`.txt` export via OS share sheet/download. NEW `RhythmLogger.tsx` — `?rhythm=1`-gated (sticky) bottom-LEFT 🟢 panel (start/stop, mic/text note, shape chips, thread input, live counts, Share/JSON/TXT/Clear), mounted beside DebugPanel. `watchPosition` logs a point on >10m move + 60s heartbeat while still; voice via feature-detected Web Speech (text always fallback). **HONEST CONSTRAINT (code+UI):** FOREGROUND-ONLY — web can't track a locked phone (iOS kills it); background = future native. Data LOCAL until David exports (no auto-upload); David-only, not tenant-wired, not in nav. New `[TRACE:RHYTHM]` ON by birth. NO schema/migration/RLS/auth. | `8839502` | **BUILDER-COMPLETE** + **PARTIAL owner-proof** (2026-06-29: a clean ~40-min attended drive-home segment captured — location + heartbeat + narration legible → PROVES rhythm is legible WHEN captured) | OWNER-PROVEN (full) owed: a full day's run. **FINDING (recorded this session):** the web logger is **ATTENDED-ONLY** (foreground/screen-on) — proven empirically; the larger ~2-hour loss that day was NOT screen-lock (auto-lock disabled) but the app **logging David out mid-trip on a network hiccup** (the offline-session bug, `BusinessProvider.tsx:237` `getUser()`) → passive all-day capture needs the NATIVE app. `[TRACE:RHYTHM]` stays ON | none — but elevates the offline-session bug (see GENUINELY OPEN) |
| 62 | Discovery size-variant capture (LOOK + RECOMMEND) | **Why the catalog scraper drops WooCommerce size variants, and how to capture them — recommend, structural, STOPPED for David's data-model pick.** **LOOK (file:line):** scraper `catalog.ts`→adapter `website.ts`→writer `populate.ts`; crawl is listing-FIRST (`catalog.ts:173`) so it reads category-card text where the size selector doesn't exist; `stripHtml` (`website.ts:44`) destroys the variation attribute and the AI schema `{variety,botanical,category,confidence}` (`catalog.ts:225`) has NO size slot + is told never to report a quantity (`catalog.ts:208`); `business_inventory` has NO size/variant/parent column (`20260612_business_assets_inventory_pmi_service.sql`); `populate.ts:78` writes parent-only `DISC-####`. **Confirmed live** (curl, browser UA) on lawnstrees.com Shoal Creek Vitex: sizes 5/15/30/45 Gallon present deterministically in `data-product_variations` JSON + `<select name="attribute_pa_tree-size">` options. **CAPTURE RECOMMEND (#4):** deterministic `extractSizeVariants(rawHtml)` parse (NOT AI) of the variation-form — forces the adapter to retain raw product-page HTML + the crawl to descend to `/product/` pages + a new parser. **DATA-MODEL CALL (#5): Option B** (one row per plant-per-size — A can't hold per-size counts, the whole point); B-clean = `ADD COLUMN size text` (no-CHECK, per-grower) + grouping key (`variant_group text` lean / `parent_id`) = **gated migration FLAGGED, not written, not applied**; B-minimal = name/sku encoding (no-migration, fragile). **#6:** per-grower size list honored (`SELECT DISTINCT size`, derived from their own variations, no global enum, AC-1/AC-3). | **BUILT `9f1063e`** (decision `27c35bd` / `docs/decisions/2026-06-27-discovery-size-variants.md`) | **BUILDER-COMPLETE** — 4 pieces: gated migration `20260628_inventory_size_variants.sql` (size+variant_group, FLAGGED, David applies), deterministic `extractSizeVariants`/`fetchProductVariants` (no AI), `populate` writes one row per (variety×size) with variant_group=slug, `[TRACE:POPULATE]` on; `catalog-variants.test.ts` 31/0 (yields 5/15/30/45), catalog 35/0, `npm run verify` zero NET-NEW, ingest bundles clean | **OWNER-PROVEN owed (after David applies the migration as postgres):** re-run populate for LAWNS → Shoal Creek Vitex shows as 4 size rows (5/15/30/45 gal), each countable; `SELECT DISTINCT size WHERE variant_group='shoal-creek-vitex'` returns the 4 sizes. **THEN the count-side size-picker** = REQUIRED FOLLOW-ON (flagged, NOT built): per-size rows make a Vitex scan return >1 same-name candidate → L4 `InventoryCount.tsx:263` AMBIGUOUS→UNKNOWN (the L5 NEED_CLARIFICATION seam); land the picker before populating per-size rows live | Migration apply (David) + count-side picker is a separate build |
| 61 | Grower-resolve L4 — name token-set EQUALITY (fixes LAWNS FALSE-UNKNOWN) | The build of recon #59's EQUALITY-FIRST cut. **Root cause confirmed (verify-first, file:line):** `InventoryCount.tsx` `handleScan` resolved L1 `cultivar_plants.tag_id` → L2 `business_inventory.sku` → UNKNOWN; discovery's `populate.ts:87-98` writes the proper name + synthetic `DISC-####` sku and **no `cultivar_plants` row** (`:30-33` comment), so a QR product-slug (`vitex-shoal-creek`) shared zero chars with the catalog except the name's words — never compared. **BUILT:** (1) NEW shared canonical-key normalizer `packages/shared/src/utils/canonicalName.ts` — `nameTokenSet` (lowercase → `&`/`&amp;`→space + drop stray `amp` → non-alnum boundary → drop 1-char + botanical connectors var/ssp/subsp/cv → Set), `canonicalNameKey` (sorted stable string), `tokenSetsEqual` (order-insensitive); exported from the shared barrel so voice/typed/QR resolve through ONE key (NOT inlined). (2) wired as **L4** in `handleScan` (after L2, before UNKNOWN): scan-slug tokens == each candidate's name tokens, EQUALITY; **1 match → resolve; >1 equal-token candidate → UNKNOWN, never auto-pick** (the NEED_CLARIFICATION seam); scannedKey empty → skip. L3 stored-slug DEFERRED (no `source_slug` column); L5 guarded subset/superset + L6 stemmed DEFERRED with the seam left at L4. `[TRACE:RESOLVE]` emits the layer that hit. **EQUALITY-ONLY** — zero false-match risk, no Cercis/Redbud collision exposure. NO schema, NO migration, NO auth/RLS. 21/21 `canonicalName.test.ts` (esbuild→node, incl. LAWNS word-order, ®/quotes/parens/`&`-amp normalization, cultivar-discriminator collision-safety, subset-is-not-equality, empty guards). | `6e75b66` | **OWNER-PROVEN** (2026-06-29, David on iPhone) | None — CLOSED. Proof: scanned the real Shoal Creek Vitex tag → L4 token-set equality resolved `vitex-shoal-creek` ↔ "Shoal Creek Vitex" → `DISC-1105` → the count wrote **45** against the resolved inventory row; the demo-blocking FALSE-UNKNOWN is dead (field-debug capture #60 exported the trail that proved it). Guarded-fuzzy L5/L6 + NEED_CLARIFICATION picker remains the fast-follow (not a close blocker) | none |
| 64 | 27–29 June master bank — north star + doctrine + domain KB + 2 stories + proofs/findings (DOCS-ONLY) | Banked the entire 27–29 June strategy+build session from the Lightning chat into the repo, wired to every standing surface. **NEW `NORTH-STAR.md`** (top-of-hierarchy, above PLATFORM_STRATEGY + MASTER_BRIEF): the spotlight-brain person · catch-the-threads core job (Belgium story + the verbatim 2026-06-28 forgotten-card note) · the four shapes (buy/task/inventory-check/project) · reasoning-in-the-gap · the **TIMING LAYER** (the one net-new unbuilt piece) · the two-tier trust architecture (web now / local-LLM premium tier) with Exhibit-A evidence (Apple Significant-Locations wall, Google Takeout scaffolding, Tuya cloud-upload — honesty-guarded: scope/reachability, NOT "Google tracked David") · the hearing-aid hardware path · build-the-brick-that-pays discipline · standing task (query the local-LLM frontier) · the test for every build. **DOCTRINE → DECISIONS.md:** **OP-10** Structure-Last (machine pays the structure tax; + small-grower expression, Barryhill-confirmed ~88% no-structure) · **D-23** faithful/connected (one source, two renders, default faithful) · **D-24** rigid-spine/flexible-edge (the per-field operate-on-it rule; JSONB attribute bag NOT built — defined for when it is) · **D-25** intelligence tiers (Tier-0 world knowledge = day-one value) · **D-26** dual lexicon (Happy Hose; honesty-corrected to SINGLE-SOURCE — no cross-grower confirmation). Structure-Last also named in bootstrap ⚡ OPERATING FACTS beside VERIFY-BEFORE-BUILD. **NEW `docs/domain/` knowledge base:** `README.md` (the 9-section MAP with depth tags + the before-any-domain-build rule) + `ontology.md` (size/ANSI-Z60.1 · naming/token-set · category/seasonal-vs-specimen · TX sourcing map). **user_stories.md +2** (asset-inventory-pmi, MAPS-TO 2.3): the no-system grower (count builds the catalog) + the real spoken-count spec (Billy Bob + the messy walk) with the voice-attribution cardinal requirement. **PROOFS wired:** #61 L4 resolve + #60 field-debug → flipped OWNER-PROVEN; #63 rhythm logger → PARTIAL proof + attended-only finding. **OPEN items recorded** (below): A/B next-build decision, offline-session priority elevation, receipt discount-line recon, Barryhill transcript parked, buyer-per-category note. **DOCS-ONLY** — no app code, schema, migration, or `[TRACE:*]` touched. | `8be1c77` | **BUILDER-COMPLETE** (docs-only; `npm run verify` exit 0 zero NET-NEW) | None — docs bank. The captured items it points to carry their own owner-proof bars (north star = horizon doctrine; OP-10/D-23..26 = captured, mostly UNBUILT; domain KB = living reference) | none |
| 66 | Barryhill / Trinten field-research bank (primary-source grower evidence) | **DOCS-ONLY.** Banked David's ~2-hour recorded field visit with **Trinten**, a category buyer at Barryhill Garden Center (Leander, TX, 4.5★ full-service) into the domain KB as primary-source evidence. **NEW `docs/domain/field-notes-barryhill-2026-06.md`** — 8 confirmed findings: (1) buyer-per-category = inventory cognition distributed across humans' heads · (2) NO inventory system, clipboards + Excel · (3) POS sales-only, doesn't decrement per-variety = the reconciliation hole in the grower's words · (4) budget-from-last-year +5–10% · (5) **seasonal reconciliation rhythm PER-CATEGORY** (seasonal fast/markdown · trees slow/appreciate · tropicals inverse) · (6) pot-size vernacular (quart/4-inch/6-inch/gallon by what's-in-the-pot and who's-speaking) · (7) lean-inventory / anti-overextension = field validation of the suggest-LESS doctrine · (8) grocery-inventory crossover. Warm interview contact recorded as a FACT only. **PII REMOVED (corrective, commit-after-`4f213b1`):** the phone number originally banked in the field-notes doc was removed from the working tree (contact fact kept; details now held by David outside the repo); added a standing **no-PII-in-the-repo** rule to the field-notes header + domain-KB README. ⚠️ **History tradeoff (awaiting David's call):** the number still lives in commit `4f213b1`'s history — current-file removal is the practical fix for a private/trusted repo; full scrub needs a `git filter-repo`/BFG history rewrite + force-push (rewrites shared history, affects anyone who has pulled) — NOT done this pass. **ATTRIBUTION CAVEATS HONORED:** Happy-pipe/Happy-hose = DAVID (dual-lexicon stays SINGLE-SOURCE Lauren, no cross-grower confirmation recorded); anti-Nelson accountant story = DAVID (not attributed to the grower); seasonal rhythm = co-constructed (D proposed veg-markdown/tree-appreciates, T confirmed + added tropical inverse). **WIRED:** domain KB MAP (README §3 + §7 + status line) · ontology §3 reconciliation-rhythm flipped open-research→**CONFIRMED (field-validated)** + §7 suggest-LESS field-validated · bootstrap Capture Index row + FIELD FINDINGS flipped (rhythm/buyer-per-category/no-system = ✅ field-confirmed). NO app code, schema, migration, or `[TRACE:*]` touched. | `4f213b1` | **BUILDER-COMPLETE** (docs-only; `npm run verify` exit 0 zero NET-NEW) | None — field-evidence bank. The reconciliation-rhythm research question is now CLOSED with field evidence (was "pending clean transcript"); deeper §4/§7/§8 enrichment owed by future research (warm contact available) | none |
| 60 | Wrap-and-capture: sequencing recon + field-debug capture (entry build) | **RECON + RECOMMEND + a trivial entry build.** **#1 emit mechanism:** TRACE = ~500 scattered raw `console.*('[TRACE:AREA]…')` sites, 40 area tags, NO central emitter, NO persistence (`BusinessProvider.tsx:281`, `CountOnceSeam.ts:586`, `customers/create.ts:48`) → cheapest tee = a **console interceptor** at boot, zero per-site edits. **#2 wrap cost:** thin — cultivar-os is a Vite SPA with NO manifest/SW/apple-meta (all absent); installable PWA = manifest + 2-3 icons + apple meta + small app-shell SW, ~3-4h additive, NO native shell, NO app-vs-wrapper layer. **Correction to David's hypothesis:** a PWA does NOT unlock richer capture — `navigator.share` (incl. files), `Blob` download, localStorage all work in a mobile Safari/Chrome TAB today → capture is DECOUPLED from wrapping. Persistent login already works online (Supabase `persistSession:true` default, `client.ts:11`); the offline→onboarding bounce is a `getUser()` network call at `BusinessProvider.tsx:237`, identical tab-or-PWA. **#3 sequencing CALL:** ship capture NOW → fix resolver + session NAKED, prove with the new capture → wrap as a thin PWA LAST. Reason: capture is the instrument + fork-independent; the SW interacts with the very offline/session surface being fixed (don't fight two things); wrapping buys only the install-tile UX (do last, after session survives offline). **#4 capture method:** console interceptor → 600-entry localStorage ring buffer (survives reload + window.onerror/unhandledrejection crash-flush) → `?debug=1`-gated 🐞 panel with Share (OS share sheet → .txt to Lightning) / Download / Copy / Clear; demos stay clean (Lauren's link has no `?debug=1`). Privacy: export carries tenant ids/emails (internal use), surfaced; redaction a named step-up. **BUILT this pass** (entry, fork-free): `shared/src/debug/captureBuffer.ts` (+barrel), `cultivar-os/src/components/DebugPanel.tsx`, wired `main.tsx`/`App.tsx`/shared index. New `[TRACE:CAPTURE]` ON. **NOT built (recommend+STOP for David's pick):** the PWA wrap + the session/offline + resolver fixes (separate prompts). | `e8c9998` (+ `docs/decisions/2026-06-27-wrap-and-capture.md`) | **BUILDER-COMPLETE** (`build:cultivar` clean 2233 modules +2; `npm run verify` exit 0 zero NET-NEW — tsc 10 / eslint 266 / knip 10·14·15; eslint clean on changed files; deterministic node smoke: interceptor captures `[TRACE:*]` verbatim w/ tenant ids, levels preserved, header carries url/ua/online, crash flushes to storage, buffer survives reload, clear empties) | **OWNER-PROVEN** (2026-06-29, David on iPhone — trail exported from the phone via the 🐞 panel; it was the instrument that proved L4 resolve #61). CLOSED | none |

---

## CLOSE THESE TABS NOW
*OWNER-PROVEN, or BUILDER-COMPLETE doc-only / recon / superseded with no live dependency. Close these editors.*

1. **#1 AIEngine deprecation** — ledger decision, no live feature.
2. **#3 RLS migrations (read-wall)** — applied as postgres, read-wall HAR-proven, matrix green. (Write-wall live proof rides #5.)
3. **#7 Citation convention** — doc convention.
4. **#8 Ignition RBAC audit** — recon complete (docs untracked by design).
5. **#12 Role-config recon** — recon complete (doc untracked by design).
6. **#14 Doctrine commits (D-010..D-015)** — doctrine captured.
7. **#15 Design captures (Regina + social-intelligence)** — doc-only doctrine. (Delivery code is deploy-verified live; only the optional phone owner-prove of the full chain remains — not blocking.)
8. **#41 / #43 / #44 Doctrine captures (surfacing-thesis · opportunity-cost · suggestion-engine-forward)** — doc-only into MASTER_BRIEF PART 4 / DECISIONS.md; no live dependency. (Open DECISION still owed on #41: the Regina planted-vs-purchased anchor conflict.)
9. **#60 Field-debug capture** — OWNER-PROVEN 2026-06-29 (trail exported from iPhone; it was the instrument that proved #61).
10. **#61 Grower-resolve L4** — OWNER-PROVEN 2026-06-29 (Shoal Creek Vitex → L4 → DISC-1105 → count wrote 45; the LAWNS demo-blocker is dead).
11. **#64 27–29 June master bank** — docs-only; the captured items carry their own bars.

→ **11 tabs closeable now.**

## OWNER-PROOF OWED — ONE DEPLOY CLEARS THESE
*All of these close in a SINGLE owner + staff live session after one Vercel deploy. Deploy `main`, then run one owner JWT + one no-`view_costs` Staff JWT through the app:*

- **#2 / #4 Part A render layer** — Staff session: cost requests absent from dashboard, inventory query qty-only, direct nav to gated routes redirects.
- **#5 Gate-3b write-wall** — no-`view_costs` JWT → 403 on cost-apply; owner JWT → succeeds.
- **#9 Tile registry grid** — owner sees all live tiles incl. 6 financial surfaces; Staff sees neither cost tiles nor cost readouts.
- **#10 Vertical field** — live grid unchanged for nursery/generalist (no regression).
- **#11 Identity header** — header on multiple pages; role badge correct owner vs Staff.
- **#47 Front-door promotion + 4 bug-fixes** — fresh business (Dave's Trees + conflicting-address URL): progressive screens → reveal raises the ADDRESS conflict ("Use site value" persists) → catalog seeds ("Added N items") → demo route bookends business→stops→business → QBO connects from Settings → non-8.25% tax set in Settings reflects at checkout.

→ **6 items, 1 deploy + 1 two-JWT live session.**

## GENUINELY OPEN
*Not built / not run / half-done — each with its next action.*

- **Build B (queued, NOT built) — per-tenant test-data seed + reset function** — `load-test-data` (seed a prospect's scraped catalog into the new Person-spine structure for a demo) and `wipe-test-data` (clear that tenant's test rows before loading their REAL data at signup). MUST be `business_id`-scoped — a test-wipe never touches another tenant (RLS isolation applies). TRACE emits on seed/wipe. This is the demo→production handoff mechanism; built AFTER the spine so it seeds into the correct shape. **Next:** build after CP1/CP2 are owner-proven. (This is how the real LAWNS demo data returns post-full-nuke.)
- **#13 Campaign double-representation diagnostic** — not run; no doc exists. **Next:** run the diagnostic (queued).
- **#6 costDiscovery live proof** — `verify-cost-discovery.ts` (live service-key) UNRUN; then owner-prove cost-apply under Role Machine. **Next:** run the service-key proof, then live owner-prove the gated endpoint.
- *(Forward, not this session)* **#1 AIEngine removal** — blocked until 9 Ignition tasks + tier-gating port to Vercel fns and 3 modules repoint.
- **A/B NEXT-BUILD DECISION (David to decide)** — (A) session-persistence / offline-login fix [Lightning's lean — fixes the floor + protects the demo] vs (B) the count-side size-picker [finishes the size-variant feature, ledger #62]. **Next:** David picks A or B; both are scoped, neither started.
- **OFFLINE-SESSION BUG — ELEVATED to priority (was "fix before PWA wrap")** — proven painful in real field use this session (rhythm logger logged David out mid-trip on a network hiccup; `BusinessProvider.tsx:237` `getUser()` network call). A field tool cannot have network-dependent login; persistent, offline-tolerant login is **foundational**, and a low-signal greenhouse count could log Lauren out mid-count → threatens the demo. **Next:** make login offline-tolerant (this is option A above).
- **RECEIPT KEEPER DISCOUNT-LINE BUG (recon-first, found 2026-06-28 on a real Lowe's receipt)** — the parser models "DISCOUNT EACH" as standalone negative line items instead of a per-unit modifier on the line above, and drops qty/unit-price ("2 @ 6.28"); result = a false "$3.06 below total — possibly tax/tip" warning when the receipt actually reconciles (82.24 + 6.78 tax = 89.02). OCR read fine; the failure is the line-item MODEL. Discounts are cost-to-produce signal (the military discount lowered true loaded cost). The line model needs qty + unit_price + per_unit_discount + extended net. **Next:** LOOK before fixing (may be in the OCR adapter or the line-item type) — recon, then build.
- **RECONCILIATION RHYTHM research (partially answered, pending clean transcript)** — rhythm is PER-CATEGORY: seasonal/veg = fast clock + markdown-or-lose; trees = slow clock, carry over + APPRECIATE; tropicals = inverse (out before winter). **Next:** confirm against the cleaned Barryhill transcript (banked in domain ontology §3).
- **Barryhill/Trenton field transcript — PARKED** for laptop speaker-de-mixing before it can be banked as clean evidence (the no-speaker-separation caveat — see D-26 honesty correction + the spoken-count story's cardinal requirement). **Next:** de-mix on the laptop, then bank.
- **BUYER-PER-CATEGORY structure (open design note, from Barryhill, provisional)** — inventory cognition is distributed across humans' heads, partitioned by category (perennial buyer, houseplant buyer…), buyers on the floor. May affect how roles/permissions + the count tool are scoped. **Next:** confirm with a clean grower interview before it shapes a build.

---

## Fabrication check — half-done items surfaced
*For each BUILDER-COMPLETE claim, verified code/commit/migration backs the ledger line.*

1. **Registry handoff said `__PENDING__`; code says COMMITTED.** Handoff entries for the tile registry (#9) and vertical field (#10) both carry `Commit __PENDING__`. Repo truth: `c36e562` and `c7cc410` are landed and pushed. **Ledger corrected to the real SHAs** — not a fabrication of completion, a stale commit placeholder.
2. **#6 costDiscovery — genuinely half-done (flagged, not hidden).** Built-inventory/handoff correctly label it BUILDER-COMPLETE on 26 unit tests but NOT owner-proven, with `verify-cost-discovery.ts` UNRUN. Confirmed: the script exists (`scripts/verify-cost-discovery.ts`) but there is no live-proof artifact. The earlier write-wall gap it carried is now closed by #5 (cap #7 PASS). **Stays OPEN until the live proof runs.**
3. **#13 campaign diagnostic — claimed "queued," confirmed NOT RUN.** No `*campaign*` diagnostic doc exists under `data/grower-scan/` or `docs/`. Correctly OPEN.
4. **Migrations: no written-not-applied half-done found.** All cost/RLS migrations through `20260622` (`is_active_member_canonical_rls`, `oauth_secrets_relocation_and_cost_wall`, `financial_wall_phase2`, `business_discovery_profiles`, `recovery_basis`, `deliveries*`) are applied per the ledger's owner-confirmed-as-postgres state, and the matrix that depends on them (cap #3/#4/#6/#7) exits 0. No staged-but-unapplied migration is sitting in the tree.
5. **verify-universals matrix is real and green** — ran `node scripts/verify-universals.mjs` → exit 0; Cultivar column #1–#7 + #a + #e all PASS; Ignition #1 PASS, rest documented SKIP. The caps backing #2/#4/#5/#9/#10/#11 are live assertions, not stubs.

**No item claimed done that the code contradicts** (the one mismatch — `__PENDING__` SHAs — was the handoff lagging the commits, corrected above).
