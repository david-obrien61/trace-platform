{
  "table": "customers",
  "columns": [
    "billing_city",
    "billing_line1",
    "billing_line2",
    "billing_state",
    "billing_zip",
    "business_id",
    "created_at",
    "credit_limit",
    "customer_type",
    "display_name",
    "email",
    "first_name",
    "id",
    "import_run_id",
    "last_name",
    "lifetime_value",
    "marketing_opt_in",
    "notes",
    "organization_name",
    "payment_terms",
    "person_id",
    "phone",
    "price_tier",
    "qb_customer_id",
    "source",
    "status",
    "tax_exempt",
    "tax_exempt_cert_doc_url",
    "tax_exempt_cert_ref",
    "tax_exempt_expires",
    "tax_exempt_reason",
    "tax_id",
    "updated_at"
  ],
  "taken_at": "2026-09-07T14:28:53.914Z",
  "source": "live PostgREST projection (SELECT * LIMIT 1) taken 2026-09-07, MINUS the four columns dropped by supabase/migrations/20260915b_drop_legacy_customer_address.sql (ledger #335). \u26a0\ufe0f THIS FILE IS NO LONGER A PURE MEASUREMENT AND SAYS SO: the drop migration is WRITTEN and NOT APPLIED, so the live table still has address_line1/city/state/zip until David runs it. The four were removed here rather than left in, because a snapshot listing a column the repo's own migration deletes is a stale declaration nobody re-derives (tech-debt #73 / R-26's class) \u2014 and the import's insert list must be checked against what it is allowed to write, not against what history allowed. RE-RUN scripts/snapshot-customers-columns.mjs IMMEDIATELY AFTER APPLYING to make this a measurement again.",
  "refresh_with": "node scripts/snapshot-customers-columns.mjs",
  "guarded_by": "packages/shared/src/quickbooks/customerImport.test.ts \u00a7M \u2014 asserts the import's insert list is a subset of this, AND that every ALTER TABLE customers ADD COLUMN in the corpus is a subset of this (so a migration without a refresh fails the build)",
  "pending_migration": "20260915b_drop_legacy_customer_address.sql (written, NOT applied)"
}
